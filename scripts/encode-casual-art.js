'use strict';
// Encode the original generated covers as WebP without changing their composition or dimensions.
const fs=require('node:fs'),{chromium}=require('../tests/браузер-робот');
const ids=['mines','klondike','spider','freecell','mahjong','blocks','match3','wordsearch','fifteen','snake'];
(async()=>{const browser=await chromium.launch();let before=0,after=0;try{const page=await browser.newPage();for(const id of ids){const file='img/casual/'+id,input=fs.readFileSync(file+'.png');const out=await page.evaluate(async data=>{const img=new Image();img.src=data;await img.decode();const canvas=document.createElement('canvas');canvas.width=img.naturalWidth;canvas.height=img.naturalHeight;canvas.getContext('2d').drawImage(img,0,0);return canvas.toDataURL('image/webp',.84).split(',')[1];},'data:image/png;base64,'+input.toString('base64'));const buffer=Buffer.from(out,'base64');fs.writeFileSync(file+'.webp',buffer);before+=input.length;after+=buffer.length;console.log(id+': '+Math.round(input.length/1024)+' KB → '+Math.round(buffer.length/1024)+' KB');}console.log('Total: '+(before/1048576).toFixed(1)+' MB → '+(after/1048576).toFixed(1)+' MB');}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
