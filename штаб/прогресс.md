# Прогресс штаба — письмо следующей сессии

Обновлено 22 сентября, 21:30 (по часам из команды). Обновлять при завершении
сессии штаба и после каждого большого этапа.

## ПЕРВЫМ ДЕЛОМ
1. **Владелец велел работать нон-стоп и не экономить** (22.09: «добавляй исполнителей,
   у нас много токенов можно не экономить до среды 7 утра»; 18:48: «почему только два
   исполнителя работают??»). Держать 4–6 исполнителей разом, разводя по файлам.
   Вопросы — в `штаб/важное.json`, не в ленту.
2. **Главный акцент по слову владельца (22.09 ~18:30): «дурак и конкретно рейтинг,
   чтобы он был как в эталоне».** Эталон — папка
   `C:/Users/Admin/Downloads/leaderboard_cloudcode_pack_v1/leaderboard_cloudcode_pack_v1/`
   (prototype/app.js и styles.css — источник истины по структуре и размерам;
   references/screens/01_global.png, 02 (шторка), 03_durak.png — что видит владелец).
3. **Опубликовано последним:** `a4a6cff` — рейтинг по макету, партия 5 (?v= style.css 70,
   рейтинг-экран.js 19, нижние-вкладки.js 7 на всех четырёх страницах; в 21:30 Pages ещё
   отдавали 69/18 — проверить, что доехало), `083e16b` — хуки прав (принято через ревью).
   Боевой сервер комнат 8790 стоит на `30324c0` (PID 6004). Здоровье:
   `node -e "fetch('http://127.0.0.1:8790/').then(r=>r.text()).then(console.log)"`.
4. **Незакоммиченное в рабочей папке — живая работа, НЕ откатывать:** с 21:29 шторка
   выбора игры (js/рейтинг-экран.js — разработчик js, style.css — разработчик css, параллельно
   по договору классов), с 21:30 tests/снимок-рейтинг-эталон.js и tests/кнопки-назад-везде.js
   (тестировщик), js/нарды-бот.js (проба «максимум p», с 20:59).

## Партия 5 рейтинга — ЗАКОММИЧЕНА a4a6cff 21:28 (план штаб/план-рейтинг-по-макету-2.md, раздел 6)
- Принято: 51/0 снимочной проверки (ломающий 51/2), node-проверки зелёные, смотритель — подиум,
  список, значок-сетка, ПК в порядке. Пол списка по замеру 224 (был 288 — считан вручную, ошибка).
- **Находка смотрителя 21:26 (не регресс, но направление партии 6):** на «Дураке» 390×844
  список получает 282px, подиум стоит ВНУТРИ #рейтинг-таблица и съедает почти всё — после
  подиума видна одна обрезанная строка «4 Ольга», остальное во внутренней прокрутке. В макете
  внутренней прокрутки нет: прокручивается вся страница, полоса закреплена. Партия 6 — не ужимать
  список в остаток, а прокрутка страницы как в макете (или подиум вне прокрутки); цифры даст
  тестировщик 21:30 (высота подиума, scrollHeight списка, сколько строк видно).
- Ниже — история приёмки (для справки, всё сделано):
- Сдано и принято штабом: js (список с 4-го места порциями по 10, «Показать ещё», «⋯»,
  Intl ru-RU, «Повторить», карточка новичка, значок игры, инициалы) + css (ровная таблица,
  53px, подиум 86/1.1fr/5, кнопка выбора 56 со значком, аватар 35 с кольцом, ПК 64/40) +
  два замечания ревьюера исправлены (фото побеждает инициалы; сброс порции) + значок-сетка
  «Все игры» (img/рейтинг/все-игры.svg + js) + хвост css «экран выше окна» (gap 10→6,
  карточка 96→72, пол списка 306→288 — считано вручную, разошлось с замером на ~370px!).
- node-проверки после всего: партия-5 33/0, четыре-игры 380/0, подиум 70/0, по-правилам 23/0,
  по-составу 25/0, нижние-вкладки 87/0.
- **Идут прямо сейчас:** тестировщик (браузер) — tests/снимок-рейтинг-эталон.js: поля
  движение/сдвиг, замеры (аватар 1 > 2/3, экран ≤ окна, «Моё место» и полоса в кадре,
  высоты всех детей экрана на 390 и 1280), пять снимков; разработчик css — правило 25×25
  для `.рейтинг-выбор-игры__значок--все`.
- **Приёмка дальше:** отчёт тестировщика → смотритель (снимки рядом с references/screens/03)
  → `node scratchpad/поднять-версии.js` (style.css 69→70, рейтинг-экран.js 18→19 на ВСЕХ
  четырёх html; scratchpad сессии 22.09 — если нет, написать заново по образцу) → grep v=70/v=19
  → коммит по scratchpad/коммит-партия-5.txt (добавить строку про svg-значок и хвост css)
  с явным `git add` (js/рейтинг-экран.js, style.css, четыре html, img/рейтинг/все-игры.svg,
  tests/рейтинг-партия-5.js, снимок-рейтинг-эталон.js, новые-кнопки-и-рейтинг.js,
  кнопки-назад-везде.js, все-экраны-в-полном-экране.js, снимок-рейтинг-золото.js,
  снимок-сукно-согретые.js) → опубликованный адрес отдаёт v=70/19 → дашборд → коммит штаба.
- **Хвост «экран выше окна» коммит НЕ держит**: пол 306 опубликован ещё в 09e7cd3. Если
  замер покажет, что не влезло, — следующему css-разработчику давать НАСТОЯЩИЕ высоты узлов
  из отчёта тестировщика, направление: список берёт остаток, пол — не выше подиума.

## Идёт сейчас и дальше (оба шага трогают js/рейтинг-экран.js и style.css — по очереди)
1. **Шторка выбора игры — В РАБОТЕ с 21:29**: план `штаб/план-шторка-выбора-игры.md`, два
   разработчика параллельно (задания в scratchpad сессии: задание-шторка-js.md / -css.md).
   После их отчётов: тестировщик обновляет тесты из плана (новые-кнопки-и-рейтинг.js:336–350,
   рейтинг-экран-четыре-игры.js:65–93, стрелка-после-рейтинга.js:415–473,
   снимок-рейтинг-эталон.js:442–443/545–546, снимок-рейтинга-с-панелью.js:21) + снимок шторки
   390×844 → смотритель рядом с references/screens/02 → ?v= 71/20 на четырёх html → коммит.
2. **Партия 6**: карточка «Моё место» по макету отдельным узлом вне списка (`#рейтинг-моё-место`
   есть в разметке и прячется классом «скрыт»), всегда при вид.я, подпись узлом из js (п.16),
   своя строка в списке подсвечена (п.17), карточка-кнопка «соседи → В начало списка» (п.18,
   макет app.js:73, 92, 168) + экран в окно по настоящим высотам. Пункт 7 (полоса «Сила игры |
   Очки за победы») и три ряда фильтров — по ответу владельца `рейтинг-ряды-фильтров`.
- Заделы: фото чужих игроков через бота; догрузка списка со страницы 2.

## Прочее в работе / очереди
- **Хуки прав приняты и закоммичены (083e16b)**: при остановке запись помечается `завершил`,
  откат по роли не берёт завершённых, чистка старше 24 ч на каждом запуске, охрана узнаёт
  возвращённого по номеру. Ловушка «возвращённый исполнитель без прав» закрыта. Заделы:
  гонка записи права.json, автотест хуков (не начинать без слова владельца).
- Нарды «мастер»: проба 2 (табличная вероятность удара) дала 56,0/56,8 против базы 54,2 —
  порог 57 не взят, журнал в шапке js/нарды-бот.js (41aca4f). Идёт проба 3: «максимум p» +
  закрытые пункты + зерно 20260922 (~2×45 мин), с выключателем, база и проба одним зерном.
- Тестировщик с 21:30 (браузер занят им): долг снимок-рейтинг-эталон.js (шапка про --сломать,
  ложно-зелёная «Моё место» → липкая своя строка, один снимок без Telegram с «‹», замеры
  подиума) + раздел 5 tests/кнопки-назад-везде.js. tests/все-экраны-в-полном-экране.js и
  снимок-рейтинг-золото.js после партии 5 браузером не гонялись — следующему тестировщику.
- Владельцу показано наглядно (вопрос `серые-плитки-на-сукне`): три снимка лобби (как есть /
  согреты карточки / согреты карточки и полоса), смотритель за третий; кнопки ответа стоят.
- Браузерная очередь: сейчас тестировщик долга; после — тесты шторки, все-экраны/золото,
  tests/самая-длинная-подсказка.js (только на тихой машине). Лобби 320×568 под часы Telegram
  (−13/−29/−29px) — в очереди.
- Ждут слова владельца: `серые-плитки-на-сукне`, `вм-домен-или-туннель`, `вм-два-клауда`,
  `рейтинг-ряды-фильтров`. «Друзья онлайн» — черновик плана есть, НЕ начинать.
- Коннектор Notion в claude.ai не авторизован — сказать владельцу один раз.

## ВМ владельца (запись очереди «Разместить проект на Ubuntu…»)
- Хаб NodePulse, ключ `NP_API_KEY` в bot/.env — НЕ СВЕТИТЬ. Всё через `node штаб/вм.js`
  (устройства | выполнить <id> <файл-скрипта> | сырой GET /runs/<id>) — файл и
  `штаб/вм-опись.md` в .gitignore, не коммитить. Опись — единственный источник фактов.
- Этапы 1–2 сданы (node 24, Claude Code без входа, cloudflared, /srv/games на 57119b9 —
  перед этапом 4 git pull на ВМ, три службы systemd выключены). `штаб/вм-перенос.js` —
  звать только из PowerShell. **Этап 3 ждёт владельца** (server/данные через хаб — данные
  людей лягут в историю запусков; bot/.env и вход Claude — его руками). Этап 4 — переключение
  (погасить Windows-сервер и бота → games-server → games-tunnel → адрес в bot/.env руками
  владельца → games-bot → опубликованный адрес). Два бота на одном токене работать не должны.

## Как работает надзор (22.09)
- Права исполнителя — по его номеру в `штаб/права.json`; хук берёт список из строки
  «Файлы: a, b» задания. **Без строки — `файлы: []`, и привратник пропускает ВСЁ.**
- Отчёт агента приходит ТОЛЬКО в task-notification, `.output` пуст. Лимит 40 ходов —
  продолжать SendMessage по номеру сразу (< 5 минут), с планом ≤10 шагов; в задание писать
  «фоновые прогоны ждать по уведомлению, не опрашивать в цикле».
- Запись в штаб/в-работе.json иногда переживает агента (хук не снял) — снимать скриптом по
  номеру задачи, `node штаб/кто-молчит.js` покажет «молчит 2 ч».
- Команды с `.env` привратник не пропускает — помощники читают ключ сами во время работы.
- важное.js: заголовок ≤ 95 знаков, кнопки — объекты {ключ, имя}, не строки.
- Дашборд 8791 отдаёт картинки только из корневых `снимки/` и `эталон/`: снимок из
  tests/снимки копировать в снимки/ и вставлять `![подпись](снимки/имя.png)`.

## Ловушки (все стреляли)
- Обратные кавычки внутри `node -e` bash выполняет как команды (22.09 так перезапустился
  боевой сервер) — длинный текст только файлом через Write.
- css-разработчик без браузера считает высоты вручную и ошибается на сотни точек: хвосты
  про «влезает ли» отдавать только с замерами тестировщика в задании.
- Скрипты на ВМ идут под dash: только латинские имена переменных, POSIX; хаб рвёт
  запуск на ~5-й минуте, но запуск продолжается — досматривать «сырой GET /runs/<id>».
- `чистаяСтрока` в js/рейтинг-экран.js вырезает незнакомые поля ответа — новое поле
  сервера надо прописать там, иначе клиент его «не видит».
- В Telegram «‹» рейтинга прячет правило `html.в-телеграме #кнопка-рейтинг-игроков-назад`
  (системная стрелка вместо неё) — на снимке без «‹» сперва проверить этот класс.
- Поддельные данные /рейтинг в снимочных проверках без `движение`/`сдвиг` → стрелок на
  снимке нет, смотритель зря тревожится.
- `\b` в regex не знает кириллицы; код выхода через `| tail` врёт; `?v=` менять
  node-скриптом по четырём html; `grep -i` не знает регистра кириллицы.
- «fatal: not a git repository» в начале прогонов с копией сервера — не провал.
- Жалоба на вид — не описание нужного: показывать снимки, спрашивать в «Важном».
