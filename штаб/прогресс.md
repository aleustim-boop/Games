# Прогресс штаба — письмо следующей сессии

Обновлено 22 сентября, 17:52 (по часам из команды). Обновлять при завершении
сессии штаба и после каждого большого этапа.

## ПЕРВЫМ ДЕЛОМ
1. **Владелец велел работать нон-стоп** (21.09, дословно): «пока ты не сделаешь все
   задачи, не останавливайся… я же не могу с тобой сидеть с утра до вечера». Вопросы
   не задавать в ленту, а записывать в `штаб/важное.json` и работать дальше.
   Экономия отменена: исполнителей запускать пачками, разводя по файлам.
   Последнее слово владельца 22.09 17:21: «так доведи все до точки я пересоздам
   сессию» — эта сессия закрыта чисто, в работе никого, всё закоммичено.
2. **Опубликовано сегодня после 17:00 (всё доехало на GitHub Pages, проверено):**
   `e5440bf` (17:37) письмо знакомому отказывает честно: «chat not found» = «Друг
   ещё не открывал нашего бота», Telegram-ошибка с кодом, зов снимается своей меткой,
   минута ожидания не тратится (server/сервер.js, tests/зов-знакомого.js 93/0);
   `9f77f01` (17:47) страница дурака снова ТЁМНАЯ, как у всех игр, сукно ТОЛЬКО на
   столе и всегда бордовое, выбор цвета сукна убран (style.css?v=68 на четырёх
   страницах, js/game.js?v=89; смотритель подтвердил семь снимков); `23a5264` (17:48)
   мастер нард ценит якорь в чужом доме по глубине. До них: `e5dc2bd` ход по лимиту
   времени озвучивается по факту, `4b66423` золото рейтинга одним числом,
   `5eb43da` событие «ход-по-времени» на сервере.
3. **Боевой сервер комнат 8790 жив, стоит на `e5440bf`** (перезапущен 17:38 при
   0 комнат; здоровье в 17:48: комнат 0). Здоровье спрашивать так:
   `node -e "fetch('http://127.0.0.1:8790/здоровье').then(r=>r.text()).then(console.log)"`
   — кириллица через curl не доезжает («Такой запрос сервер не принимает»).
   Перезапуск — `node штаб/перезапуск-сервера.js`, только посмотрев число комнат.
   Серверных правок после `e5440bf` нет — перезапускать нечего.
4. **Незакоммиченного нет.** `штаб/в-работе.json` пуст, исполнителей не осталось.
   Папка `$ЦЕЛЬ/` (чужая копия img/ и js/ из-за неразвёрнутой переменной в команде
   тестировщика, 17:37) сверена diff -rq — байт в байт — и убрана 17:52;
   `tests/корень-чистый.js` 26/0.
5. **Ответы владельца 17:13–17:23 разобраны** (важное.json помечено «решено»):
   картинки медалей/короны и стрелку движения места — «взять один в один» (обе записи
   в очереди «скоро»); вокруг стола в партии — тёмным (совпало с 9f77f01); диалог
   Telegram при закрытии — «Оставить как есть» (запись очереди закрыта, код не трогать).

## Следующее по очереди (в этом порядке)
1. **«Рейтинг на ПК не похож на макет» — ГОРИТ.** План готов:
   `штаб/план-рейтинг-по-макету-2.md`, четыре этапа. А (js/рейтинг-экран.js: погасить
   карточку `#рейтинг-моё-место`, строка «моё место» внизу, подписи внутри тумб,
   снять замки друзей у дурака, шапка списка, «i») и Б (style.css: сегмент «Все игроки |
   Друзья» золотом, тумбы, ширина через переменную колонки вместо max-width:520px
   ~11419) — параллельно двумя разработчиками; В (js/нижние-вкладки.js: набор
   «рейтинг», активная вкладка золотая); Г (tests/: снимок-рейтинг-золото.js строки
   476, 509, 850 нужен `:not(.рейтинг-строка--шапка)`). Приём — снимок 1280×800 и
   390×844 смотрителю РЯДОМ с макетом владельца; поднять ?v= у style.css (68→69),
   js/рейтинг-экран.js и js/нижние-вкладки.js на ВСЕХ четырёх страницах.
2. После него, тем же файлом js/рейтинг-экран.js по одному исполнителю: картинки
   медалей/короны из макета (запись «Рейтинг: медали, корона и значки игр —
   картинками…», img/рейтинг/, 1x+2x, цифра поверх медали текстом), затем стрелка
   движения места (server/рейтинг.js суточный снимок мест — ревьюер обязателен;
   запись «Хранить суточный снимок мест…»).
3. Браузерные (по одному!): кнопка «Назад» дурака (tests/финальная-проверка-кнопок-назад.js);
   код выхода `tests/снимок-тёмных-служебных-экранов-дурака.js` — сегодня прогон
   даёт 8/0 и код 0, запись в очереди, возможно, уже устарела — проверить и закрыть
   приметой; центральный аватар подиума.
4. Нарды «мастер»: турнир 500 (зерно 20260915, 17:07–17:50): короткие 52,6% ±4,4
   (порог 57% НЕ взят, разработчик мерил 55,0% — дрожание прогона), длинные 65,4% ±4,2
   (взят), лестница 71,6/60,8 (ок), ход 99-й перцентиль 58 мс (ок). Следующие идеи
   разработчика в scratchpad прошлой сессии не сохранились — брать из заметки записи
   очереди: вероятность быть побитым по расстоянию, строители числом бросков,
   контакт/гонка отдельными весами. Турнир 500 идёт 40+ минут — запускать фоном.
5. Полный прогон `tests/самая-длинная-подсказка.js` — только на тихой машине.

## Ждут ответа владельца (в `штаб/важное.json`, «нужен ответ» — три)
1. `серые-плитки-на-сукне` — серые карточки на бордовом сукне: согреть или оставить.
2. `слова-про-очки-после-возврата` — какими словами сказать «доиграть можно, но без очков».
3. `рейтинг-фон-у-дурака` — рейтинг у дурака: сукно или чёрный. После 9f77f01
   (сукно только на столе) ответ, скорее всего, «чёрный» — но не решать за него.
**Оформление плиток без ответа не трогать.** «Ищем соперника» — не чинить без
просьбы. «Профиль» ведёт на разные экраны — ждёт настоящего экрана профиля.

## Как теперь работает надзор (22.09)
- **Права исполнителя — по его номеру** (`штаб/права.json`, ключ = agentId из ответа
  Agent, поле `номер`). Строка «Файлы:» в задании — пути через запятую.
- **Отчёт фонового агента приходит ТОЛЬКО в уведомлении** (`task-notification`);
  `.output` завершённого агента пуст — отчёт брать сразу.
- **Агенты упираются в лимит 40 ходов** посреди работы — продолжать `SendMessage`
  по номеру с коротким планом и бюджетом (контекст > ~200k — не возвращать).
- Живость исполнителей: `node штаб/кто-молчит.js`.
- Приметы «сделано» у записей очереди: `{файл, есть|нету}` (подстрока в файле),
  `{слова:[…]}` (все слова в одном заголовке коммита, регистр не важен) или прямое
  `"закрыто": {когда: ISO из date, чем}`; `node штаб/списки.js` убирает опознанные.
  После запуска индексы съезжают — искать записи по тексту `что`, не по номеру.
- Браузер — строго один проверяющий разом. Штаб картинки не открывает — смотритель.
- **В каждое задание писать:** «Временные папки НЕ УДАЛЯЙ — чистятся сами», занят ли
  браузер, запрет склейки команд, запрет git checkout/restore/stash/reset, **пути к
  файлам вывода — с ПРЯМЫМИ слэшами**, и **переменные в командах копирования
  должны быть определены** (иначе в корне вырастает папка `$ЦЕЛЬ/`).

## Ловушки, о которых забывают (все стреляли)
- **Обратные кавычки внутри node -e bash выполняет как команды.** 22.09 15:06 так
  случайно перезапустился боевой сервер. Длинный текст — только в файл через Write
  или heredoc с закавыченным словом-концом, потом `node файл`.
- **Heredoc в Bash-инструменте срывается на строках вида `{файл:'…', есть:'…'}`** —
  такой текст писать инструментом Write.
- **735e507 (сукно на всех экранах дурака) ОТМЕНЁН коммитом 9f77f01.** Не возвращать:
  владелец сказал «я просил только поменять игральное сукно». Сукно читают только
  `#зона-стола, .стол-игры` (~7813), `.пример-перевода, .рассадка` (~10828),
  `#рейтинг-главное` на «Как я играю» (~11477). Темы дыхание/лампа/туман/отсвет —
  только слой `.фон-игры` (~468–580). Первый образец фона у дурака подписан «Сукно»
  (общее имя из ОбщийФон, как в шашках) — владельца может смутить; образцы на глаз
  слабо различимы — если пожалуется, это косметика в общем правиле `.образец-фона`.
- **Код выхода через конвейер врёт**: `| tail` показывает 0 на упавшем прогоне.
- **`\b` в regex не знает кириллицы**; `?` в regex — не вопросительный знак;
  номера `?v=` снимать node-скриптом по четырём html, не grep'ом.
- **`js/game.js` читает не только браузер** — его require'ят сервер и партия-по-сети.
- **«fatal: not a git repository» в начале прогонов с копией сервера** — сервер.js
  делает `git rev-parse HEAD` в temp-копии; это не провал.
- `tests/позвать-в-комнате.js` — браузерный, не запускать при занятом браузере;
  `tests/зов-знакомого.js` умеет `--сервер=ПУТЬ` для копии, порт первым доводом.
- Просьбу в очередь — СРАЗУ, до разведки. Время только из команды; сперва команда
  со временем, запись — следующим ходом.
- **Проверка, которая предсказывает, а не меряет, — украшение.** Спрашивать «на
  какой настоящей поломке она покраснела».
- **Жалоба на вид — не описание нужного.** «Сукно не то» оказалось «красное везде»,
  «верни чёрный фон» оказалось «сукно только на столе».
