# Прогресс штаба — письмо следующей сессии

Обновлено 19 сентября, 10:52 (по часам из команды). Обновлять при завершении сессии штаба и после каждого большого этапа.

## ПЕРВЫМ ДЕЛОМ
1. Открытых вопросов владельцу нет, кроме одного старого без ответа: «нарды-поворот-доски» в штаб/важное.json (задан 12 сентября, ответа всё ещё нет).
2. **«Рейтинг: вторая ось счёта по правилу дурака (подкидной/переводной)» — ЗАВЕРШЕНА и опубликована 19.09.** Все 4 этапа плана штаб/план-рейтинг-правило.md сданы и лично приняты штабом (диффы построчно, node --check, независимые тестовые прогоны с ломающим сценарием на каждом этапе): server/рейтинг.js (7f71e3e), server/комнаты.js (ae28e98), server/сервер.js (5716428), js/рейтинг-экран.js + index.html (39c2341) + забытый штабом при приёме номер версии поднят отдельным коммитом (ba7b1a1 — файл общий для index.html/нарды.html/шахматы.html/шашки.html, номер надо поднимать у всех четырёх разом). Постоянные тесты: tests/рейтинг-по-правилам-дурака.js (node, 23 проверки + флаг --сломать), tests/рейтинг-переключатель-правил.js (браузер, клик/класс/aria-checked/отсутствие нового fetch), tests/застолье-рейтинга-правило.js (комнаты.js, 15 проверок). Общий/сводный рейтинг не трогали, старые накопленные очки не мигрировали — решение владельца 19.09 09:28.
   Осталось открытым и НЕ начато (нужно слово владельца): алгоритм рейтинга для «2 на 2», фильтр «друзья» (списка друзей ещё нет). Запись в штаб/списки.json сужена до этих двух пунктов, важность «потом».
3. Владелец 16.09 просил не начинать новых игр: домино, морской бой, деберц в очереди помечены ОТЛОЖЕНО, пока не доведены четыре нынешние.
4. Ночью 18.09 закрыто кодом и решениями: свежесть сервера комнат (сверка git-коммита + подключение к чужой-бой-по-шагам.js), порог времени хода нард сравнивается с 99-м перцентилем вместо максимума, сторож дашборда (штаб/сторож-дашборда.js) поднимает дашборд при падении и сам себя перезапускает, узел ничьей в шашках сознательно оставлен отдельным от строки хода (архитектурное решение, не унификация), тестовый крючок в server/комнаты.js для форсирования кода комнаты + постоянный автотест защиты «родиласьТогда» (после ревью и точечного исправления утечки флага).

## Где мы
Четыре игры в одном стиле. Столы шахмат и шашек по наборам владельца закрыты целиком (Э1–Э3 каждый), у нард лобби на общем каркасе и третий уровень бота «мастер» — только в длинных (владелец 15.09 явно отложил усиление для коротких ради перехода к шашкам). Программа единого стиля прошла Э8 (широкий экран); остался Э7 — дурак на общий лист «⋯».

Сеть: «Позвать знакомого» работает от сервера до кнопки в комнате дурака, «Найти игру» — список открытых столов с посадкой одним нажатием, теперь с защитой от переиспользованных кодов (родиласьТогда) и постоянным автотестом на неё.

Сторож дашборда — новый штатный процесс: штаб/сторож-дашборда.js следит за живостью штаб/сборщика и поднимает дашборд сам при падении, PID лежит в штаб/сторож-дашборда.pid.

## Следующее по очереди
Безопасных для немедленного старта пунктов сейчас нет. Кандидаты требуют слова владельца:
1. Ответ на «нарды-поворот-доски» (штаб/важное.json) — задан 12.09, ждём.
2. Живая проверка ленты бота в настоящей партии через Telegram (нужен владелец с телефоном).
3. «Профиль» на разные экраны — ждёт настоящего экрана профиля (макета у владельца ещё нет).
4. «Рейтинг с фильтрами: друзья, личный/2 на 2» — подкидной/переводной уже сдан (см. п.2 ПЕРВЫМ ДЕЛОМ); остаток ждёт решения владельца про алгоритм 2 на 2 и про список друзей.
5. Мелкие «задел»-пункты в штаб/очередь.md — не трогать без прямого слова владельца по каждому.

## Ловушки, о которых забывают (все стреляли)
- Ревьюер на полной модели окупается на сервере: поймал утечку тестового флага в свободныйКод() (server/комнаты.js) — без ревью форсированный код мог тихо перезаписать живую комнату при утечке в несвязанный вызов.
- «Один файл — один исполнитель» держится только текстом: tester ночью 18.09 дважды дописывал в чужой server/комнаты.js вместо tests/ — штаб поймал через git diff, потребовал остановиться, файл цел, но правило не защищено кодом (это и есть та самая незаведённая находка про хуки-стражи).
- Каждая проверка обязана уметь краснеть по-настоящему: та же tester сначала сделала декоративный `--сломать` (просто печатал сообщение, ничего не портил) — штаб заметил, инструкция на честный путь через `undefined` (документированный пропуск сверки) дала настоящее покраснение.
- Правка штабной программы проверяется на КОПИЯХ файлов: тестовые прогоны раньше затирали заметку дашборда и строки ЗАДАЧИ.md — урок в штаб/уроки.md.
- Задача «на вид маленькая» может оказаться средней — разведка (Explore) перед стартом «Рейтинг с фильтрами» вскрыла архитектурный вопрос заранее, а не в середине работы разработчика.
- Время только из команды; боевой 8790 перезапускает штаб, посмотрев «/»; после перезагрузки компьютера адрес туннеля у бота в bot/.env вписывает владелец.
- Разработчик может сдать правку общего js-файла и забыть поднять «?v=» — сам код рабочий, node --check и тесты зелёные, а игрокам едет старая закэшированная версия. Штаб при приёме ОБЯЗАН отдельно проверить `grep -rn "файл.js?v=" *.html` до коммита, не полагаясь на дифф разработчика (19.09, js/рейтинг-экран.js — забытый номер поймал сам штаб перед коммитом, не разработчик).
