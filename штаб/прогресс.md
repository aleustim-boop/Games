# Прогресс штаба — письмо себе завтрашнему

Обновлено 23.09 00:24 (часы из хука). Владелец 22.09 23:23: «я пошёл спать, сделай по максимуму до 7 утра!» — работать нон-стоп, исполнителей не жалеть, вопросы в «Важное», не ждать ответов. Ночь идёт: 7–9 исполнителей разом.

## Главные установки владельца (22.09)
1. **Нон-стоп и без экономии** («добавляй исполнителей, нужно спалить все токены»): исполнители пачками, разводя по файлам. Один файл — один исполнитель.
2. **Главный акцент — рейтинг дурака как в эталоне** (макет `C:/Users/Admin/Downloads/leaderboard_cloudcode_pack_v1/leaderboard_cloudcode_pack_v1/`: prototype/app.js, styles.css; картинки references/screens/03_durak.png). 22:13: «рейтинг выглядит совершенно не так как в эталоне!!» → партия 6, план `штаб/план-рейтинг-партия-6.md`. Следом «Профиль по макету» — `штаб/план-профиль-по-макету.md`.
3. **Нарды онлайн** (22:22) — `штаб/план-нарды-по-сети.md`, Э1–Э8.
4. **«Друг пропал со связи» в дураке** (22:25, горит) — `штаб/план-друг-пропал-со-связи.md`, Э1–Э3 — **закрыто по коду 23.09 00:22**, боевой 8790 перезапущен на d5f49f6.
5. **Переезд на ВМ владельца** (18:47) за домен https://igra.medart.com.ua — опись `штаб/вм-опись.md` (не коммитить, как и штаб/вм.js). Этап 4 ждёт ответа `вм-данные-и-env`.

## Что опубликовано последним (git log)
- d5f49f6 23.09 00:22 — Дурак по сети Э3: за столом на двоих пропавшего ждут 5 минут (только этап «игра»), уборка хвостов; 8790 перезапущен.
- 11239e1 00:11 — Нарды Э4+Э5: доска в сетевом режиме (js/нарды-экран.js; `?v=` 12→13 поднимается коммитом Э8).
- 94e4c85 00:04 — Нарды Э7: мост js/нарды-сеть.js, МЕТКИ_ИГР nardy, кнопка «Сдаться»; сеть.js?v=43 на index/шашки/нарды, style-нарды.css?v=9.
- 1811652 — четыре проверки уводят server/данные в mkdtemp. Раньше: 34fdade (последнийХод.бросок), 4d16b13 (сторож полей нард), 9f0a625/912baf8 (пропал Э2/Э1).

## В работе прямо сейчас (незакоммиченное — живая работа, НЕ откатывать)
- **Рейтинг партия 6** (`js/рейтинг-экран.js` + `style.css`, обе руки сданы): node-проверки приняты штабом (рейтинг-партия-5.js 74/0, подиум-2-1-3.js 72/0, ломающие красные). Браузерный тестировщик sonnet переписывает tests/снимок-рейтинг-эталон.js (Шаг 2 + раздел 8) и золото.js, снимки 390×844 «Дурак»/«Все игры» → смотритель против 03_durak.png. Ревьюер opus читает разницу js (scratchpad/дифф-рейтинг-партия6.txt). После: `?v=` style.css 71→72 и рейтинг-экран.js 20→21 на 4 страницах (index:18/1247, шашки:216/912, шахматы:165/597, нарды:203/758), коммит (в него же попадает Э2 профиля из рейтинг-экран.js), проверка опубликованного адреса. Потом свободны style.css (Э4 профиля, пункт 36 «320×568», пункт 32 картинки img/рейтинг/ уже лежат) и рейтинг-экран.js (пункты 35, 38, 39, 40, 43, 27).
- **Профиль по макету**: Э2 (рейтинг-экран.js) и Э5 (нижние-вкладки.js, ревью идёт) сданы; Э3 (`js/профиль-экран.js`, 67/0) сдан, возвращён на две правки по ревью (назад через вернутьсяОткудаПришли; текст причины сервера в словаОБеде); Э1 первая половина (server/рейтинг.js моиОчкиПоИграм) принята ревью; Э1б (server/сервер.js таблицаРейтинга поИграм + tests/профиль-очки-по-играм-сервер.js) у разработчика; Э7а (game.js: дверь «лобби-вкладка-профиль» → ЭкранПрофиля.открыть('дурак'), возврат из «Как я играю» в профиль, ЭКРАНЫ_ПОВЕРХ + 'экран-профиля-игрока', tests/дверь-профиля-дурака.js) у разработчика. Дальше: Э4 (designer, style.css — после коммита партии 6), Э6 (браузер, tests/снимок-профиля.js, смотритель с 01_global.png), Э7 штаб: `<script src="js/профиль-экран.js?v=1">` на 4 страницах после рейтинг-экран.js и нижние-вкладки.js, `?v=` нижние-вкладки.js; заодно нарды.html:607/614 — нижние-вкладки.js подключён дважды?
- **Нарды по сети**: Э3 (server/игры/реестр.js `нарды: проверитьДоговор(...)`, server/комнаты.js вариантНард в применитьНастройки и раздать) у разработчика opus; проверка tests/нарды-по-сети-автопрогон.js (порт 8798, untracked, красная до Э3) — коммитить вместе с Э3. После Э3: ревьюер → коммит → перезапуск 8790 при 0 комнат → коммит Э8 (нарды.html карточка «Онлайн» + `#кнопка-с-другом` включены, js/нарды-лобби.js переходник, tests/лобби-нард.js 45/0; штабу перед коммитом прогнать `--сломать-онлайн` и `--сломать-мастер` — браузер!) с `?v=` нарды-экран.js 12→13 и нарды-лобби.js 3→4 → проверить опубликованный адрес → сыграть с телефона утром.
- **Опись пункта 1** («сукно фиолетовое без бархата, не доезжает между устройствами») — ревьюер sonnet пишет диагноз; правки после — developer/designer.
- `tests/кнопки-назад-везде.js` — незакоммиченные +45/−15, не проверены; в чужие коммиты не брать.

## Бот и службы дома
- Сервер комнат 8790 — перезапущен 00:22 на d5f49f6 (PID новый, `node штаб/перезапуск-сервера.js`; перед этим `curl -s http://127.0.0.1:8790/%D0%B7%D0%B4%D0%BE%D1%80%D0%BE%D0%B2%D1%8C%D0%B5` → комнат 0). Бот PID 38940 (включён штабом 23:24, вопрос `бот-на-windows-стоит` закрыт записью), туннель cloudflared → 8790. Дашборд 8791 PID 47880 + сторож 54916; страницы 8080 PID 4148.

## ВМ (np-ubuntu, хаб NodePulse)
- Сделано этапы 1–3: node 24, клон /srv/games, nginx :80 за доменом (`server/nginx/igra.conf`), службы games-server/games-bot/games-tunnel выключены. Домен: Caddy у Serhii → WireGuard → socat → 192.168.10.17:80. Сеть на ВМ НЕ трогать.
- Этап 4 — переключение — ТОЛЬКО после ответа владельца `вм-данные-и-env` (данные server/данные через хаб только с его слова, bot/.env — его руками, GAME_SERVER_ORIGIN=https://igra.medart.com.ua). Два бота на одном токене работать не должны.
- Хаб: `node штаб/вм.js устройства | выполнить <id> <скрипт> bash`. Ловушки: dash, ~5 мин, `.env` привратник не пропускает, MSYS переписывает пути. Секреты в скрипты хаба не подставлять.

## Открытые вопросы владельцу (штаб/важное.json, «нужен ответ»)
- `вм-данные-и-env`, `друзья-онлайн-кто-видит` (пункт 2 очереди, черновик план-друзья-онлайн-черновик.md), `профиль-золото-или-салат`, `серые-плитки-на-сукне`. Ответы — `штаб/ответы.json`/`письма.json` на старте хода.

## Ловушки (22–23.09)
- Исполнители упираются в 40 ходов — продолжать SendMessage сразу, коротко: «не перечитывай, три шага, отчёт». Перед SendMessage сдавшему отчёт — вернуть запись в штаб/права.json (хук снимает `завершил`/запись).
- Отчёт агента только в task-notification; .output — огромный JSONL, не читать.
- Тестировщик, час молчащий без правок файлов и без процессов node, — встал: TaskStop и прогнать самому.
- `node штаб/списки.js` удаляет из очереди записи, чьи приметы «сделано» целиком встретились в одной записи git log. Пункт 50 закрывается фразой «друг-пропал: Э3 закоммичен и 8790 перезапущен» в сообщении штабного коммита.
- Playwright `click` по disabled узлу висит до таймаута — в ломающих прогонах `{ force: true, timeout: 3000 }` в try/catch.
- Проверки с js/сеть.js через поддельный браузер — заканчивать process.exit. Heredoc с длинной кириллицей — через Write. Обратные кавычки в bash выполняются.
- Боевой 8790 перезапускает только штаб при 0 живых комнат.
- Ревьюер без Bash: `git diff` штаб кладёт в scratchpad файлом и даёт путь.

## Следующая сессия — с чего начать
1. `/штаб-старт`: хвосты ответов/писем, git log, `git status` (незакоммиченное — чьё, см. выше), процессы (8790, 8080, 8791, бот, туннель).
2. Собрать сданное: партия 6 (смотритель → ?v= → коммит), нарды Э3 (ревью → коммит + автопрогон → перезапуск 8790 → коммит Э8 с ?v=), профиль (Э1б, Э3-правки, Э7а → Э4 → Э6 → Э7 подключение → коммит с ?v= нижние-вкладки.js).
3. Коммит штабных файлов (планы, списки.json, важное.json, ответы.json, письма.json, права.json, ЗАДАЧИ.md, прогресс.md) — никогда вм.js/вм-опись.md, не брать tests/кнопки-назад-везде.js.
4. Браузерная очередь по одному: лобби-нард.js --сломать-онлайн/--сломать-мастер, кнопки-назад-везде.js, все-экраны-в-полном-экране.js (пункт 36), снимок-профиля.js, самая-длинная-подсказка.js.
