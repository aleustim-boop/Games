# Прогресс штаба — письмо следующей сессии

Обновлено 22 сентября, 20:22 (по часам из команды). Обновлять при завершении
сессии штаба и после каждого большого этапа.

## ПЕРВЫМ ДЕЛОМ
1. **Владелец велел работать нон-стоп и не экономить** (22.09: «добавляй исполнителей,
   у нас много токенов можно не экономить до среды 7 утра»; 18:48: «почему только два
   исполнителя работают??»). Держать 4–6 исполнителей разом, разводя по файлам.
   Вопросы — в `штаб/важное.json`, не в ленту.
2. **Главный акцент по слову владельца (22.09 ~18:30): «дурак и конкретно рейтинг,
   чтобы он был как в эталоне».** Эталон — папка
   `C:/Users/Admin/Downloads/leaderboard_cloudcode_pack_v1/leaderboard_cloudcode_pack_v1/`
   (prototype/app.js и styles.css — источник истины по структуре и размерам;
   references/screens/01_global.png и 03_durak.png — что видит владелец).
3. **Опубликовано последним:** `09e7cd3` — рейтинг по макету, партии 1–4 (подиум,
   «моё место», стрелка, шапка «‹ … i», кубок; ?v= 69/7/18 на всех четырёх страницах);
   `0c53524` — журнал пробы нард; `30324c0` — сервер: верхушка 25 мест.
   Боевой сервер комнат 8790 стоит на `30324c0` (перезапущен 19:50 при 0 комнат, PID 6004).
   Здоровье: `node -e "fetch('http://127.0.0.1:8790/').then(r=>r.text()).then(console.log)"`.
   **Опубликованный адрес в 20:20 ещё отдавал v=68 — сборка Pages шла; перепроверить.**
4. **Незакоммиченное в рабочей папке — живая работа, НЕ откатывать:**
   style.css и js/рейтинг-экран.js (два разработчика партии 5, с 20:18),
   js/нарды-бот.js (разработчик пробы, с 19:58), tests/снимок-рейтинг-эталон.js (новый),
   tests/снимок-рейтинг-золото.js, tests/кнопки-назад-везде.js,
   tests/все-экраны-в-полном-экране.js, tests/новые-кнопки-и-рейтинг.js (тестировщик, с 20:09).

## Где стоим по рейтингу (план штаб/план-рейтинг-по-макету-2.md)
- Партии 1–4 приняты и закоммичены (09e7cd3). Смотритель по снимкам 20:11: подпись выше
  вкладок, кубок, подиум 2-1-3, ровные строки — совпали с макетом; отличия: на снимке
  дурака не видно «‹» (на общем есть — тестировщик выясняет, не ставит ли тест класс
  `в-телеграме`), нет значка игры в кнопке выбора, «Моё место» на 8px слипается с шапкой
  столбцов на дураке при высоте 800/844.
- **Партия 5 в работе** (раздел 6 плана, два разработчика параллельно по договору имён
  классов): style.css — ровная таблица с разделителями, строка 53px, подиум 86px/1.1fr/зазор 5,
  кнопка выбора игры 56px со значком, аватар 35px с рамкой и инициалы, ПК 64/40, хвост про
  8px; js/рейтинг-экран.js — список с 4-го места порциями по 10 с «Показать ещё»/«Все игроки
  показаны»/«Показаны первые N», разделитель «⋯» на разрыве мест, разделитель тысяч
  (Intl ru-RU, неразрывный пробел!), кнопка «Повторить» в ошибках, карточка новичка,
  значок игры в кнопке, инициалы без фото. Ревьюер параллельно смотрит коммит 09e7cd3.
- **Приёмка партии 5 (порядок):** отчёты обоих → node-проверки (подиум-2-1-3,
  рейтинг-экран-четыре-игры, рейтинг-состав, проверка-нижних-вкладок; покраснели из-за
  110px/1.4fr/50px/«24860» — тестировщику обновить) → снимок эталона → смотритель →
  поднять ?v= (style.css 69→70, рейтинг-экран.js 18→19 на ВСЕХ четырёх страницах; скрипт
  scratchpad/поднять-версии.js — заменить числа) → коммит → опубликованный адрес → дашборд.
- Дальше: шторка выбора игры (пункты 10–15), мелочи 16–18; пункт 7 (полоса «Сила игры |
  Очки за победы») — по ответу владельца `рейтинг-ряды-фильтров`.
- Заделы в очереди: фото чужих игроков через бота; догрузка списка со страницы 2.

## ВМ владельца (запись очереди «Разместить проект на Ubuntu…»)
- Хаб NodePulse, ключ `NP_API_KEY` в bot/.env — НЕ СВЕТИТЬ. Всё через `node штаб/вм.js`
  (устройства | выполнить <id> <файл-скрипта> | сырой GET /runs/<id>) — файл и
  `штаб/вм-опись.md` в .gitignore, не коммитить. Опись — единственный источник фактов.
- Этапы 1–2 сданы: на ВМ node 24 (snap), Claude Code (без входа), cloudflared, проект в
  /srv/games на 57119b9 (отстаёт — перед этапом 4 сделать git pull на ВМ), три службы
  systemd заведены и ВЫКЛЮЧЕНЫ.
- `штаб/вм-перенос.js <папка> </путь/на/ВМ>` (закоммичен) переносит файлы кусками base64
  со сверкой sha256 — звать ТОЛЬКО из PowerShell (Git Bash переписывает /tmp/… в C:/…).
- **Этап 3 ждёт владельца:** переносить ли server/данные через хаб (данные живых людей
  лягут в историю запусков хаба), bot/.env и вход Claude — его руками; вопросы
  `вм-домен-или-туннель`, `вм-два-клауда` в «Важном».
- Этап 4 (переключение): погасить сервер и бота на Windows → games-server → games-tunnel
  (адрес из journalctl) → адрес в bot/.env руками владельца → games-bot → проверить
  опубликованный адрес. Два бота на одном токене работать не должны.

## Прочее в работе / очереди
- Нарды «мастер»: проба «вероятность быть побитым» (21 бросок в части 2б) отброшена
  (54,4/55,2/53,6 против 55,4%), журнал в шапке js/нарды-бот.js. Идёт проба 2: табличная
  вероятность попадания по расстоянию в БЫСТРОЙ оценке у листьев, с выключателем-константой;
  база и проба меряются подряд одним зерном. Замеры мастера не повторяются до числа
  (сторож по времени, загруженная машина) — порог 57% только на тихой машине.
- Браузерная очередь: сейчас у тестировщика (эталон/золото/назад); после — tests/самая-
  длинная-подсказка.js (только на тихой машине). Находка тестировщика в очереди: лобби на
  320×568 уходит под часы Telegram (−13/−29/−29px).
- Ждут слова владельца: `серые-плитки-на-сукне`, `вм-домен-или-туннель`, `вм-два-клауда`,
  `рейтинг-ряды-фильтров`. «Друзья онлайн» — черновик плана есть, НЕ начинать.
- Коннектор Notion в claude.ai не авторизован — сказать владельцу один раз.

## Как работает надзор (22.09)
- Права исполнителя — по его номеру в `штаб/права.json`; хук берёт список из строки
  «Файлы: a, b» задания. **Без строки — `файлы: []`, и привратник пропускает ВСЁ.**
- Отчёт агента приходит ТОЛЬКО в task-notification, `.output` пуст. Лимит 40 ходов —
  продолжать SendMessage по номеру сразу (< 5 минут), с коротким планом; в задание
  писать «фоновые прогоны ждать по уведомлению, не опрашивать в цикле».
- Агент Plan писать не умеет — план записывает штаб из его отчёта.
- Команды с `.env` привратник не пропускает — помощники читают ключ сами во время работы.
- Живость: `node штаб/кто-молчит.js`; процессы — Get-Process node (StartTime).
- важное.js отвергает заголовок длиннее 95 знаков. `grep -i` в Git Bash не знает
  регистра кириллицы — искать «Итого» с большой буквы или через tail.

## Ловушки (все стреляли)
- Обратные кавычки внутри `node -e` bash выполняет как команды (22.09 так перезапустился
  боевой сервер) — длинный текст только файлом через Write.
- Скрипты на ВМ идут под dash: только латинские имена переменных, POSIX; хаб рвёт
  запуск на ~5-й минуте, но запуск продолжается — досматривать «сырой GET /runs/<id>».
- ВМ за NAT, наружного адреса нет — дорога игрокам только через туннель.
- `чистаяСтрока` в js/рейтинг-экран.js вырезает незнакомые поля ответа — новое поле
  сервера надо прописать там, иначе клиент его «не видит».
- В Telegram «‹» рейтинга прячет правило `html.в-телеграме #кнопка-рейтинг-игроков-назад`
  (системная стрелка вместо неё) — на снимке без «‹» сперва проверить этот класс.
- Поддельные данные /рейтинг в снимочных проверках без `движение`/`сдвиг` → стрелок на
  снимке нет, смотритель зря тревожится.
- `\b` в regex не знает кириллицы; код выхода через `| tail` врёт; `?v=` менять
  node-скриптом по четырём html.
- «fatal: not a git repository» в начале прогонов с копией сервера — не провал.
- Разработчик начал пробу до замера базы — теперь в задание пишем «проба с выключателем,
  база и проба подряд одним зерном».
- Жалоба на вид — не описание нужного: показывать снимки, спрашивать в «Важном».
