# Прогресс штаба — письмо следующей сессии

Обновлено 21 сентября, 19:36 (по часам из команды). Обновлять при завершении
сессии штаба и после каждого большого этапа.

## ПЕРВЫМ ДЕЛОМ
0. **ВНИМАНИЕ: `js/дурак-путь.js` восстановлен вручную 21.09 в 20:04.** Исполнитель
   сделал `git checkout` на этом файле и стёр незакоммиченную работу четырёх
   заходов сразу: переименование кнопки на «Создать игру», аватар через
   `new Image()`, перестройку лобби на три входа, починку закрытого стола
   (`{ открытый: true }`). Штаб вернул всё из уцелевшей копии исполнителя —
   `git diff --stat` даёт 121 вставку, `node --check` чист. **Если увидишь в этом
   файле незнакомые 121 строку — это не чужая самодеятельность, это возврат
   потерянного.** Ревьюер сверял целостность отдельно. Запрет на откат файлов
   исполнителями вписан в CLAUDE.md, история — в `штаб/уроки.md`.
1. **За вечер 21.09 два коммита, оба доехали.** `5042ecc` — очки и сила не теряются
   при падении сервера; `a476734` — то же для награды дня. Боевой сервер перезапущен
   на `a476734` в тихую минуту (комнат 0), то есть обе починки работают у живых
   людей, а не лежат в репозитории. **Пачка лобби по-прежнему не закоммичена**,
   и она переплетена:
   `index.html`, `js/game.js`, `js/дурак-путь.js`, `js/сеть.js`, `js/telegram.js`,
   `js/открытые-столы.js`, `js/нижние-вкладки.js`, `js/похвастаться.js`,
   `js/рейтинг-экран.js`, три файла лобби (шашки/шахматы/нарды), `style.css`,
   четыре html-страницы и проверки в `tests/`. Разделить на мелкие коммиты нельзя:
   html-страницы несут поднятые `?v=` сразу нескольких файлов. **Коммитить только
   пачкой, когда лобби собрано целиком** — в полуразобранном виде публиковать
   нельзя, у игроков пропадут входы.
   **Серверные файлы идут отдельно от пачки и уже ушли** (`server/рейтинг.js`,
   `server/сила-игроков.js`, `server/раздача-дня.js` и проверки к ним): страниц и
   `?v=` они не касаются, поэтому коммитятся сами по себе, не дожидаясь лобби.
   Тем же путём пойдут `server/друзья.js` и `server/знакомые.js` — в работе.
2. **Владелец велел работать нон-стоп** (21.09, дословно): «пока ты не сделаешь все
   задачи, не останавливайся… я же не могу с тобой сидеть с утра до вечера». Вопросы
   не задавать в ленту, а записывать в `штаб/важное.json` и работать дальше.
3. **Экономия отменена**: исполнителей запускать пачками, разводя по файлам. Правило
   «один файл — один исполнитель» осталось — оно про столкновения, не про экономию.
4. Боевой сервер комнат 8790 жив и перезапущен в 20:06 на коммите `a476734`
   (старый процесс 43336 снят, комнат было 0). Дашборд 8791 жив. Спрашивать
   здоровье сервера только через node с `encodeURIComponent` — кириллица в URL
   через curl не доезжает, сервер честно отвечает «такой запрос не принимаю».

## Номера `?v=` к подъёму перед коммитом (перепроверено скриптом 19:36)
| файл | сейчас | у каких страниц |
|---|---|---|
| `style.css` | 58 → 59 | все четыре |
| `js/рейтинг-экран.js` | 12 → 13 | все четыре |
| `js/сеть.js` | 39 → 40 | дурак, шашки |
| `js/открытые-столы.js` | 1 → 2 | дурак, шашки |
| `js/похвастаться.js` | 2 → 3 | дурак, шашки |
| `js/game.js` | 81 → 82 | дурак |
| `js/дурак-путь.js` | 5 → 6 | дурак |

Уже подняты: `js/telegram.js` 24, `js/нижние-вкладки.js` 5, `js/шашки-лобби.js` 3,
`js/шахматы-лобби.js` 3, `js/нарды-лобби.js` 2. Ни один номер между страницами **не
разъехался** — проверено скриптом, он же лежит образцом в scratchpad сессии.
Таблицу снимать так: node-скриптом по четырём html, **не grep'ом** — кириллица в
классах символов grep рубится по байтам, а `?` в регулярном выражении означает
«необязательный символ», и запрос `\.js?v=` молча врёт.

## Что сделано вечером 21.09 (ещё не опубликовано)
- **Крестик переспрашивает** (просьба владельца): `спрашиватьПриЗакрытии()` /
  `неСпрашиватьПриЗакрытии()` в `js/telegram.js`. Проверка
  `tests/крестик-подтверждение.js` — 6 пунктов, умеет краснеть.
- **Аватар игрока во всех играх**: `освежитьАватар()` перенесена в шашки, шахматы,
  нарды. Ревьюер нашёл дыру — в шашках профиль обновлялся раньше, чем Telegram
  отдавал данные; починено тройной подстраховкой (`load`, вызов при показе лобби,
  MutationObserver за классом `экран--виден`).
- **Лобби дурака — три входа**: `лобби-онлайн` и `лобби-код` с главного экрана убраны,
  остались `лобби-играть-онлайн`, `лобби-боты`, `лобби-друзья`. «Пригласить друзей»
  больше не создаёт комнату — зовёт в приложение через чат бота.
- **Экран «Играть онлайн» собран** (`js/открытые-столы.js`) и **оформлен**
  (`style.css` ~13231-13432). Ревьюер прошёл обе ветки файла — шашечная половина не
  задета, все три двери защищены на случай отсутствия, повторное открытие не
  задваивает обработчики. Попутно починена кнопка `#открытые-столы-код`:
  растягивалась на 520 точек шире главной.
- **Возврат в незаконченную партию (сетевую)**: плашка показывается только после
  тихого подтверждения сервера, нажатие ведёт прямо за стол. Ревьюер нашёл, а
  разработчик починил гонку поколений: `наИтог(null)` на устаревшем ответе, иначе
  `проверкаЖивостиИдёт` оставалась `true` навсегда и плашка замолкала до перезагрузки.
- **Рейтинг — панель выбора игры вместо ленты кнопок** (`js/рейтинг-экран.js`):
  `СПИСОК_ИГР` заменён `РЕЕСТР_ИГР` + `играПоКлючу()`, лента кнопок → кнопка с
  названием текущей игры → шторка снизу по образцу `.лист-ещё` (`style.css`
  ~11538-11606 и ~11935-12107, `prefers-reduced-motion` ~1916-1920). Панель кладётся
  в корень `#приложение`, не внутрь экрана. Мёртвые обработчики
  `кнопка-рейтинг-дурак`/`кнопка-рейтинг-шашки` убраны. В файле по-прежнему **ни
  одного `innerHTML`** — только `createElement`/`textContent`.
- **Бежевое пятно убрано** (`style.css`): `.код-игры` и `.поле-кода` красились
  переменными игральной **карты** (`--карта-фон` #f6ead5). На столе это верно, на
  служебном экране — второй яркий цвет против правила «один салатовый акцент».
  Переключены на `var(--плитка)` / `var(--сб-текст)`, рамка и каретка салатовые.
  Контраст 16:1.
- **Обрезанный текст на экране «Игра с другом» оказался мелочью**: экран
  прокручивается (`overflow-y: auto` стоит явно на `#экран-друга`, `#экран-комнаты`,
  `#экран-входа`). Текст не сокращали — добавлено затухание нижних 24 точек
  (`mask-image`, `style.css` ~4467-4486) по образцу строк 7145, 9623, 9764.
- **Очки больше не теряются при падении сервера** (`server/рейтинг.js`): была
  отложенная запись через 5 секунд, теперь по концу партии, если начислено больше
  нуля, пишем немедленно (`записатьСрочно()`); отложенная осталась для мелочи вроде
  счётчиков. Проверка `tests/срочная-запись-рейтинга.js` роняет дочерний процесс
  по-настоящему и краснеет на возврате старого поведения.
- **Привратник команд** `.claude/hooks/разрешение-команд.js`.

## Находки, которые стоят дороже самих правок
- **Одна строка роняла весь сервер, и никто этого не видел.** `js/game.js:10562`
  объявляла `window.ДуракЛобби` без проверки на браузер. Этот файл require'ит
  `server/сервер.js` — в Node `window` нет, файл падал целиком. Два исполнителя
  подряд не смогли выполнить свои задания, и только по совпадению их жалоб стало
  понятно, что это одна причина. **Мораль: два исполнителя жалуются на разное — ищи
  одну причину.**
- **Жалоба на вид — это НЕ описание нужного.** Владелец сказал «сукно не то», штаб
  достроил «значит, нужно фиолетовое» и полдня чинил переезд оформления между
  устройствами. Владелец потом написал: «мне наоборот нужно было **красное** сукно
  везде». Спрашивать, чего человек хочет, дешевле, чем угадывать.
- **Проверка, не нашедшая узел, обязана КРАСНЕТЬ, а не пропускать шаг.** Мягкий
  пропуск (`if (await locator.count())`) превращает проверку в украшение. За один
  вечер два случая: `tests/аватар-везде.js` давала 35/35 и ДО починки шашек, и ПОСЛЕ;
  `tests/стрелка-после-рейтинга.js` даёт 53/53 и не проверяет переключение игр, ради
  которого писалась. Спрашивать не «сколько пунктов зелено», а **«на какой настоящей
  поломке она покраснела»**.
- **Починка наполовину — обычное дело, проверять надо grep'ом.** Разработчик закрыл
  потерю очков в `server/рейтинг.js` и оставил ту же дыру в `server/сила-игроков.js`,
  хотя «сила» на экране рейтинга показана ПЕРВОЙ. Возвращён доделывать.
- **Я преувеличил при докладе владельцу и пришлось поправляться.** Сказал, что очки
  можно задвоить. Ревьюер прошёл все пути и опроверг: флаг `комната.партияУчтена`
  ставится ДО начисления (`server/комнаты.js:2282-2294`), код синхронный, Node
  однопоточен, билет удаляется раньше проверок (`server/билеты.js:174`), комнаты на
  диск не пишутся вовсе. Поправка внесена в «Важное» и сказана владельцу прямо.
  **Мораль: «теоретически возможно» нельзя докладывать как «возможно».**
- **Окна «разрешить?» — это склейка, а не настройки.** Лечение: привратник плюс
  **запрет склейки прямо в текст каждого задания исполнителю**.
- **За тестировщиком нужно проверять уборку** (висела копия на 8792).
- **Агенты упираются в лимит 40 ходов.** Не перезапускать нового — продолжать
  SendMessage по task-id, контекст у агента сохраняется.

## Ловушки, о которых забывают (все стреляли)
- **Просьбу в очередь — СРАЗУ, до разведки.**
- **Код выхода через конвейер врёт**: `| tail` показывает 0 на упавшем прогоне.
  Смотреть `node тест.js --сломать > /dev/null 2>&1; echo $?`.
- **`\b` в regex не знает кириллицы**; `?` в regex — не вопросительный знак.
- **Класс `.экран` фона не красит** — сквозь служебный экран виден фон body (сукно).
- **Не больше одного браузера разом** — машина падала. Это весь вечер тормозило
  работу: снимки стояли в очереди за проверяющим.
- **`js/game.js` читает не только браузер** — его require'ят `server/сервер.js` и
  `server/партия-по-сети.js`.
- Штаб картинки не открывает — снимок оценивает смотритель.

## Ждут ответа владельца (все в `штаб/важное.json`)
1. `красное-сукно-везде-что-именно` — своя тема «Отсвет» или красное сукно во всех
   четырёх играх. **Без ответа оформление не трогать.**
2. `сколько-стол-ждёт-пропавшего` — полчаса / час / до возврата / оставить 3 минуты.
3. `рейтинг-золото-или-салатовый` — совет штаба: середина, золото только на короне,
   медалях и подиуме.
4. `рейтинг-общий-по-всем-играм` — совет штаба: общий зачёт по накопительным очкам,
   «сила» остаётся внутри игры.

## Долги и следующее по очереди
1. **Долг Э6:** `tests/найти-игру-в-браузере.js:209` ищет `#лобби-онлайн`, которой
   больше нет (теперь `#лобби-играть-онлайн`, `index.html:207`). **Пока не переписана
   — экран «Играть онлайн» протестированным не считать.** В работе.
2. **Переписать `tests/стрелка-после-рейтинга.js`** — зелёная и пустая, см. находки.
3. Собрать и закоммитить пачку (см. пункт 1 и таблицу `?v=`). Проверить
   ОПУБЛИКОВАННЫЙ адрес, не стенд.
4. **Заказать один заход съёмки** (браузер по очереди!) и отдать смотрителю: экран
   «Играть онлайн», комната и вход без бежевого, рейтинг с открытой панелью выбора
   игры, экран друга с затуханием. Все на 390×844.
5. Возврат в партию, этап Б: доигрывание с ботом. Разведка —
   `штаб/разведка-партия-с-ботом.md`. **Решение штаба принято: восстановленная партия
   рейтинга не приносит**, иначе появляется накрутка «сохранил — переиграл — выиграл».
6. **Рейтинг по макету владельца.** Разведка готова: `штаб/разведка-рейтинг-макет.md`,
   графика описана в `штаб/графика-рейтинга.md`, 27 png уже лежат в `img/рейтинг/`
   (692 КБ, ещё не в git). Главное столкновение — **не золото и не TypeScript, а
   модель очков**: пакет строит всё вокруг `globalPoints = Σ ratingPoints`, а у нас
   две меры — накопительные очки (`server/рейтинг.js:135-140`) и Elo-подобная «сила»
   (`server/сила-игроков.js`). Суммировать «силу» разных игр бессмысленно, места
   складывать нельзя. Делать после ответа на вопрос 4 выше.
