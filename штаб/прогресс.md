# Прогресс штаба — письмо себе завтрашнему

Обновлено 22.09 23:23 (часы из хука). Владелец 23:23: «я пошёл спать, сделай по максимуму до 7 утра!» — работать нон-стоп, исполнителей не жалеть, вопросы в «Важное», не ждать ответов.

## Главные установки владельца (22.09)
1. **Нон-стоп и без экономии** («добавляй исполнителей, нужно спалить все токены»): 6–8 исполнителей разом, разводя по файлам. Один файл — один исполнитель.
2. **Главный акцент — рейтинг дурака как в эталоне** (макет `C:/Users/Admin/Downloads/leaderboard_cloudcode_pack_v1/leaderboard_cloudcode_pack_v1/`: prototype/app.js, styles.css, index.html; картинки references/screens/01_global.png, 02_game_picker.png, 03_durak.png). 22:13: «рейтинг выглядит совершенно не так как в эталоне!!» → партия 6, план `штаб/план-рейтинг-партия-6.md`.
3. **Нарды онлайн** (22:22) — план `штаб/план-нарды-по-сети.md`, этапы Э1–Э8.
4. **«Друг пропал со связи» в дураке** (22:25, горит) — план `штаб/план-друг-пропал-со-связи.md`, Э1–Э3.
5. **Переезд на ВМ владельца** (18:47) за домен https://igra.medart.com.ua — опись `штаб/вм-опись.md` (не коммитить, как и штаб/вм.js), README напарника Serhii на ВМ `/srv/claude-serhii/README.md`.

## Что опубликовано последним (git log)
- 912baf8 23:23 — Дурак по сети Э1: мягкая надпись «Друг переподключается…» 20 секунд, опрос 30 с; сеть.js?v=42 на index.html и шашки.html.
- c7c9956 23:11 — Нарды Э6: сетевые экраны в нарды.html (друг, комната, вход, строка связи).
- 7949282 — Нарды Э1: картинкаПартии/партияИзКартинки в правилах.
- 5f5f1ea, 3e15a91, b3124dd, 0591538 — рейтинг: шторка выбора игры, svg, «Мастер» в нардах.

## В работе прямо сейчас (незакоммиченное — живая работа, НЕ откатывать)
- **Рейтинг партия 6**: руки A и C сданы; js-рука B (`js/рейтинг-экран.js`: 8.1 кнопка «Фильтры»+`#рейтинг-настройки-среза`, 8.2 шторка `#рейтинг-инфо`, 8.11) и css-рука D (`style.css`) в работе; тестировщик переписывает `tests/рейтинг-партия-5.js` и `tests/подиум-2-1-3.js` под новую разметку карточки. После: ревьюер на js, браузерный тестировщик (шаги 2 и 7 плана, по одному браузеру), смотритель против 03_durak.png, `?v=` у style.css и рейтинг-экран.js на всех 4 страницах, коммит.
- **Нарды по сети**: Э2 (`server/игры/нарды.js` + `tests/нарды-сервер-правила.js`, 119/0, ломающий 44/4) — на ревью; Э4+Э5 (`js/нарды-экран.js`) в работе с решениями 23:13 (последнийХод.бросок, видПобеды «сдача», пропуск = ход с пустыми шагами). Дальше Э3 (реестр.js + комнаты.js `вариантНард`, перезапуск 8790 штабом при 0 комнат), Э7 (`js/нарды-сеть.js`, `МЕТКИ_ИГР` в сеть.js, подключение в нарды.html + `?v=`), Э8. Очередь: `tests/сторож-полей.js` не знает нард.
- **Дурак «пропал» Э2** (`server/комнаты.js` + `tests/лента-бота-сервер.js`): убрать отсрочку пропажи (`отложитьОтметкуПропажи`, `ОТСРОЧКА_ПЕРЕД_ПРОПАЖЕЙ`, `таймерОтсрочкиПропажи`), сверка 8б. После — ревьюер обязателен, коммит, Э3 (ПРОПАЛ_НАСОВСЕМ 3→5 мин за столом на двоих), перезапуск 8790 при 0 комнат.
- **ВМ этап 3** (developer): nginx на :80 за доменом — `server/nginx/igra.conf` (страницы + передача API на 127.0.0.1:8790, закрыты .git/bot/server/штаб/tests/*.md/*.cmd), git pull, таймер games-pull, curl-проверки. Итог — в `штаб/вм-опись.md` и в очередь.
- `tests/кнопки-назад-везде.js` — незакоммиченные +45/−15, не проверены; в чужие коммиты не брать.

## Бот и службы дома
- 23:15 штаб увидел: бот `node bot/бот.js` не запущен (сервер 8790 PID 31912 с 22:55, туннель cloudflared PID 17760 → 8790 живёт с 15.09, значит адрес в bot/.env актуален). 23:24 штаб включил бота сам (Start-Process, журнал в scratchpad сессии `бот-журнал.txt`), потому что владелец спит и просил «по максимуму». Вопрос `бот-на-windows-стоит` в Важном закрыт записью об этом.
- Дашборд 8791 (PID 35064) + сторож (54916); страница 8080 (PID 4148).

## ВМ (np-ubuntu, хаб NodePulse, устройство ea35b08f-8826-47e4-b0e9-ee27e032eda3)
- Сделано: node 24 (/snap/bin/node), клон в /srv/games (отстал от main — этап 3 делает git pull), службы games-server/games-bot/games-tunnel выключены, порты только 22 и 53 (до nginx).
- Домен: https://igra.medart.com.ua → Caddy у Serhii (176.241.129.117) → WireGuard 10.77.0.1→10.77.0.2 → socat np-igra-fwd → 192.168.10.17:80 (обычный http). Настоящий IP игрока в X-Forwarded-For; TCP-источник всегда 192.168.10.30. Сеть на ВМ НЕ трогать (netplan, cloud-init выключен). games-tunnel больше не нужен.
- Этап 4 — переключение — ТОЛЬКО после ответа владельца `вм-данные-и-env` (данные server/данные через хаб только с его слова, bot/.env — его руками, GAME_SERVER_ORIGIN=https://igra.medart.com.ua). Два бота на одном токене работать не должны; до ответа games-server/games-bot на ВМ не включать.
- Хаб: `node штаб/вм.js устройства | выполнить <id> <скрипт> bash | сырой GET /runs/<id>`. Ловушки: скрипт идёт под dash (переменные латиницей), убивается через ~5 мин, вывод одним полем, `.env` в командах привратник не пропускает, MSYS переписывает `/пути` (запускать из PowerShell или MSYS_NO_PATHCONV=1). Секреты в скрипты хаба не подставлять — они остаются в истории запусков.

## Открытые вопросы владельцу (штаб/важное.json, «нужен ответ»)
- `вм-данные-и-env` — как переносить данные игроков и bot/.env на ВМ.
- `серые-плитки-на-сукне` — старый, про вид.
(Ответы читать через `штаб/ответы.json` и `штаб/письма.json` на старте хода.)

## Ловушки, найденные 22.09
- Исполнители упираются в 40 ходов — продолжать через SendMessage сразу (< 5 минут), запись прав в права.json ещё жива.
- Отчёт агента только в task-notification; .output пуст.
- `node штаб/списки.js` УДАЛЯЕТ из очереди записи, чьи приметы «сделано» совпали со старыми записями «Готово» (так пропала запись про бота, примета ['бот','запущен']). Приметы делать уникальными.
- Проверки, грузящие js/сеть.js через tests/поддельный-браузер.js, висят из-за setInterval — заканчивать process.exit. Файл сеть.js с CRLF — порча копии должна искать `\r\n`.
- Heredoc с длинной кириллицей в Bash ломается — длинные тексты через Write в scratchpad; короткий `cat >> файл <<'EOF'` работает. Обратные кавычки в bash выполняются.
- Боевой сервер 8790 перезапускает только штаб при 0 живых комнат; проверить `curl 127.0.0.1:8790/…` перед.
- Хуки: SubagentStop снимает запись прав; охрана прав не смотрит tool_name, правки через Bash обходят её; пустая строка «Файлы:» = без ограничений.
- Notion-коннектор не авторизован — сказать владельцу один раз (сделано в прогрессе).

## Следующая сессия — с чего начать
1. `/штаб-старт`: хвосты ответов/писем, git log, `git status` (незакоммиченное — чьё), процессы (8790, 8080, 8791, бот, туннель).
2. Собрать сданное: партия 6 рейтинга (ревью → браузер → смотритель → ?v= → коммит), нарды Э2/Э4/Э5 (коммит + Э3, Э7, Э8), пропал Э2 (ревью → коммит → Э3 → перезапуск 8790), ВМ этап 3 (записать, этап 4 ждёт ответа).
3. Коммит штабных файлов (планы, списки.json, важное.json, ответы.json, письма.json, права.json, ЗАДАЧИ.md, прогресс.md) — никогда вм.js/вм-опись.md.
4. Браузерная очередь по одному: tests/кнопки-назад-везде.js (обычный + --сломать), все-экраны-в-полном-экране.js, снимок-рейтинг-золото.js, самая-длинная-подсказка.js.
