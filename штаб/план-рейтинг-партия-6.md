# План «Рейтинг по макету, партия 6»: прокрутка страницы, карточка «Моё место», своя строка

Утверждён штабом 22.09.2026, 21:42 (по часам). Опись и шаги — описчик (Plan, opus) по находке
смотрителя: на «Дураке» 390×844 подиум внутри списка съедает 282px, после него видна одна
обрезанная строка, остальное — во внутренней прокрутке. В макете внутренней прокрутки нет:
прокручивается вся страница, низ закреплён (docs/02_RESPONSIVE_RU.md:27: «Нельзя закреплять весь
экран: список должен прокручиваться»). Номера строк — на 21:42, после шторки могут сдвинуться:
искать по селекторам и именам функций.

Начинать ТОЛЬКО после коммита шторки выбора игры (оба файла — js/рейтинг-экран.js и style.css —
сейчас несут её незакоммиченные правки).

## 1. Как сейчас устроена прокрутка экрана рейтинга

- Страница не прокручивается вовсе: `html, body { height:100%; overflow:hidden; overscroll-behavior:none }` — style.css:253–258; `#приложение { position:fixed; inset:0 }` — 324–326.
- Экран — коробка в размер окна: `.экран { position:absolute; inset:0; display:none; flex-direction:column }` — 620–629, видимый — `.экран--виден { display:flex }` (631).
- У `#экран-рейтинга-игроков` только `overflow-x: hidden` — 11439 (общий блок 11415–11445), вертикальной прокрутки у экрана НЕТ. Зазор `gap: 6px` — 6915–6919; поля — 11437–11438.
- Остаток высоты держит список: `#экран-рейтинга-игроков > #рейтинг-таблица { flex: 1 1 auto; min-height: 224px; gap:0 }` — 12264–12274 (обоснование пола — комментарий 12214–12263). Сама прокрутка списка — от общего класса `.таблица { flex:0 1 auto; min-height:0; overflow-y:auto; overscroll-behavior:contain; width:min(100%,420px) }` — 3578–3587. Тем же классом помечен `#рейтинг-объяснение` (index.html:741) — у него тоже свой внутренний скролл.
- Маски-затухания две, обе про внутреннюю прокрутку списка: низ — 7277–7280; верх — 12272–12273.
- Полоса вкладок: `.нижние-вкладки { position:fixed; bottom:0; z-index:5; height: calc(var(--вт-панель-низ) + var(--отступ-снизу)) }` — 1622–1644; `--вт-панель-низ: clamp(52px,14.36vw,64px)` — 11497. Резерв снизу — `#экран-рейтинга-игроков:has(> .нижние-вкладки) { padding-bottom: calc(var(--вт-панель-низ) + 12px + var(--отступ-снизу)) }` — 11514–11516 (те самые 68px).
- Своя строка липкая: `#рейтинг-таблица .рейтинг-строка.рейтинг-строка--я:not(:has(~ .рейтинг-строка)) { position:sticky; bottom:0; z-index:1 }` — 12951–12960; вид карточки (заливка, рамка 1.5px, радиус 14, строка сетки «метка») — 12894–12908; ярлык `--я::after { content:"Моё место" }` — 12916–12925. Строка, за которой идут другие, — подсветка + `outline` — 12932–12939.
- Карточка `#рейтинг-моё-место` (index.html:728) при мере «очки» прячется: `строкаСверху.className = 'скрыт'` — js/рейтинг-экран.js:1721–1727; её оформление (сукно, рама, кубок) — style.css:11666–11791, набор `.рейтинг__карточка-*` (11804–11890) сейчас мёртв — js таких узлов не строит.
- Прокрутку в js считают от списка: js:1654–1660 (сохранить/вернуть `таблица.scrollTop` у «Показать ещё»), 1794 (`таблица.scrollTop = 0`), 1805–1818 + вызов 1473 (`подкрутитьКСвоейСтроке`). Подиум лежит первым узлом ВНУТРИ списка — js:1745.

## 2. Как устроена прокрутка в макете

- Прокручивается страница целиком: `body` без `overflow`, `.app-shell { min-height:100dvh }` — prototype/styles.css:23; внутренних скроллов нет ни у `.table-panel` (63, на ПК 145 — рамка и `overflow:hidden` ради скруглений), ни у списка.
- Низ закреплён: `.my-dock { position:fixed; bottom:0; z-index:40; padding-bottom: safe }` — 77, внутри карточка `.my-card` (78) и навигация `.bottom-nav` (85). Резерв `.main { padding-bottom: calc(158px + safe) }` — 28 (планшет 170 — 134). На ПК от 1200 док `position:sticky; bottom:20px` — 171, `.main` резервирует 40 (181).
- Блок «Моё место»: `myCard()` — app.js:70–78; это КНОПКА `data-action="near-me"` (73), ярлык текстом `МОЁ МЕСТО` / `МОЁ МЕСТО СРЕДИ ДРУЗЕЙ` (74, регистр от `.my-eyebrow`, styles.css:79), содержимое — место, аватар, имя + пилюля «Вы», очки, стрелка (75–77). Новичку — «Сыграйте первую рейтинговую партию» (72).
- Нажатие (app.js:168–172): я в первой тройке — `nearMe=false` и подиум `scrollIntoView({block:'center'})`; иначе `nearMe=true` и список `scrollIntoView({block:'start'})`. В режиме соседей срез списка `start = max(3, меня−3)`, конец `меня+4` (88–89), внизу вместо «Показать ещё» — «В начало списка» (92): `nearMe=false` и `window.scrollTo({top:0})` (173).
- Своя строка в списке остаётся и просто подсвечена: `.rank-row.own-row { background:#e1c27c10; border-radius:8px; outline:1px solid #d0b27680 }` — styles.css:74. Липкости нет.

## 3. Как ведут себя соседние домашние экраны

- Лобби: прокручивается САМ ЭКРАН — `#экран-лобби { overflow-x:hidden; overflow-y:auto; overscroll-behavior:contain; scrollbar-width:none }` — style.css:10143–10186, полосу прокрутки прячет 10188–10189; дети `flex-shrink:0; width:100%; max-width:520px` — 10193–10199; резерв под полосу 10237–10240.
- Витрина так же: 838–895, 899 (`> * { flex-shrink:0 }`), 904–909 (колонка 520).
- Экраны друзей/комнат — тот же приём; подсказка «дальше есть ещё» — `#экран-друга::after { position: fixed; bottom:0; height:60px }` — 4567–4600.
- «Профиль» (`#экран-рейтинга`, index.html:641–659) живёт по старой модели (внутри скроллится `#таблица-рейтинга.таблица`). Образец для рейтинга — лобби/витрина/друзья; перевод «Профиля» — отдельная задача.

## 4. Шаги (каждый — один файл; порядок: 1 ∥ 3 → 2 → 4 → 5 → 6 → 7)

**Шаг 1. Экран прокручивается сам** — style.css.
В блок 11415–11445 (только `#экран-рейтинга-игроков`, не `#экран-рейтинга`) добавить `overflow-y:auto; overscroll-behavior:contain; scrollbar-width:none` и `#экран-рейтинга-игроков::-webkit-scrollbar { display:none }` — один в один с 10178–10189. В правило детей 11518–11529 добавить `flex-shrink: 0`. Правило 12264–12274: убрать `flex:1 1 auto` и `min-height:224px`, поставить `flex:0 0 auto; min-height:0; overflow:visible` (перебить `.таблица`:3581), перечислить в селекторе и `#рейтинг-объяснение`; убрать маску 12272–12273. Удалить правило 7277–7280 целиком. `padding-bottom` 11515 не трогать (вернуться, только если проверка шага 2 покажет, что последний узел уходит под полосу).
Проверки: tests/снимок-рейтинг-эталон.js (переписывается шагом 2), tests/снимок-рейтинг-золото.js:653–663 (комментарий про маску 7219 устареет), tests/затухание-края-при-прокрутке.js — чужой экран.
Краснеет на: подмене `overflow-y: auto` → `hidden` у экрана и на обнулении `padding-bottom` (11515).

**Шаг 2. Переписать проверку модели прокрутки** — tests/снимок-рейтинг-эталон.js.
Выбросить «экран помещается в окно» — 576–581 (390×844) и 766–773 (1280×800). Вместо: (1) у экрана живой `overflow-y === 'auto'`, у `#рейтинг-таблица` — `'visible'`; (2) внутри списка прокрутки нет: `scrollHeight <= clientHeight + 1`; (3) низ достижим: прокрутить экран до дна, низ последнего видимого узла (`#кнопка-рейтинг-игроков-назад`, в Telegram он спрятан — тогда `#кнопка-рейтинг-как-считается`) не ниже верха `.нижние-вкладки`; (4) docs/02:26 — при прокрутке до дна низ последней `.рейтинг-строка` не закрыт карточкой `#рейтинг-моё-место`; (5) при `scrollTop = 0` верх шапки не отрицательный. Ломающий прогон 113–138: поломку «в)» (`min-height:900px` списку) заменить на две — снять `overflow-y` у экрана и обнулить его `padding-bottom`. Замеры высот 440–474 и «Моё место не под полосой» 584–590 остаются.
Краснеет на: обеих новых поломках стиля.

**Шаг 3. Прокрутка в js считается от экрана** — js/рейтинг-экран.js.
`показатьЕщёСтроки` 1654–1660 — запоминать и возвращать `scrollTop` узла `ЭКРАН` (константа 36), не таблицы. Строку 1794 (`таблица.scrollTop = 0`) убрать из отрисовки; прокрутку экрана сбрасывать только при смене игры/меры/круга (где сбрасывается `показаноСтрок`, 86), иначе «Показать ещё» будет швырять к шапке. `подкрутитьКСвоейСтроке` 1805–1818 и вызов 1473 — считать от экрана либо `своя.scrollIntoView({ block:'center' })`.
Проверки: tests/рейтинг-партия-5.js (vm без геометрии — остаётся зелёной), новая проверка шага 2, п. 3.
Краснеет на: возврате `таблица.scrollTop` — браузерная проверка «после „Показать ещё“ позиция экрана сохранилась» падает.

**Шаг 4. Карточка «Моё место» есть всегда, подпись — узлом** — js/рейтинг-экран.js.
В `нарисоватьОчки` 1719–1734 ветку `вид.я` перевернуть: класс `скрыт` не ставить, а заполнять `#рейтинг-моё-место` (`УЗЕЛ_МЕСТО`, 37) помощниками: `кнопкаЭл` (1924) с id `кнопка-рейтинг-моё-место`, внутри `<span class="рейтинг__карточка-подпись">` «Моё место» / «Моё место среди друзей» (по `кругСейчас`), затем место, инициалы (`узелИнициалов` 1174), имя с пилюлей «Вы» (`ячейкаИмени` 1139), очки и стрелка (`узелДвижения` 1191), фото — `поставитьФото` (1257). Дубль карточки внутри списка убрать: 1767–1768 и `мояСтрокаДляКарточки` 1677–1682 (своя строка остаётся в списке, если попала в порцию). Новичок (`строкаНовичка` 523–535) — в ту же карточку. Ветка «беды» (`вид.я === null`) — без изменений.
Проверки: tests/подиум-2-1-3.js:278–280 (ждёт класс «скрыт») — на «узел не скрыт и внутри кнопка»; tests/рейтинг-партия-5.js:66,69,87,100 (11/21 строки) — 10/20, карточка вне списка; tests/браузер-рейтинг-круг-друзей.js:540–544 — фраза при пустых друзьях остаётся.
Краснеет на: возврате `className='скрыт'` и подмене подписи («Моё место среди друзей» при круге «Друзья»).

**Шаг 5. Вид карточки и своей строки** — style.css.
Карточке `#экран-рейтинга-игроков > #рейтинг-моё-место` (11768–11791) — `position:sticky; bottom: calc(var(--вт-панель-низ) + var(--отступ-снизу) + 6px); z-index:4` (ниже полосы, у неё 5), непрозрачный фон `var(--рейтинг-золото-поверхность)`, рамка 1px `var(--рейтинг-золото-край)`, радиус 15, поля 8/12/10 — числа `.my-card` (prototype/styles.css:78); порядок — `order:1` карточке, 2 — `#рейтинг-итог`, 3 — «Как считается» и `#рейтинг-объяснение`, 4 — `#кнопка-рейтинг-игроков-назад`. Убрать кубок `::before` (11714–11724) и сукно/раму (11677–11711 оставить только `#рейтинг-главное`). Внутренности по `.my-content` (prototype:80): сетка `auto minmax(0,1fr) auto auto`, зазор 12; переписать `.рейтинг__карточка-*` (11804–11890), ярлык — 10px, `letter-spacing .5px`, `text-transform:uppercase` (prototype:79). Своя строка: в 12894–12908 оставить только подсветку макета, убрать строку сетки «метка», `padding-top:8px` и `::after` (12916–12925); 12932–12939 сделать безусловным (снять `:has(~ .рейтинг-строка)`); 12951–12960 (`position:sticky`) удалить. Кольцо аватара 12982–13000 не трогать.
Проверки: tests/снимок-рейтинг-золото.js, tests/снимок-рейтинг-эталон.js:584–590 плюс новый пункт «своя строка не sticky и имеет `outline`».
Краснеет на: возврате `position:sticky` своей строке и на снятии `outline`.

**Шаг 6. Карточка-кнопка: соседи ↔ «В начало списка»** — js/рейтинг-экран.js.
Флаг `соседи` рядом с `показаноСтрок` (86), сброс там же. Обработчик кнопки карточки: я на подиуме (как 1678) — `соседи=false`, перерисовать, подиум `scrollIntoView({block:'center'})`; иначе `соседи=true`, перерисовать, `#рейтинг-таблица.scrollIntoView({block:'start'})` (app.js:168–172). В `нарисоватьОчки` 1759 при `соседи` срез `лидерыПодПодиумом` от `мойИндекс−3` до `мойИндекс+4` (app.js:88–89). В `концоСписка` 1635–1647 при `соседи` — `#кнопка-рейтинг-в-начало` «В начало списка» → `соседи=false`, перерисовать, `scrollTop` экрана в ноль (app.js:92,173).
Проверки: новая node-проверка как tests/рейтинг-партия-5.js (vm + поддельный DOM): нажали карточку — своя строка и трое соседей сверху, есть «В начало списка», нет «Показать ещё»; нажали её — первая порция с 4-го места.
Краснеет на: подмене среза на обычную порцию и удалении «В начало списка».

**Шаг 7. Довести ожидания старых проверок** — tests/рейтинг-партия-5.js (и tests/подиум-2-1-3.js).
Числа строк 66, 69, 87, 100 — под карточку вне списка; пункт «в таблице ровно одна `.рейтинг-строка--я` и ноль, если моё место не в порции».
Краснеет на: возврате карточки внутрь таблицы.

## 5. Риски

- **«Все игры»**: нет строки меры (js:2703–2704) и фильтров (2695–2696), содержимое короче окна — липкая карточка встанет в своём месте потока, между ней и «Как считается» может быть пустота; проверять «Все игры» и «Дурак» на 390×844 и 320. Следить, чтобы при коротком содержимом не появилась прокрутка в 1–2 точки из-за `gap: 6px` и резерва 68px.
- **ПК 1280×800**: в макете от 768 панель с рамкой, док от 1200 sticky — у нас панель и две колонки запрещены (все игры одинаково), колонка 440 (13616–13618): берём только липкость; при 800 и полосе 64 карточка — не больше четверти окна, «Показать ещё» достижима.
- **Telegram**: `html.в-телеграме` прячет «Назад» (9734–9738) — проверку «низ достижим» писать по фактически видимому последнему узлу. `overscroll-behavior: contain` на экране обязателен (свайп вниз закрывает мини-апп). Смещение карточки и резерв — только через `--вт-панель-низ` (11497) и `--отступ-снизу` (235), литерал 56 не вписывать.
- **Мера «сила»**: `нарисоватьСилу` (1425) с `подкрутитьКСвоейСтроке` (1473) — после снятия внутренней прокрутки станет пустышкой; проверять обе меры.
- **Молчащие мелочи**: не убранная маска 7277 — блёклая последняя строка; не перебитый `overflow-y` у `#рейтинг-объяснение` — второй скролл; подиум остаётся внутри списка (js:1745, tests/подиум-2-1-3.js:284) — безвреден, перенос в отдельный блок «Лидеры» — следующая партия.

## Приёмка
node (партия-5, подиум-2-1-3, четыре-игры, нижние-вкладки) → ревьюер на js (шаги 3, 4, 6) →
браузер: снимок-рейтинг-эталон (переписанная), снимок-рейтинг-золото, новая node-проверка соседей →
смотритель рядом с references/screens/03_durak.png (390×844, Дурак и Все игры) → ?v= на четырёх
html → коммит → опубликованный адрес.


## 8. Остальные расхождения по смотрителям 22.09 22:14

Раздел написан исполнителем (Plan) 22.09 22:28, штаб принял 22:35 с решением 8.10 внизу.
Номера строк — на 22:40 (после шторки выбора игры); искать по именам функций и селекторам.
Раздел не спорит с шагами 1–7, кроме трёх мест — они выписаны в конце отдельным списком.
Имена узлов ниже предложены здесь впервые: если штаб их поменяет, менять и в проверках.

### 8.1 Три ряда фильтров сворачиваются под кнопку «Фильтры»

- **Файлы:** `js/рейтинг-экран.js` (структура), `style.css` (вид). `index.html` не трогаем: узлы
  уже есть, js их **переносит**, как `вынестиКругИгроков` (2157–2175) переносит сегмент круга.
- **Где:** `#рейтинг-выбор-меры` строится в 2201–2206; `#рейтинг-фильтры` — `блокФильтровДурака`
  2089–2095, вызов 2208; показ/скрытие — `отметитьИгру` 2703–2713; подпись под селектором —
  `убедитьсяЧтоЕстьПодписьВыбораИгры` 2303–2310. Вид рядов — style.css 11942–11967 и 12160–12179.
- **Что сделать (js):**
  1. Новая `убедитьсяЧтоЕстьСтрокаПодписи()`: `рамка('рейтинг-подпись-строка')` ставится на место
     `#рейтинг-выбор-игры-подпись`, подпись переезжает внутрь первой, кнопка — второй.
  2. Кнопка — `кнопкаЭл('кнопка-рейтинг-фильтры', 'Фильтры', 'кнопка кнопка--мелкая
     рейтинг-настройки__кнопка', переключитьНастройкиСреза)`.
  3. Новая `убедитьсяЧтоЕстьНастройкиСреза()`: `контейнер('рейтинг-настройки-среза',
     'рейтинг-настройки-среза скрыт')` ставится сразу под строкой подписи, внутрь `appendChild`
     переезжают существующие `#рейтинг-выбор-меры` и `#рейтинг-фильтры`.
  4. `переключитьНастройкиСреза()` — снимает/ставит `скрыт` на обёртке и `aria-expanded` на кнопке.
  5. Чистая `счётчикСреза(мера, правило, состав)` — сколько значений не по умолчанию (умолчания:
     `мераСейчас` 181 — после 8.10 это `'очки'`, `правилоСейчас='подкидной'` 189,
     `составСейчас='личный'` 197). `освежитьКнопкуФильтров()` пишет
     `textContent = счёт ? 'Фильтры · ' + счёт : 'Фильтры'` и вешает класс
     `рейтинг-настройки__кнопка--задано`. Счётчик честнее и короче, чем перечисление:
     «Очки за победы · Переводной · 2 на 2» в 390 точек не влезает.
  6. В `отметитьИгру` (2703–2713) прежние две строки оставить как есть, добавить: обёртку и кнопку
     прятать при `игра === 'все'` (внутри пусто), при смене игры обёртку сворачивать обратно.
  7. Вызовы `убедитьсяЧтоЕсть…` — рядом с `вынестиКругИгроков` в `открыть` (2889) и `подключить` (3310).
- **Что сделать (css):** `.рейтинг-подпись-строка { display:flex; align-items:center;
  justify-content:space-between; gap:8px }`; `.рейтинг-настройки-среза { display:grid; gap:5px }`;
  кнопка — тихая, как `.рейтинг-круг__друзья` (12136–12150), с золотом при `--задано`.
- **Проверка:** node-проверка на `счётчикСреза` в `tests/рейтинг-партия-5.js` (чистая функция, через
  `module.exports` 3329–3339) + браузер: до нажатия `#рейтинг-выбор-меры` и `#рейтинг-фильтры` не
  видны (`offsetParent === null`), после нажатия видны; у «Все игры» кнопки нет вовсе.
  **Ломающий запуск:** в копии js снять `скрыт` с обёртки при построении — проверка «по умолчанию
  свёрнуто» обязана покраснеть; вторая поломка — вернуть `счётчикСреза` всегда 0.
- **Кто:** developer js → developer css → tester.

### 8.2 «i» открывает объяснение шторкой; сводки и «Как считается» на экране нет

- **Файлы:** `js/рейтинг-экран.js`, `style.css`.
- **Где:** `#рейтинг-итог` — создание 2211, заполнение `показатьИтог` 1412–1416 (вызов 1785);
  кнопка `#кнопка-рейтинг-как-считается` — 2212, обработчик 3270, `спрятатьОбъяснение` 3242–3247,
  `переключитьОбъяснение` 3249–3256; `#рейтинг-объяснение` — 2214, `заполнитьОбъяснение` 1867–1873;
  «i» — 2375–2377. Образец шторки: `убедитьсяЧтоЕстьПанельВыбораИгры` 2388–2428,
  `открытьПанельВыбораИгры` 2595–2607, `закрытьПанельВыбораИгры` 2610–2618; её вид — style.css
  13258–13297 и далее.
- **Что сделать:**
  1. Из общих кусков панели вынести два помощника: `показатьШторку(панель)` и
     `спрятатьШторку(панель, кнопкаВозврата)` (снять/поставить `скрыт`, вернуть фокус). Обе шторки
     зовут их — раздвоения приёма нет. Слушатель `Escape` (2425–2427) переписать один на обе:
     закрывать ту, что открыта.
  2. `убедитьсяЧтоЕстьШторкаИнфо()` по образцу 2388–2428: `#рейтинг-инфо` (класс `рейтинг-инфо
     скрыт`), внутри `.рейтинг-инфо__фон` (клик = закрыть), `.рейтинг-инфо__окно`,
     `.рейтинг-инфо__шапка` с `h2.рейтинг-инфо__заголовок` «Как считается» и
     `#кнопка-рейтинг-инфо-закрыть` (`.рейтинг-инфо__закрыть`).
  3. В это окно **переносим существующие узлы** `#рейтинг-объяснение` и `#рейтинг-итог` (сводка
     сама продолжит обновляться: `показатьИтог` пишет в тот же узел, менять её не нужно).
  4. Кнопку `#кнопка-рейтинг-как-считается` в построенной разметке не создавать (убрать 2212), а
     найденную в `index.html` — удалить из страницы в той же `убедитьсяЧтоЕстьШторкаИнфо()`.
     `спрятатьОбъяснение`/`переключитьОбъяснение` переписать на шторку, строки про кнопку убрать.
  5. «i» (2375) вешаем на `открытьШторкуИнфо` — та зовёт `заполнитьОбъяснение()` и `показатьШторку`.
- **Проверка:** браузер — на экране нет ни `#кнопка-рейтинг-как-считается`, ни видимого
  `#рейтинг-итог`; клик по `#кнопка-рейтинг-инфо` открывает `#рейтинг-инфо` без класса `скрыт`,
  внутри есть и правила, и строка «Всего в таблице…»; Esc и клик по фону закрывают, фокус
  возвращается на «i». **Ломающий запуск:** в копии js не удалять старую кнопку — проверка
  «кнопки нет» краснеет; вторая — не звать `заполнитьОбъяснение`, шторка открывается пустой.
- **Кто:** developer js → developer css (`.рейтинг-инфо*` числами `.рейтинг-панель-игр*`) → tester.

### 8.3 Заголовок столбца очков: «Очки» у всех игр, «Рейтинг» у одной

- **Файл:** `js/рейтинг-экран.js`.
- **Где:** `шапкаТаблицы` 1217–1229, строка 1222 (`столбцы === 'сила' ? 'Сила' : 'Очки'`); вызовы —
  1445 (мера «сила») и 1754 (мера «очки»).
- **Что сделать:** чистая `подписьСтолбцаОчков(столбцы, игра)`: `'сила'` → «Сила»; иначе
  `игра === 'все'` → «Очки», иначе «Рейтинг». `шапкаТаблицы(столбцы, игра)` принимает игру вторым
  доводом; вызовы — `шапкаТаблицы(вид.столбцы, играСейчас)` и `шапкаТаблицы('очки', играСейчас)`.
  Функцию добавить в `module.exports` (3329–3339).
- **Проверка:** node в `tests/рейтинг-партия-5.js` — три случая по чистой функции; браузер: у
  «Дурака» в шапке списка слово «РЕЙТИНГ», у «Все игры» — «ОЧКИ». **Ломающий запуск:** в копии
  вернуть «Очки» всегда — оба пункта краснеют.
- **Кто:** developer js → tester.

### 8.4 Заголовок шапки остаётся по центру, когда «‹» спрятана

- **Файл:** `style.css`.
- **Где:** прячет кнопку `html.в-телеграме #кнопка-рейтинг-игроков-назад { display:none }` —
  9734–9738 (правило общее на все экраны). Сетка шапки — 11571–11577 (`44px 1fr 44px`), название —
  11579–11592 (`grid-column:2`), «‹» в шапке — 11603–11608.
- **Что сделать:** сетка нужного вида уже стоит, поэтому **первым делом замер**, а не правка:
  в браузере при `html.в-телеграме` сравнить центр `.рейтинг-шапка__название` с центром
  `.рейтинг-шапка` (разница ≤ 1 точки). Если разъехалось — добавить узкое правило
  `html.в-телеграме #экран-рейтинга-игроков > .рейтинг-шапка > #кнопка-рейтинг-игроков-назад
  { display:grid; visibility:hidden }`: место в колонке остаётся, кнопка не нажимается. Общее
  правило 9736 для остальных экранов не трогать.
- **Проверка:** пункт в `tests/снимок-рейтинг-эталон.js` рядом с проверкой шапки (625): центр
  названия = центру шапки ±1, в двух прогонах — «как в Telegram» и «без Telegram» (там есть готовый
  `безTelegram`, 831). **Ломающий запуск:** в копии стиля заменить `44px 1fr 44px` на `auto 1fr auto`.
- **Кто:** developer css → tester.

### 8.5 «Все игроки» перестаёт переноситься на две строки

- **Файл:** `style.css`.
- **Где:** 12094–12102 — двойной селектор `#рейтинг-круг, #рейтинг-круг .переключатель.рейтинг__фильтр`
  даёт полосе **две** рамки и **два** флекса: внешний растянут на ширину, внутренний сжат по
  содержимому, поэтому кнопкам тесно и слово ломается. Кнопки — 12106–12116.
- **Что сделать:** оформление полосы оставить **одному** узлу. Внешнему `#рейтинг-круг` — рамку,
  фон, `padding:3px`, `display:flex`; внутреннему `#рейтинг-круг > .переключатель.рейтинг__фильтр` —
  `flex:1 1 auto; min-width:0; border:none; padding:0; background:none; border-radius:0`. Кнопкам —
  `white-space:nowrap`. Размер шрифта 14px из макета не трогать.
- **Проверка:** в `tests/снимок-рейтинг-эталон.js` рядом с проверкой сегмента (624): у обеих кнопок
  круга высота рамки ≈ одной строке (`clientHeight ≤ 48`) и ширина полосы = ширине колонки экрана
  ±2 точки, на 390 и на 320. **Ломающий запуск:** в копии вернуть `flex:0 0 auto` внутреннему узлу.
- **Кто:** developer css → tester.

### 8.6 Подпись под селектором — по левому краю

- **Файл:** `style.css`.
- **Где:** центрует общее `.экран > .подпись { text-align:center }` — 686–688; размер 15px — `.подпись`
  706–711. Макет: `.context-note { color:var(--muted); font-size:12px; margin:7px 0 12px }`.
- **Что сделать:** правило `#экран-рейтинга-игроков #рейтинг-выбор-игры-подпись { text-align:left;
  font-size:12px; margin:0; color:var(--сб-текст-тихий) }` — два id перебивают общее правило и
  работают и до, и после переноса подписи внутрь строки из 8.1 (порядок шагов не важен).
- **Проверка:** в снимочной проверке — `getComputedStyle(...).textAlign === 'left'` и левый край
  подписи совпадает с левым краем `#рейтинг-выбор-игры` ±1. **Ломающий запуск:** в копии вернуть
  `text-align:center`.
- **Кто:** developer css → tester.

### 8.7 Селектор: белое название и живой значок «Все игры»

- **Файлы:** `style.css`, `img/рейтинг/все-игры.svg`.
- **Где:** цвет текста кнопки — style.css 12041 (`color: var(--рейтинг-золото)` внутри правила
  12020–12042); значок — 12065–12083; строит его `значокВыбраннойИгры` (js 2265–2279).
- **Что сделать (css):** в 12041 поставить `var(--сб-текст)` — в макете `.selector .selector-name`
  белый, золотые только значок сетки и шеврон; шеврон `::before` (12048–12058) и значок не трогать.
- **Что сделать (svg):** рисунок **совпадает** с `icon('grid')` макета один в один (четыре
  `rect 6×6 rx 1.5`), беда не в рисунке: в комментарии файла, строка 3, стоит имя переменной с
  двойным дефисом в начале (`рейтинг-золото-общее` с двумя дефисами перед словом). Двойной дефис
  внутри XML-комментария запрещён, разбор падает, и браузер рисует значок «битой картинки» — тот
  самый «как картина» на снимке `рейтинг-эталон-общий-390.png`. Убрать двойной дефис из комментария
  (написать «переменной рейтинг-золото-общее») либо снять комментарий целиком. Других svg с такой
  бедой в проекте нет.
- **Проверка:** node-проверка (одна, дешёвая): прочитать все `img/**/*.svg` и потребовать, чтобы
  внутри комментариев `<!-- … -->` не было двойного дефиса; плюс в браузерной — `naturalWidth > 0` у
  `.рейтинг-выбор-игры__значок--все` (сейчас он 0) и `color` кнопки = цвету `--сб-текст`.
  **Ломающий запуск:** копия svg с возвращённым двойным дефисом; для цвета — копия css с золотом.
- **Кто:** developer css + developer (файл картинки) → tester.

### 8.8 Тумбы подиума без золотой обводки

- **Файл:** `style.css`.
- **Где:** `#рейтинг-таблица .рейтинг-подиум-основание` — 12829–12845 (`border: 1px solid
  var(--рейтинг-золото-край)`), первое место — 12846–12848 (`border-color: var(--рейтинг-золото)`),
  своя карточка — 12855–12860 (`box-shadow: inset 0 0 0 1px …`).
- **Что сделать:** снять `border` и `border-bottom:none` из 12829–12845, удалить правило 12846–12848
  и `box-shadow` своей тумбы. Градиент, радиус `30px 30px 0 0` и поля `19px 4px 10px` оставить —
  они уже макетные. Своё место на подиуме остаётся заметным золотым именем (12855–12857).
- **Проверка:** в снимочной проверке — `borderTopWidth === '0px'` у всех трёх тумб (в том числе у
  `[data-место="1"]`). **Ломающий запуск:** копия css с возвращённой рамкой.
- **Кто:** developer css → tester (и смотритель рядом с `references/screens/03_durak.png`).

### 8.9 Не в этом плане

Фото других игроков (в макете у всех аватарки) сервер не отдаёт — Telegram даёт фотографию только
своего игрока. Опись и план — `штаб/план-фото-игроков.md` (важность «потом»), в разделе 8 работы нет.

### 8.10 Решение штаба: мера по умолчанию — «очки» (22.09 22:35)

Сейчас мера по умолчанию — `'сила'` (js:181), и после 8.1 переключатель мер уезжает под «Фильтры».
Человек, открывший «Дурака», видел бы «Сила игры» без подиума, а макет показывает «Рейтинг» с
подиумом. Умолчание меняется на `'очки'`; «сила» остаётся доступной через «Фильтры». Проверка —
node в `tests/рейтинг-партия-5.js`: при первом открытии `мераСейчас === 'очки'`; ломающий — копия с
`'сила'`. Владелец может отменить одним словом (вопрос в важное.json).

---

### Как раздел 8 встраивается в шаги 1–7

- **Параллельно двумя руками:** js (шаги 3, 4, 6 → потом 8.1-js, 8.2-js, 8.3, 8.10) и css (шаги 1, 5 →
  потом 8.4, 8.5, 8.6, 8.7-css, 8.8, вид `.рейтинг-инфо*` и строки подписи). Один файл — один
  исполнитель: внутри каждой руки строго по очереди.
- **Только после js:** вид `.рейтинг-настройки-среза`, `.рейтинг-подпись-строка`, `.рейтинг-инфо*` —
  этих узлов до 8.1/8.2 в странице нет.
- **8.7-svg и 8.4** ни от чего не зависят — можно делать первыми, они дешёвые и заметные.
- **Проверки (шаги 2, 7)** — последними, уже поверх 8.1–8.3: иначе их придётся переписывать дважды.

### Что поправить в самих шагах 1–7 (иначе спорят)

1. **Шаг 2, пункт 3** берёт запасным «последним видимым узлом» `#кнопка-рейтинг-как-считается` —
   после 8.2 такой кнопки нет. Брать `#кнопка-рейтинг-показать-ещё` (js 1636) или последнюю
   `.рейтинг-строка`.
2. **Шаг 1** велит перечислить `#рейтинг-объяснение` в правиле 12264–12274 (`overflow:visible`) —
   после 8.2 узел уезжает в шторку, правило станет мёртвым, а внутри шторки внутренняя прокрутка
   как раз нужна. Шаг 1 его не трогает.
3. **Шаг 5** задаёт порядок `order`: 1 — карточка, 2 — `#рейтинг-итог`, 3 — «Как считается» и
   объяснение, 4 — «Назад». После 8.2 остаются только 1 — карточка и 2 — «Назад».

### Проверки в tests/, которые правятся тем же коммитом

- `tests/браузер-рейтинг-круг-друзей.js:410, 528` — `locator('#кнопка-рейтинг-очки').click()`
  по невидимой кнопке **упадёт по таймауту**: сперва нажать `#кнопка-рейтинг-фильтры`.
- `tests/рейтинг-переключатель-правил.js:160–201` — `[data-правило]` ищется и жмётся через
  `evaluate`, то есть переживёт скрытие молча и станет украшением: добавить открытие «Фильтров» и
  пункт «до нажатия кнопки «Подкидной» не видно».
- `tests/снимок-рейтинг-эталон.js:724–731` (клик по мере), `:752` (`#рейтинг-фильтры` без `скрыт` —
  проверка станет ложью), `:625` (рядом дописать 8.4); плюс новые пункты 8.2, 8.5, 8.6, 8.7, 8.8.
- `tests/снимок-рейтинг-золото.js:360, 576–593, 834` — клики по мере и замеры цвета мер/фильтров:
  открывать «Фильтры» перед замером.
- `tests/подиум-2-1-3.js:269, 356, 371, 415, 466, 489, 528, 597` и
  `tests/рейтинг-партия-5.js:87, 142, 163` — клики по `#кнопка-рейтинг-очки` в поддельном DOM: узел
  остаётся на месте, должны остаться зелёными; тестировщик подтверждает прогоном, а не на глаз.
- Новые пункты в `tests/рейтинг-партия-5.js`: `счётчикСреза` (8.1), `подписьСтолбцаОчков` (8.3),
  умолчание меры (8.10) — чистые, node, один запуск.

### 8.11 Шторка выбора игры: четыре мелочи против макета (смотритель 22.09 22:39)

Снимок `tests/снимки/рейтинг-шторка-390.png` рядом с `references/screens/02_game_picker.png`:
устройство совпало (снизу, затемнение, круглые углы, значки, галочка у выбранной, «Общий рейтинг»
под «Все игры»), поломок нет. Расхождения:

- **Файлы:** `js/рейтинг-экран.js` (структура), `style.css` (вид). Образец в макете:
  `prototype/index.html` 12–16 (`dialog#picker`, `.dialog-header` с `button.icon-button#close-picker` «×»,
  `label.search-field` со `span.search-symbol` перед полем) и `prototype/app.js` `renderOptions`
  (у невыбранных строк `icon('right')` — шеврон «›», у выбранной `icon('check')`).
1. **«×» вместо слова «Закрыть».** Кнопка `#кнопка-рейтинг-панель-игр-закрыть` (js ~2415) получает
   текст «×» и `aria-label="Закрыть выбор игры"`; класс `.рейтинг-панель-игр__закрыть` в css — круглая
   кнопка 32×32, как `.icon-button` макета. Проверка: `textContent.trim() === '×'` и `aria-label` есть.
2. **Лупа в поле поиска.** Перед полем `.рейтинг-панель-игр__поиск` (js ~2409) — обёртка
   `.рейтинг-панель-игр__поле-поиска` с `span.рейтинг-панель-игр__лупа` (`aria-hidden="true"`), картинка
   лупы — css `background` через data-URI или существующий svg в `img/рейтинг/` (если нет — новый
   `img/рейтинг/лупа.svg`, без двойных дефисов в комментариях, см. 8.7). Проверка: узел лупы есть,
   `offsetWidth > 0`.
3. **Шевроны у невыбранных строк.** В пункте панели (js ~2476–2490) у строки без галочки — узел
   `span.рейтинг-панель-игр__шеврон` («›»), у выбранной — галочка, как сейчас. Проверка: у строки
   «Дурак» (выбрана) галочка и нет шеврона; у «Шашки» шеврон и нет галочки.
4. **Заголовок «ИГРЫ» виден.** В js он строится (`разделПанелиИгр('ИГРЫ')`, ~2563), но на снимке
   смотритель его не увидел. Сперва замер: `offsetHeight > 0` и цвет отличим от фона
   (`getComputedStyle().color !== background`). Если узел есть, но не виден — правило css для
   `.рейтинг-панель-игр__раздел` (тихий цвет `--сб-текст-тихий`, 12px, верхний регистр, отступ 12px 0 4px).
- **Ломающие запуски:** копия js с текстом «Закрыть» вместо «×»; копия js без узла лупы; копия js с
  галочкой у всех строк; копия css с `display:none` у заголовка раздела.
- **Кто:** developer js (после 8.1–8.3, тот же исполнитель) → developer css → tester.
