# План: рейтинг дурака, партия 8 — шторка выбора игры и возврат по макету

Открыт 23.09 02:24 (часы хука). Источник — сверка с макетом `C:/Users/Admin/Downloads/leaderboard_cloudcode_pack_v1/leaderboard_cloudcode_pack_v1/` ревьюером (только чтение, 02:23) и смотрителем по снимку tests/снимки/рейтинг-шторка-390.png против references/screens/02_game_picker.png (02:23–02:24). Пункт очереди — 25 (зонтик «рейтинг по макету»).

## Что нашли
1. **Стрелка Telegram при открытой шторке** закрывает весь экран, а по макету (docs/01, раздел 5 «Возврат») первое нажатие закрывает шторку, второе — уходит в origin. У нас назадИзРейтинга (≈3174) не смотрит панельВыбораИгрыОткрыта()/шторкаИнфоОткрыта(). — **отдано 7-А третьим пунктом 02:23** (тот же файл).
2. **Крестик «×» шторки** стоит не в строке заголовка «Выбрать игру» (в макете — справа от заголовка), а в строке ниже, рядом с полем поиска: js добавляет его в шапку после поля (≈2945–2948), оформление раскладывает шапку так, что заголовок один в строке.
3. **Пункт «Все игры»** в шторке без значка (в макете — сетка из четырёх квадратов, тот же знак, что в кнопке выбора: SVG есть в js ≈2566, но пункту панели его не дают сознательно, см. комментарий ≈2966–2975).
4. **Порядок игр**: макет — Дурак, Шахматы, Шашки, Нарды; у нас РЕЕСТР_ИГР (≈139) — Дурак, Шашки, Шахматы, Нарды.
5. **Раздел «СКОРО»** с Домино и Морским боем есть в макете; у нас код раздела готов (заполнитьСписокИгр, статус 'скоро'), но в реестре таких записей нет. Вопрос владельцу в «Важном» (ключ `шторка-скоро-домино`); по умолчанию — как в макете, пункты не нажимаются.
6. Оформление шторки (по смотрителю): поле поиска без рамки-контура; заголовок «Выбрать игру» легче и меньше макета; у пунктов нет внутреннего поля слева (карточка вплотную к краю шторки); затемнение фона за шторкой светлее макета.
7. Справочно (не делаем, решение штаба в плане партии 7, пункт 4): двух- и трёхколоночная раскладка на 768+/1200+, диалог выбора по центру на ПК. Удержание фокуса Tab внутри шторки — мелочь для клавиатуры, отложено.

## Этапы
- **8-А (developer, js/рейтинг-экран.js + tests/рейтинг-партия-8.js)** — после коммита 7-А: пункты 2 (× сразу после заголовка, до поля поиска), 3 (значок-сетка пункту «все»: тот же SVG, что у кнопки выбора; узел значка с классом как у игр, чтобы поле слева совпало), 4 (порядок реестра), 5 (две записи статус 'скоро': домино, морской бой — если владелец не ответил «нет»; пункт не нажимается, без галочки, подпись «скоро»). Проверка в песочнице vm: порядок пунктов, у «все» есть узел значка, × — второй ребёнок шапки, раздел «СКОРО» с двумя пунктами и без обработчика выбора; ломающий запуск на копии. ?v= рейтинг-экран.js поднять штабом при коммите.
- **8-Б (developer, style.css — css-В)** — после коммита css-Б: шапка шторки — сетка «заголовок + × в одной строке, поле поиска второй строкой на всю ширину»; рамка-контур поля поиска; вес/размер заголовка как в макете; внутреннее поле слева у пунктов; затемнение фона темнее; пункты «скоро» приглушены. Проверка: tests/снимок-рейтинга-с-панелью.js 17/0 + снимок → смотритель против 02_game_picker.png.

## Журнал
- 23.09 02:24 — план открыт; пункт 1 ушёл в 7-А; вопрос про «СКОРО» — в «Важное».
- 23.09 02:31 — 8-А запущен (developer sonnet, js/рейтинг-экран.js + tests/рейтинг-партия-8.js, задание scratchpad/задание-партия-8-js.md) сразу после коммита 7-А 9723b6e. Задание css-В написано (scratchpad/задание-css-в.md), ждёт коммита css-Б.
- 02:41 — ревью 7-А (sonnet): принять с замечаниями. **Важно, в 8-А (тот же файл):** (1) на экране «Как я играю» кнопка «Выберите другую игру» после выбора зовёт общий `обновить` и перерисовывает чужую таблицу `#рейтинг-таблица`, а `#счёт-таблица` остаётся с «Игра недоступна» — звать `открытьСчёт(ключ)`; (2) `назадИзСчёта` (~3796) не закрывает сперва панель/шторку, как `назадИзРейтинга` — добавить те же две проверки. Мелочи: (3) два узла с id `кнопка-рейтинг-выбрать-игру`, если оба экрана в беде разом — сейчас недостижимо; (4) js/нижние-вкладки.js:287 `к-играм-рейтинг` прячет экран напрямую, минуя `назадИзРейтинга` — открытая шторка не сбрасывается; отдельный малый этап после 8-А (нужна наружная функция закрытия у ЭкранРейтинга).
- 02:47 — нарушение 8-А: в 02:40 запустил цепочкой через `| tail` три проверки, в том числе браузерную tests/рейтинг-наполненный.js (браузер в это время был у css-Б). Одёрнут сообщением; в отчёте владельцу — упомянуть. Урок: в заданиях на регресс «рейтинг-*.js» писать явно, какие из них браузерные, а не «без браузера».
