# План этапа «Экран „Профиль“ по макету владельца»

Опись и план составлены исполнителем (Plan) 22.09 22:20 по макету
`C:/Users/Admin/Downloads/leaderboard_cloudcode_pack_v1/leaderboard_cloudcode_pack_v1/`.
Номера строк — на момент осмотра; после чужих правок искать по именам функций и селекторам.

Важность: **потом** — после партии 6 рейтинга (Э2 и Э4 трогают те же js/рейтинг-экран.js и style.css).
Штаб утвердит перед запуском; вопрос владельцу «золото или салат» — в штаб/важное.json при старте.

## 1. Опись

### 1.1 Что показывает профиль в макете

- `prototype/app.js:124–125` — весь экран профиля это `header('Профиль')` + `<main class="main">` + `breakdown()`.
- Шапка `header` (`app.js:40–43`): слева круглая «‹» (назад), посередине название, справа круглая «i»
  («Как считается рейтинг»). Полоса вкладок на телефоне в шапке спрятана (`prototype/styles.css:27`).
- `breakdown()` (`app.js:63–69`) — единственный блок с данными: подзаголовок «ВАШИ ОЧКИ ПО ИГРАМ»
  (`.option-heading`, `styles.css:112`: 11px, разрядка, тихий цвет); по строке на игру: картинка 32×32,
  название, очки золотом (`.breakdown-row`, `styles.css:116–117`), каждая строка — **кнопка** в рейтинг этой
  игры (`app.js:155`); внизу «Всего» и сумма (`.breakdown-total`, `styles.css:118`).
- Тот же `breakdown()` макет показывает внутри окошка «Как считается» (`app.js:149`).
- `docs/01:29–46`: «Всего» — сумма очков по играм; игра без партий даёт 0. `docs/01:104–117` — состояния:
  загрузка, ошибка с «Повторить», пусто, новичок (место «—», очки 0), нет аватара → инициалы.
- `docs/02:18–30`: колонка, поля 16px (12 на 320–359), название 22px, строка игры около 70px, резерв под
  закреплённый низ. `docs/05:20–36`: проверять 320×568 … 1440×900, без горизонтальной прокрутки.
- Чего в макетном профиле **нет**: имени и фото игрока, места, серии дней, «Как я играю», друзей
  (`docs/04:92–96` — прототип без сети и без настоящего игрока).

### 1.2 Что из этого уже есть данными

| Что нужно | Есть ли | Откуда |
|---|---|---|
| «Всего» (сумма очков по всем играм) | есть | `/рейтинг` с `игра:'все'` → `я.очки` (`server/сервер.js:2708–2718`, `server/рейтинг.js:1088–1095`) |
| Моё место в общем рейтинге и «из скольких» | есть | тот же ответ: `я.место`, `всего`, `яВТаблице` |
| Стрелка движения места за сутки | есть | `я.движение` + `я.сдвиг` (`server/рейтинг.js:931–948`) |
| **Очки отдельно по каждой игре** | **нет одним ответом** | сервер отвечает по одной игре за раз; заводим поле `поИграм` (Э1) — сервер и так обходит все таблицы в `списокПоВсемИграм` (`server/рейтинг.js:1040–1079`) |
| Имя и фото игрока | есть | `window.Телеграм.имяИгрока()` / `.фотоИгрока()` (`js/telegram.js:125, 153`); вне Telegram — инициалы (`js/рейтинг-экран.js:1174`) |
| Серия дней подряд | есть, пишется только у дурака | `window.Рейтинг.серияДней()` (`js/telegram.js:3902`); узел `#серия-дней` заполняет только `js/game.js:10205–10222` |
| «Как я играю» | есть | экран `#экран-рейтинга`: у дурака — `js/game.js` (`index.html:641–659`), на трёх других — `js/рейтинг-экран.js:3006–3025` |
| Картинки игр | есть | `img/рейтинг/дурак.png` + `@2x` и т.д. |
| Список игр | есть | `РЕЕСТР_ИГР` (`js/рейтинг-экран.js:114–121`), наружу `пунктыПанелиИгр()` — второй копии не заводим |

Не выдумываем: аватары чужих, «место среди друзей», сезоны и награды (`docs/01:23`), второе правило очков.

### 1.3 Где сейчас живёт вкладка «Профиль»

- витрина: `кнопка-вкладка-мой-счёт` → действие `мой-счёт` (`js/нижние-вкладки.js:76, 189–192`) → «Как я играю»;
- лобби дурака: `лобби-вкладка-профиль` (`нижние-вкладки.js:89`) → `открытьПрофильДурака` (`js/game.js:10855–10858`);
- лобби шашек / шахмат / нард: `мой-счёт-шашки|шахматы|нарды` (`нижние-вкладки.js:107, 122, 136` → 210–224)
  → `window.ЭкранРейтинга.открытьСчёт(игра)` (`js/рейтинг-экран.js:3225`); туда же карточка `#лобби-профиль`;
- экран рейтинга: `мой-счёт-рейтинг` (`нижние-вкладки.js:242–252`).

Сегодня «Профиль» везде = «Как я играю». После этапа «Профиль» = новый экран, «Как я играю» — дверь из него.

## 2. Решение

**Один общий модуль `js/профиль-экран.js`, разметку строит сам, гнёзд в html не заводим.** Доводы:
1. Так сделан рейтинг (`штаб/план-рейтинг-по-макету-2.md`: «разметку чинит только js»).
2. Расширять «Как я играю» дорого: у дурака им распоряжается `js/game.js` (13 346 строк), у трёх других —
   `js/рейтинг-экран.js`.
3. Источники разные: «Как я играю» — память телефона про одну игру; профиль макета — очки с сервера про все.
4. `js/рейтинг-экран.js` уже 3342 строки и занят партией 6.

**Каркас — как у рейтинга:** колонка `max-width: min(100%, var(--колонка-широкая, 520px))`
(`style.css:11518–11529`), тёмная страница, прокручивается сам экран (`overflow-y:auto;
overscroll-behavior:contain`), внизу полоса вкладок, сверху шапка «‹ Профиль».

**Про акцент.** Золото разрешено на `#экран-рейтинга-игроков` (`style.css:11446–11453`). Профиль показывает
те же очки — предлагаем тот же набор переменных (`--рейтинг-золото-общее`, `style.css:333–334`). Вопрос
владельцу в важное.json: «золото или салат». До ответа — золото (переиграть — четыре строки).

## 3. Договор имён

- id экрана: **`экран-профиля-игрока`**.
- Шапка: `.профиль-шапка`, `#кнопка-профиля-назад.профиль-шапка__назад`, `h2.заголовок.профиль-шапка__название`.
- Карточка игрока: `#профиль-карточка.профиль-карточка`, внутри `__аватар`, `__имя`, `__место`, `__очки`, `__движение`.
- Очки по играм: `#профиль-очки.профиль-очки`; `.профиль-очки__заголовок`; строка-кнопка `.профиль-очки__строка`
  с `__значок` (img со srcset), `__игра`, `__число`; итог `.профиль-очки__итог` с `__итог-подпись`, `__итог-число`.
- Серия: `#профиль-серия.профиль-серия`, модификатор `.профиль-серия--вчера`.
- Дверь вниз: `#кнопка-профиля-как-я-играю`, классы `кнопка кнопка--мелкая`.
- Полоса: `<nav class="нижние-вкладки" data-вкладки="профиль">`, активна «Профиль».
- Пустое и беда: общие `скрыт`, `таблица__пусто`, `пометка-скоро`.

**js задаёт только структуру, id, классы, текст и числа. css — всё остальное.**

## 4. Шаги по файлам

Порядок: Э1 ∥ Э2 → Э3 → Э4 ∥ Э5 → Э6 → Э7 (штаб).
**Э2 и Э4 трогают `js/рейтинг-экран.js` и `style.css` — не начинать до коммита партии 6 рейтинга.**

**Э1. Сервер отдаёт мои очки по каждой игре** — `server/рейтинг.js` + `server/сервер.js`. *developer полная,
затем reviewer обязательно.* В `server/рейтинг.js` рядом со `списокПоВсемИграм` (1040–1079) — новая
`моиОчкиПоИграм(номер)`: обойти `таблицы`, взять запись игрока, вернуть `[{ игра, очки, побед, партий }]`
по убыванию очков; номера наружу не идут. В `server/сервер.js`, `таблицаРейтинга` (2663–2767), при
`этоВсеИгры` поле `поИграм`, при одной игре — `null`. Проверка `tests/профиль-очки-по-играм-сервер.js`:
три игры → три записи, сумма = `я.очки`; без записей → пустой массив. Краснеет на `null` вместо массива и
на подмене порядка.

**Э2. Одна дверь к данным профиля** — `js/рейтинг-экран.js`. *developer лёгкая.* `мойПрофиль()` через тот же
кэш (`последнийОтвет`, 209–216), чистит поля (`чистаяСтрока` 395, `число` 422), отдаёт
`{ беда, узналиВас, место, всего, очки, движение, сдвиг, поИграм }`; нет поля — честный `null`. Наружу
`window.ЭкранРейтинга.мойПрофиль` и `.игры = пунктыПанелиИгр`. Проверка: `tests/рейтинг-экран-четыре-игры.js`
случаи «без поИграм» и «мусор в поИграм». Краснеет на пустом массиве вместо `null`.

**Э3. Сам экран** — новый `js/профиль-экран.js`. *developer полная.* Строит `#экран-профиля-игрока` приёмами
`построитьРазметкуРейтингаИгроков` (`js/рейтинг-экран.js:2185–2222`); полоса вкладок последним узлом
(лениво через `window.НижниеВкладки.заполнитьПолосу`). Наружу `window.ЭкранПрофиля = { открыть }`;
`открыть(игра)` запоминает, откуда пришли (приём `назадИзРейтинга`, 2643–2664), привязывает стрелку
Telegram. Содержимое: имя и фото из `window.Телеграм`; место/очки/строки — из `мойПрофиль()`; все четыре
игры всегда, без записи — `0`. Состояния по `docs/01:104–117`; старый сервер без `поИграм` — блок очков не
рисуем, одна честная строка. Строка игры → `ЭкранРейтинга.открыть(ключ)`; кнопка внизу →
`открытьСчёт(играСтраницы)`. Серия — `серияДней()`, текст по правилу `словаОСерииВПрофиле`
(`js/game.js:10186–10193`). Ключи памяти не трогаем. Проверка `tests/профиль-экран-случаи.js` (vm, образец
`tests/рейтинг-партия-5.js`). Краснеет на подмене суммы, пропаже строки, цифрах при `null`.

**Э4. Вид экрана** — `style.css`. *designer.* Числа из `prototype/styles.css` (112, 116–118); каркас как у
`#экран-рейтинга-игроков` (`style.css:11415–11445`, 11514–11516); золото ссылками на `--рейтинг-золото-общее`;
полосе — набор `--вт-*` (образец 11488–11503).

**Э5. Вкладка «Профиль» ведёт в профиль** — `js/нижние-вкладки.js`. *developer лёгкая.* Набор `'профиль'`;
действия `мой-счёт*` → `window.ЭкранПрофиля.открыть(игра)`, без модуля — прежнее поведение. Проверки:
`tests/проверка-нижних-вкладок.js` (набор `профиль`), `tests/карта-входов-в-рейтинг.js`.

**Э6. Браузерная проверка и снимок** — `tests/снимок-профиля.js`. *tester.* 390×844 и 320×568: открывается с
четырёх страниц, четыре строки, «Всего» = сумме, нет горизонтальной прокрутки, низ достижим, «Назад»
возвращает. Ломающие — копия экрана без `overflow-y` и `padding-bottom`. Смотритель — с
`references/screens/01_global.png`.

**Э7. Подключение и номера** — *штаб.* `<script src="js/профиль-экран.js?v=1">` на четырёх страницах после
`js/рейтинг-экран.js` и `js/нижние-вкладки.js`; `?v=` у изменённых общих файлов. Заодно `нарды.html:607` и
`614` — `js/нижние-вкладки.js` подключён дважды (проверить после коммита f78f793).

## 5. Риски и чего НЕ делать

- Не трогать ключи памяти игрока. Не раздваивать реестр игр, объяснение «как считается», сумму очков.
- Не ломать дверь «Как я играю» (`#экран-рейтинга`, `кнопка-мой-счёт`, `открытьПрофильДурака`, `открытьСчёт`).
- Новое поле сервера доедет после перезапуска 8790 (только штаб) — клиент переживает честной строкой.
- Не заводить друзей, сезоны, награды, второй рейтинг.
- Telegram: `overscroll-behavior: contain`; «‹» внутри Telegram прячет `html.в-телеграме`.
- Мелочи: набор `--вт-*`, `flex-shrink: 0`, `srcset`; порядок скриптов — профиль строго после рейтинга.

## 6. Приёмка

1. `node tests/профиль-очки-по-играм-сервер.js` — три записи, сумма = `я.очки`; без партий — пустой массив; ломающий красит.
2. `node tests/профиль-экран-случаи.js` — ровно 4 строки; «Всего» = сумме; новичок: «—», 0; беда: «Повторить»; без `поИграм` — блока нет.
3. `node tests/рейтинг-экран-четыре-игры.js`, `node tests/рейтинг-партия-5.js` — прежние зелёные.
4. `node tests/проверка-нижних-вкладок.js` — набор «профиль»: 3 вкладки, активна третья.
5. `node tests/карта-входов-в-рейтинг.js` — «Профиль» отовсюду ведёт в `ЭкранПрофиля.открыть`.
6. `node tests/корень-чистый.js`.
7. Браузер `tests/снимок-профиля.js`: 390×844 и 320×568 — без горизонтальной прокрутки, низ достижим, «Назад» работает.
8. Смотритель — с `references/screens/01_global.png`.
9. Ревьюер — Э1 обязательно и Э3.
10. Штаб: `?v=` одним коммитом, проверка опубликованного адреса.
- 23.09 00:16 — Э3 сдан (js/профиль-экран.js новый, tests/профиль-экран-случаи.js 67/0, --сломать 67/14 на трёх порчах копии). Разработчик развёл случаи: новичок → четыре строки по нулям; узналиВас и поИграм не массив → одна честная строка «Очки по играм появятся после обновления». Замечания разработчика: на index.html возврат из «Как я играю» идёт через память game.js (экранОткудаПришли) и может вернуть не в профиль — дверь для Э7; Э4 нужны overflow-y:auto, нижний отступ под полосу и вид .профиль-карточка__инициалы. Ревьюер sonnet читает.
- 00:16 — Э1 первая половина сдана: server/рейтинг.js моиОчкиПоИграм(номер) (+62 строки, только добавление), tests/профиль-очки-по-играм.js 36/0, --сломать 38/0 при двух замеченных поломках (код 0 = обе поломки замечены; выход 2, если строку поломки не нашли). Вторая половина (server/сервер.js таблицаРейтинга: поИграм при этоВсеИгры, иначе null) — после коммита «пропал» Э3. Ревьюер sonnet читает первую половину.
- 00:16 — Партия 6 рейтинга у браузерного тестировщика sonnet (переписывает модель прокрутки в снимок-рейтинг-эталон.js, правки золото.js, снимки 390×844 Дурак/Все игры для смотрителя). Э4 (style.css) — только после коммита партии 6.
- 00:23 — ревью Э1 (первая половина, sonnet): принять без замечаний. Э1б (server/сервер.js таблицаРейтинга: поИграм при «все игры», иначе null; tests/профиль-очки-по-играм-сервер.js) отдан разработчику sonnet.
- 00:23 — ревью Э3 (sonnet): принять с замечаниями — (1) назадИзПрофиля должна идти через window.вернутьсяОткудаПришли, как актуальная назадИзРейтинга (две памяти «Назад» расходятся); (2) словаОБеде теряет текст причины сервера (мойПрофиль кладёт причину как есть). Возвращено разработчику Э3. На будущее (game.js, не Э3): ЭКРАНЫ_ПОВЕРХ (game.js:11748) не знает «экран-профиля-игрока» — опрос сети может выдернуть из профиля посреди чтения; добавить в Э7а/Э7.
- 00:29 — Э3-правки сданы: назадИзПрофиля через window.вернутьсяОткудаПришли (запасной путь — своя память), словаОБеде показывает причину сервера слово в слово, коды нет-адреса/плохой-адрес/связь — свои фразы, отказ-сервера/пусто — «Очки пока не прочитались». tests/профиль-экран-случаи.js 71/0 (штаб перепроверил), --сломать 71/16. Э3 ПРИНЯТ.
- 00:29 — ревью Э5 (sonnet): вернуть — на шахматы.html «Игры» из профиля не прячет экран (кнопка-лобби-назад → уйтиНаВитрину() в js/шахматы-экран.js снимает экран--виден только с перечисленных узлов); index/шашки/нарды прячут широко — верно. Та же беда, похоже, у «к-играм-рейтинг» с экраном рейтинга на шахматах. Отдано разработчику sonnet 00:31: js/нижние-вкладки.js помощница спрятатьЭкран(id) перед дверью для обоих действий + проверки в tests/проверка-нижних-вкладок.js.
- 00:31 — ревью партии 6 (opus) нашло, что дверь профиля Э2 (профильПоОтвету в js/рейтинг-экран.js) проверяет ок===false раньше узналиВас — игрок вне Telegram получал «отказ сервера» вместо нулей. Чинится вместе с правками партии 6 (см. план-рейтинг-партия-6.md 00:31).
- 00:34 — Э7а сдан (sonnet): открытьПрофильДурака зовёт window.ЭкранПрофиля.открыть('дурак') при наличии, иначе показатьМойСчёт; ЭКРАНЫ_ПОВЕРХ + 'экран-профиля-игрока'; возврат «‹» из «Как я играю» в профиль — без правок (запомнитьГдеБыли видит любой .экран). tests/дверь-профиля-дурака.js 21/0 (штаб перепроверил), --сломать ловит три порчи; регресс карта-входов 106/0, нижние-вкладки 110/0. Разница 41 строка → scratchpad/дифф-профиль-э7а-game.txt, ревьюер sonnet 00:37.
- 00:37 — Э1б сдан (sonnet): server/сервер.js таблицаРейтинга — поИграм при «все игры» (≈2739, ≈2772), null при одной игре; tests/профиль-очки-по-играм-сервер.js 15/0 (штаб перепроверил; порт 8801, --сломать копия сервера на 8802 краснеет на а и г); рейтинг-все-игры-сервер 41/0. Ловушка Node/Windows: server.close + process.exit → UV_HANDLE_CLOSING, обход паузой 100 мс. Разница 30 строк → scratchpad/дифф-профиль-э1б-сервер.txt, ревьюер sonnet 00:38.
- 00:37 — Э5-правка сдана (sonnet): js/нижние-вкладки.js помощница спрятатьЭкран(id) перед дверью в 'к-играм-профиль' (экран-профиля-игрока) и 'к-играм-рейтинг' (экран-рейтинга-игроков — пункт 2 ревьюера подтвердился); tests/проверка-нижних-вкладок.js 112/0 (штаб перепроверил), --сломать 10/10 сценариев. Э5 ПРИНЯТ. Корень (уйтиНаВитрину в js/шахматы-экран.js узкая) — пункт 57 очереди, разработчик sonnet 00:37 (широкий приём + tests/шахматы-витрина-прячет-всё.js).
- Осталось: ревью Э7а и Э1б → Э4 (designer, style.css — после коммита партии 6) → Э6 (браузер, снимок-профиля.js) → Э7 подключение на 4 страницах + ?v= нижние-вкладки.js + коммит.
- 00:42 — ревью Э1б (sonnet): принять, замечаний нет (кто — опознанный номер; поле не ломает клиента и рейтинг-все-игры-сервер; общий catch двери → 500; проверка честно краснеет на копии 8802). Э1 ЦЕЛИКОМ ПРИНЯТ, коммит c31fac2 (server/рейтинг.js + server/сервер.js + tests/профиль-очки-по-играм.js 36/0 + tests/профиль-очки-по-играм-сервер.js 15/0). Боевой 8790 перезапущен при 0 комнат 00:44, стоит на c31fac2.
- 00:45 — ревью Э7а (sonnet): принять; одно наблюдение на будущее — ветка с ЭкранПрофиля не выставляет экранОткудаПришли (сейчас безопасно: профиль открывается только из лобби; при новом входе не из лобби выставлять явно или довериться памяти назадИзПрофиля). Э7а ПРИНЯТ, коммит d04d533 (js/game.js + tests/дверь-профиля-дурака.js + index.html game.js?v=90).
- 00:45 — Э4 (designer sonnet) запущен: только style.css, новый блок в КОНЦЕ файла, все селекторы с префиксом #экран-профиля-игрока, золото ссылкой на --рейтинг-золото-общее, набор --вт-* на экране, overflow-y и padding-bottom под полосу (задание scratchpad/задание-э4-профиль.md). Снимков не делает (браузер занят) — снимок Э6.
- Осталось: Э4 → Э7 подключение (штаб: `<script src="js/профиль-экран.js?v=1">` после рейтинг-экран.js и нижние-вкладки.js на 4 страницах; выровнять порядок — пункт 37; нарды.html двойной include) → Э6 браузер (когда свободен) → коммит с ?v= нижние-вкладки.js и словами «профиль по макету: экран подключён на четырёх страницах».
- 00:58 — Э4 сдан (designer sonnet): блок «Экран «Профиль»» в конце style.css (15352–15737), все селекторы с #экран-профиля-игрока, золото через --рейтинг-золото-общее, --вт-* и padding-bottom под полосу, «‹» #кнопка-профиля-назад отдельным набором правил (44×44, прячется в Telegram). Ограничение ширины колонки на ПК не добавлял — при надобности отдельный малый этап. Закоммичен вместе с партией 6 (aeb8dcc) — экран ещё не подключён, правила никого не красят. Ревьюер (sonnet) читает блок против js/профиль-экран.js и макета.
- 01:02 — Э7 (штаб): `<script src="js/профиль-экран.js?v=1">` вставлен после рейтинг-экран.js на index.html:1253, шашки.html:918, шахматы.html:603, нарды.html:764; нижние-вкладки.js?v=7→8 на четырёх страницах (Э5). Двойного include в нарды.html уже нет (f78f793). Порядок вкладки→рейтинг→профиль одинаков на всех четырёх. Незакоммичено: ждём Э6 (браузер, задание-э6-снимок-профиля.md — после тестировщика золота и css-разработчика) и ревью Э4.
- 01:03 — ревью Э4 (sonnet): ПРИНЯТЬ. Три мелочи, не блокируют: (1) аватар clamp(84px,27vw,100px) — сознательное отступление от 100px макета под 320; (2) font-weight 600 у .профиль-очки__заголовок (15611–15618) — в макете .option-heading веса нет; (3) боковые 4px у строк/итога (15624–15637, 15675–15682) — в макете 0. Мелочи 2–3 можно снять при следующей правке style.css (пункт очереди не заводим, запись здесь). Э4 ПРИНЯТ.
- Осталось: Э6 (браузер, tests/снимок-профиля.js по заданию scratchpad/задание-э6-снимок-профиля.md) — запуск после css-разработчика (его прогон снимок-рейтинг-эталон.js держит браузер) → смотритель против 01_global.png → коммит: js/профиль-экран.js (новый), js/нижние-вкладки.js, tests/проверка-нижних-вкладок.js, tests/снимок-профиля.js, четыре страницы (нижние-вкладки.js?v=8 + профиль-экран.js?v=1), в сообщении слова «профиль по макету: экран подключён на четырёх страницах» → проверка опубликованного адреса.
- 01:10 — Э6 запущен (tester sonnet, браузер): tests/снимок-профиля.js по заданию scratchpad/задание-э6-снимок-профиля.md — четыре страницы, 390×844 и 320×568, три ломающих на копиях, снимки профиль-390.png / профиль-320.png. Css-разработчик рейтинга предупреждён ждать браузер.
- 01:12 — ЛОВУШКА: на машине нет wmic (Windows 11 26200) — проверка «занят ли браузер» во всех заданиях молча отвечала «свободно». Обоим исполнителям с браузером разослана замена (powershell Get-CimInstance); разработчик (sonnet) пишет штаб/браузер-занят.js; урок в штаб/уроки.md.
- 01:14 — следующий css-этап после сдачи css-разработчика А: scratchpad/задание-css-б.md — мелочи ревью Э4 (вес заголовка, боковые 4px), колонка профиля на ПК как у рейтинга, пункт 36 очереди (три экрана лобби под часами на 320×568, стенд все-экраны-в-полном-экране.js до 0 провалов).

- 01:19 — штаб/браузер-занят.js сдан и принят (свой прогон: «свободно», код 0). Закоммичен 78e065e вместе с правилом CLAUDE.md (строка о браузерных проверяющих теперь называет программу) и уроком. Э6 (снимок профиля), css-А, js-мелочи, пункт 28 — в работе.
- 01:29 — Э6 упёрся в 40 ходов без файла (ждал браузер, читал разметку; 124k контекста). Возвращён сообщением: писать tests/снимок-профиля.js одним Write, гнать обычный + три ломающих, ≤10 вызовов. Браузер в 01:28 свободен. Пункт 28 (самая-длинная-подсказка) закрыт коммитом 64fa2cb: прогон 2 мин 50 с, ключ --журнал; очередь опознала сделанным.
- 01:38 — Э6: tests/снимок-профиля.js (30 КБ, 01:32) и снимки профиль-390.png / профиль-320.png (01:36) есть, отчёта тестировщика ещё нет (ломающие идут). Смотритель по снимкам: поломок нет — наложений, обрезаний, горизонтальной прокрутки, ухода под полосу нет; шапка «‹ Профиль», аватар, «Игрок», «ВАШИ ОЧКИ ПО ИГРАМ», четыре строки + «Всего», «Как я играю», полоса с золотой вкладкой на месте. «–» вместо места и без стрелки — вне Telegram сервер не опознал, честные нули по плану; «серия» при нуле спрятана по плану; на 320×568 экран прокручивается (низ достижим — проверяет Э6). Мелочи вдогонку (не блокируют коммит): (а) вне Telegram у аватара нет буквы — инициалы('') пусто, а имя падает на «Игрок»: считать инициалы от того же запасного имени (js/профиль-экран.js ≈489); (б) золотое кольцо вокруг аватара и рамка карточки, как у «Моё место» макета — css, в css-Б или следующий css-этап.
