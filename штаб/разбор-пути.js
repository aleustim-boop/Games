/* Страница разбора: «сколько касаний до партии и почему столько текста».

   Зачем отдельная страница. Владелец написал: «вопрос про витрину я не понял,
   касаний много для старта согласен и много текста на странице. пришли
   развёрнутый ответ и варианты решения в дашборд». Вопрос ему был задан
   одной строкой в списке — так спрашивать нельзя, это вина штаба. Ответ
   должен быть страницей, которую читают с телефона, и выбрать в ней надо
   одним касанием.

   Все числа здесь — замеренные, не на глаз:
   - шаги пути сверены с index.html и js/game.js (кнопка «Дурак» открывает
     экран меню, «Играть с ботом» — экран настроек, «Играть» — стол);
   - знаки текста посчитаны по разметке: выброшены пояснения для нас,
     рисунки и спрятанные узлы, посчитано ровно то, что человек видит.

   Выбор владельца эта страница только показывает. Записывает его сервер
   (штаб/дашборд.js) в штаб/выбор-владельца.json. */

'use strict';

const fs = require('fs');
const path = require('path');
const страница = require('./страница.js');

const ФАЙЛ_ВЫБОРА = path.join(__dirname, 'выбор-владельца.json');

/* Замеры. Держим числа в одном месте: если экраны изменятся, править тут.
   Как получены — сказано на самой странице, чтобы им можно было верить. */
const ЗАМЕР = {
  витрина: 41,
  меню: 83,
  настройки: 259,      // 211 в разметке плюс 48 строки-пояснения от программы
  послеУборки: 115,    // если настройки стола спрятать за «Изменить»
  толькоВыбор: 51      // если оставить лишь уровень соперника и «Играть»
};
ЗАМЕР.всего = ЗАМЕР.витрина + ЗАМЕР.меню + ЗАМЕР.настройки;

const ВАРИАНТЫ = [
  {
    ключ: 'А',
    имя: 'Кнопка «Играть» прямо на плитке',
    касаний: 2,
    суть: 'На плитке «Дурак» в углу появляется маленькая кнопка. Нажали её — и вы сразу за столом, с теми настройками, с какими играли в прошлый раз.',
    плюс: 'Самый короткий путь: из чата в партию за два касания.',
    минус: 'Плитка перестаёт быть простой картинкой: на ней две цели вместо одной. Человек, который зашёл первый раз и ещё не знает, что такое дурак, может провалиться сразу за стол.',
    текст: 'текста на пути почти не остаётся — вы его просто не видите'
  },
  {
    ключ: 'Б',
    имя: 'Пропускать экран настроек',
    касаний: 3,
    суть: '«Играть с ботом» сразу начинает партию — с теми настройками, что были в прошлый раз. Сами настройки прячутся за кнопкой «Изменить» и открываются, только если они нужны.',
    плюс: 'Убирает целый экран из пути и больше половины текста на нём: 259 знаков превращаются в 115.',
    минус: 'Первую партию человек играет с настройками по умолчанию — вдвоём и по обычным правилам, — не увидев, что их можно поменять. Это ровно та игра, которая была у нас до появления этих кнопок.',
    текст: '259 → 115 знаков на экране настроек'
  },
  {
    ключ: 'В',
    имя: 'Слить витрину и меню',
    касаний: 3,
    суть: 'На плитке игры сразу две кнопки — «С ботом» и «С другом». Отдельного экрана «Выберите, с кем играть» больше нет.',
    плюс: 'Исчезает целый экран, и сразу видно, что в каждую игру можно играть и одному, и вдвоём.',
    минус: 'Витрина становится плотнее. Сейчас игр три, а когда их станет восемь, каждая плитка вырастет вдвое, и витрина перестанет помещаться на экран телефона.',
    текст: 'минус 83 знака — весь экран «Выберите, с кем играть»'
  },
  {
    ключ: 'Г',
    имя: 'Ничего не менять',
    касаний: 4,
    суть: 'Оставляем как есть.',
    плюс: 'Каждый экран простой и делает одно дело. Ничего не ломается, и переучиваться не нужно.',
    минус: 'Четыре касания перед каждой партией. Тот, кто играет каждый день, проходит этот путь снова и снова.',
    текст: '383 знака на пути остаются как есть'
  }
];

/** Что выбрал владелец — если уже выбрал. Ничего не выдумываем. */
function прочитатьВыбор() {
  try {
    const данные = JSON.parse(fs.readFileSync(ФАЙЛ_ВЫБОРА, 'utf8'));
    return данные && данные.вариант ? данные : null;
  } catch (ошибка) {
    return null;
  }
}

/** Обезвреживаем текст, как и на главной странице. */
function э(текст) {
  return String(текст === null || текст === undefined ? '' : текст)
    .replace(/&/g, '&amp;').replace(/</g, '&lt;')
    .replace(/>/g, '&gt;').replace(/"/g, '&quot;');
}

/* ---------------------------------------------------------------------------
   Оформление: только то, чего нет на главной. Цвета и шрифты общие.
--------------------------------------------------------------------------- */

const СВОЁ_ОФОРМЛЕНИЕ = `
  .разбор { max-width: 720px; margin: 0 auto; padding: 0 4px; }
  .разбор h1 { font-size: 28px; line-height: 1.15; margin: 0 0 10px; }
  .разбор h2 { font-size: 21px; margin: 34px 0 12px; }
  .разбор h3 { font-size: 17px; margin: 0 0 6px; font-family: "Alegreya Sans", sans-serif; }
  .разбор p { font-size: 16.5px; line-height: 1.55; color: var(--ink-soft); margin: 0 0 12px; }
  .разбор b { color: var(--ink); }
  .назад { display: inline-block; margin-bottom: 18px; font-size: 15px; color: var(--sukno); }

  .шаги { width: 100%; border-collapse: collapse; font-size: 15.5px; margin: 0 0 14px; }
  .шаги th, .шаги td { text-align: left; padding: 9px 10px; border-bottom: 1px solid var(--line-soft); vertical-align: top; }
  .шаги th { font-size: 13px; text-transform: uppercase; letter-spacing: .04em; color: var(--muted); }
  .шаги td:first-child { font-family: "JetBrains Mono", monospace; color: var(--muted); width: 34px; }

  /* Мокап — не картинка, а просто прямоугольники: так видно раскладку,
     и никакие файлы с рисунками не нужны. */
  .телефоны { display: flex; flex-wrap: wrap; gap: 14px; margin: 14px 0 8px; }
  .телефон {
    width: 168px; border: 2px solid var(--line); border-radius: 14px;
    padding: 10px; background: var(--surface); display: flex; flex-direction: column; gap: 7px;
  }
  .телефон .подпись-макета { font-size: 12px; color: var(--muted); text-align: center; margin: 0; }
  .кусок { border: 1.5px solid var(--line); border-radius: 7px; padding: 7px 8px; font-size: 12.5px; color: var(--ink-soft); }
  .кусок--заголовок { border: none; padding: 2px; font-weight: 700; color: var(--ink); font-size: 13px; }
  .кусок--плитка { display: flex; align-items: center; justify-content: space-between; gap: 6px; min-height: 40px; }
  .кусок--главный { background: var(--sukno); color: #fff; border-color: var(--sukno); text-align: center; font-weight: 700; }
  .кусок--мелкий { font-size: 11.5px; color: var(--muted); text-align: center; border-style: dashed; }
  .кусок--новый { border-color: var(--yantar); color: var(--yantar); font-weight: 700; }
  .значок { width: 22px; height: 26px; border: 1.5px solid currentColor; border-radius: 3px; flex: none; }

  .вариант {
    background: var(--surface); border: 1px solid var(--line); border-radius: var(--radius);
    padding: 16px 18px; margin: 0 0 12px; box-shadow: var(--shadow);
  }
  .вариант--советую { border-color: var(--sukno); border-left: 4px solid var(--sukno); }
  .буква {
    display: inline-block; width: 26px; height: 26px; line-height: 26px; text-align: center;
    border-radius: 50%; background: var(--surface-2); font-weight: 700; margin-right: 8px;
  }
  .вариант--советую .буква { background: var(--sukno); color: #fff; }
  .касания { font-family: "JetBrains Mono", monospace; font-size: 13px; color: var(--muted); }
  .за-против { display: grid; gap: 8px; margin-top: 8px; font-size: 15px; }
  .за-против div { padding-left: 22px; position: relative; color: var(--ink-soft); }
  .за-против .за::before { content: "+"; position: absolute; left: 4px; color: var(--sukno); font-weight: 700; }
  .за-против .против::before { content: "−"; position: absolute; left: 4px; color: var(--chervi); font-weight: 700; }

  .совет { background: var(--sukno-soft); border-radius: var(--radius); padding: 16px 18px; margin: 0 0 12px; }
  .совет p { color: var(--ink); }

  .кнопки-выбора { display: grid; gap: 10px; margin: 16px 0 8px; }
  .кнопка-выбора {
    display: block; text-align: center; text-decoration: none;
    background: var(--surface); border: 1.5px solid var(--sukno); color: var(--sukno);
    border-radius: var(--radius); padding: 16px 12px; font-size: 17px; font-weight: 700;
  }
  .кнопка-выбора span { display: block; font-size: 13.5px; font-weight: 400; color: var(--muted); margin-top: 3px; }
  .принято { background: var(--sukno); color: #fff; border-radius: var(--radius); padding: 16px 18px; margin: 0 0 14px; }
  .принято p { color: #fff; margin: 0; }
`;

/* ---------------------------------------------------------------------------
   Мокапы — прямоугольниками, прямо на странице
--------------------------------------------------------------------------- */

function макетВитрины(вариант) {
  const кнопкаНаПлитке = вариант === 'А'
    ? '<span class="кусок кусок--новый" style="padding:3px 7px">▶</span>' : '';
  const двеКнопки = вариант === 'В'
    ? '<div class="кусок кусок--новый" style="text-align:center">С ботом · С другом</div>' : '';

  return '<div class="телефон">'
    + '<div class="кусок кусок--заголовок">Настольные игры</div>'
    + '<div class="кусок кусок--плитка"><span class="значок"></span>Дурак' + кнопкаНаПлитке + '</div>'
    + двеКнопки
    + '<div class="кусок кусок--плитка"><span class="значок"></span>Шашки</div>'
    + '<div class="кусок кусок--плитка"><span class="значок"></span>Шахматы</div>'
    + '<p class="подпись-макета">витрина</p>'
    + '</div>';
}

function макетМеню() {
  return '<div class="телефон">'
    + '<div class="кусок кусок--заголовок">Подкидной дурак</div>'
    + '<div class="кусок кусок--мелкий">Выберите, с кем играть</div>'
    + '<div class="кусок кусок--главный">Играть с ботом</div>'
    + '<div class="кусок кусок--главный" style="background:var(--surface-2);color:var(--ink-soft);border-color:var(--line)">Играть с другом</div>'
    + '<div class="кусок кусок--мелкий">К выбору игры</div>'
    + '<p class="подпись-макета">с кем играть</p>'
    + '</div>';
}

function макетНастроек(короткий) {
  let нутро = '<div class="кусок кусок--заголовок">Игра с ботом</div>'
    + '<div class="кусок">Соперник: лёгкий · обычный · сложный</div>';

  if (короткий) {
    нутро += '<div class="кусок кусок--новый">Изменить стол</div>';
  } else {
    нутро += '<div class="кусок">Сколько игроков: 2 3 4 5 6</div>'
      + '<div class="кусок">Правила: без перевода · переводной</div>'
      + '<div class="кусок">Кто против кого: каждый сам · парами</div>'
      + '<div class="кусок кусок--мелкий">Двое, обычные правила — привычная игра с ботом</div>';
  }

  нутро += '<div class="кусок кусок--главный">Играть</div>'
    + '<div class="кусок кусок--мелкий">Раздача дня · Как играть · Рекорды</div>'
    + '<p class="подпись-макета">' + (короткий ? 'настройки спрятаны' : 'настройки сейчас') + '</p>';

  return '<div class="телефон">' + нутро + '</div>';
}

function макетСтола() {
  return '<div class="телефон">'
    + '<div class="кусок кусок--мелкий">колода · козырь</div>'
    + '<div class="кусок" style="min-height:52px">стол</div>'
    + '<div class="кусок кусок--главный" style="background:var(--yantar);border-color:var(--yantar)">ваши карты</div>'
    + '<p class="подпись-макета">партия началась</p>'
    + '</div>';
}

/* ---------------------------------------------------------------------------
   Куски текста
--------------------------------------------------------------------------- */

function какСейчас() {
  return `
  <h2>Как сейчас — по шагам</h2>
  <p>Сначала честно посчитаем. Вот весь путь от чата в Telegram до первой сданной карты. Ничего не пропущено.</p>
  <table class="шаги">
    <tr><th></th><th>Что вы видите</th><th>Что нажимаете</th></tr>
    <tr><td>1</td><td>чат с ботом</td><td>кнопку «Играть»</td></tr>
    <tr><td>2</td><td>витрина «Настольные игры»</td><td>плитку «Дурак»</td></tr>
    <tr><td>3</td><td>«Выберите, с кем играть»</td><td>«Играть с ботом»</td></tr>
    <tr><td>4</td><td>«Игра с ботом»: соперник, число игроков, правила, кто против кого</td><td>«Играть»</td></tr>
  </table>
  <p><b>Четыре касания.</b> Три из них внутри игры и одно в чате — но считать честно надо от чата, потому что вы всегда приходите оттуда. Я сверил этот путь не по памяти, а по самому коду игры: плитка «Дурак» открывает экран выбора, «Играть с ботом» — экран настроек, и только третья кнопка начинает партию.</p>`;
}

function проТекст() {
  return `
  <h2>Сколько на этих экранах текста</h2>
  <p>Вы написали «много текста на странице». Я посчитал, чтобы не спорить словами. Считал только то, что вы вправду видите: заголовки, подписи и надписи на кнопках. Пояснения, которые читаем мы, и рисунки не в счёт.</p>
  <table class="шаги">
    <tr><th>Экран</th><th>Знаков</th><th>Что там написано</th></tr>
    <tr><td>витрина</td><td><b>${ЗАМЕР.витрина}</b></td><td>названия игр, и только</td></tr>
    <tr><td>с кем играть</td><td><b>${ЗАМЕР.меню}</b></td><td>заголовок, подпись и три кнопки</td></tr>
    <tr><td>настройки</td><td><b>${ЗАМЕР.настройки}</b></td><td>четыре ряда настроек, пояснение и шесть кнопок</td></tr>
    <tr><td><b>всего</b></td><td><b>${ЗАМЕР.всего}</b></td><td>примерно две трети экрана телефона, занятые словами</td></tr>
  </table>
  <p>Видно, где беда: <b>две трети всего текста — на одном экране настроек</b>. Витрина и выбор соперника коротки, там убирать нечего.</p>

  <h2>Какой текст нужен, а какой можно спрятать</h2>
  <p>Тут есть простая мерка, и она делит текст надвое.</p>
  <p><b>Текст, чтобы выбрать</b>, — остаётся. Это «Соперник: лёгкий, обычный, сложный» и кнопка «Играть». Без него нельзя начать партию: вы должны сказать, с кем играете.</p>
  <p><b>Текст, чтобы понять игру</b>, — прячется за «Как играть». Это «переводной», «парами, 2 на 2», «каждый сам», «в колоде 52 карты — есть и двойки». Эти слова ничего не значат для того, кто дурака не знает, а тому, кто знает, они не нужны каждый раз перед партией.</p>
  <p>Если убрать со стартового экрана всё, что относится ко второй половине, останется <b>${ЗАМЕР.послеУборки} знаков вместо ${ЗАМЕР.настройки}</b> — меньше половины. Настройки при этом никуда не денутся: они за кнопкой «Изменить», в одном касании.</p>
  <div class="телефоны">${макетНастроек(false)}${макетНастроек(true)}</div>`;
}

function блокВариантов(выбор) {
  let куски = '<h2>Четыре варианта</h2>'
    + '<p>Каждый описан так: что вы получите и чем за это заплатите. Внизу можно выбрать один — нажатием, отвечать письмом не нужно.</p>';

  куски += '<div class="телефоны">' + макетВитрины('А') + макетМеню() + макетСтола() + '</div>'
    + '<p class="подпись-макета" style="text-align:left;font-size:13.5px;color:var(--muted)">Слева направо: витрина (с маленькой кнопкой из варианта А в углу плитки), экран выбора соперника, стол.</p>';

  for (const в of ВАРИАНТЫ) {
    const советую = в.ключ === 'Б';
    куски += '<div class="вариант' + (советую ? ' вариант--советую' : '') + '">'
      + '<h3><span class="буква">' + в.ключ + '</span>' + э(в.имя) + '</h3>'
      + '<p class="касания">' + в.касаний + ' касания от чата до партии · ' + э(в.текст) + '</p>'
      + '<p>' + э(в.суть) + '</p>'
      + '<div class="за-против">'
      + '<div class="за">' + э(в.плюс) + '</div>'
      + '<div class="против">' + э(в.минус) + '</div>'
      + '</div>'
      + (советую ? '<p class="касания" style="margin-top:10px;color:var(--sukno)">это я и советую — почему, ниже</p>' : '')
      + '</div>';
  }
  return куски;
}

function советШтаба() {
  return `
  <h2>Что я советую</h2>
  <div class="совет">
    <p><b>Вариант Б, и к нему маленькую добавку из А.</b></p>
    <p>Сначала Б: «Играть с ботом» сразу начинает партию с прошлыми настройками. Это убирает из пути целый экран и больше половины текста на нём, и при этом ничего не отнимает — настройки остаются в одном касании, за кнопкой «Изменить». Тот, кто хочет играть вчетвером и переводного, найдёт их там же, где они были.</p>
    <p>Почему не А как основной вариант: кнопка на плитке даёт самый короткий путь, но плитка перестаёт быть простой. Сейчас на витрине всё однозначно — нажал игру, попал в игру. С двумя целями на одной плитке человек, зашедший впервые, может провалиться сразу за стол и не понять, куда попал.</p>
    <p>Поэтому добавку из А стоит включать не всем, а <b>только тому, кто уже сыграл хотя бы одну партию</b>. Тогда новичок видит простую витрину, а вы, играющий каждый день, — кнопку «Продолжить» и путь в два касания. Игра узнаёт вас по тому, что вы уже играли: это она и так помнит, отдельно ничего спрашивать не надо.</p>
    <p>Вариант В (две кнопки на плитке) я не советую сейчас по одной причине: игр станет больше. Сегодня их три, и плитки помещаются на экран. С восемью играми витрина с двойными плитками перестанет помещаться, и мы будем разбирать её заново.</p>
  </div>`;
}

function кнопкиВыбора(выбор) {
  let куски = '<h2>Ваш выбор</h2>';

  if (выбор) {
    куски += '<div class="принято"><p><b>Принято: вариант ' + э(выбор.вариант) + '</b> — ' + э(выбор.имя || '') + '.<br>'
      + 'Записано ' + э(выбор.когда || '') + '. Можно нажать другую кнопку — запишется новое.</p></div>';
  } else {
    куски += '<p>Нажмите один раз. Больше ничего делать не нужно: выбор запишется, и штаб увидит его сам.</p>';
  }

  куски += '<div class="кнопки-выбора">';
  for (const в of ВАРИАНТЫ) {
    куски += '<a class="кнопка-выбора" href="/выбор?вариант=' + в.ключ + '">'
      + в.ключ + '. ' + э(в.имя)
      + '<span>' + в.касаний + ' касания до партии</span></a>';
  }
  куски += '</div>';
  куски += '<p style="font-size:14.5px;color:var(--muted)">Ничего не понравилось — просто напишите словами, что не так. Разбор не обязан заканчиваться одной из четырёх кнопок.</p>';
  return куски;
}

/* ---------------------------------------------------------------------------
   Целая страница
--------------------------------------------------------------------------- */

function целаяСтраница() {
  const выбор = прочитатьВыбор();
  if (выбор) {
    const найден = ВАРИАНТЫ.find(function (в) { return в.ключ === выбор.вариант; });
    if (найден) выбор.имя = найден.имя;
  }

  return `<!doctype html>
<html lang="ru">
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Путь до партии — разбор</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Alegreya:wght@500;700;800&family=Alegreya+Sans:wght@400;500;700&family=JetBrains+Mono:wght@400;700&display=swap">
<style>${страница.ОФОРМЛЕНИЕ}${СВОЁ_ОФОРМЛЕНИЕ}</style>

<div class="лист разбор">
  <a class="назад" href="/">← к дашборду</a>
  <h1>Сколько касаний до партии и почему на экранах столько слов</h1>
  <p>Вы написали: «вопрос про витрину я не понял». Вопрос был задан одной строчкой в списке — так спрашивать нельзя, и это моя вина, а не ваша невнимательность. Объясняю с начала: что происходит сейчас, из чего выбирать и что я советую.</p>
  ${какСейчас()}
  ${проТекст()}
  ${блокВариантов(выбор)}
  ${советШтаба()}
  ${кнопкиВыбора(выбор)}
  <p class="сноска"><b>Откуда числа.</b> Шаги пути сверены с самой игрой: с разметкой экранов и с тем, какая кнопка какой экран открывает. Знаки посчитаны программой по этой же разметке — считался только видимый текст, без пояснений для нас и без рисунков. Ни одно число тут не поставлено на глаз.</p>
</div>
</html>`;
}

/** Страница «принято» после нажатия кнопки. */
function страницаПринято(вариант) {
  const найден = ВАРИАНТЫ.find(function (в) { return в.ключ === вариант; });
  return `<!doctype html>
<html lang="ru">
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Принято</title>
<style>${страница.ОФОРМЛЕНИЕ}${СВОЁ_ОФОРМЛЕНИЕ}</style>
<div class="лист разбор">
  <div class="принято"><p><b>Принято: вариант ${э(вариант)}</b>${найден ? ' — ' + э(найден.имя) : ''}.<br>Штаб увидит это сам, писать ничего не нужно.</p></div>
  <a class="назад" href="/путь-до-партии">← вернуться к разбору</a><br>
  <a class="назад" href="/">← к дашборду</a>
</div>
</html>`;
}

module.exports = {
  целаяСтраница: целаяСтраница,
  страницаПринято: страницаПринято,
  ВАРИАНТЫ: ВАРИАНТЫ,
  ЗАМЕР: ЗАМЕР
};
