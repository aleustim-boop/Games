# План: рейтинг дурака, партия 10 — остатки чек-листа приёмки макета

Источник: сверка docs/05_ACCEPTANCE_RU.md и qa/VALIDATION_RU.md макета (leaderboard_cloudcode_pack_v1) с кодом на диске, ревьюер-сверщик sonnet, 23.09 03:26. Пункт очереди — 25 (зонтик «рейтинг по макету»).

## Что сошлось (14 сценариев docs/05 — все ВЫПОЛНЕНО)
Каталог → «Все игры»; четыре игры → свой id; возврат «Дурак → рейтинг → Шахматы → Назад → Дурак» (откудаПришли в открыть(), обновить() его не трогает); Back/Esc закрывает шторку, не экран; «Все игры» из игрового рейтинга; пятая игра в реестре без правки раскладки; до первой партии итог не растёт; +20 без задвоения (учестьПартию, ключ партии); устойчивая сортировка очки → победы → свежесть → номер; настоящее место в закреплённой карточке; «Моё место» → соседи/подиум; друзья — настоящие связи; устаревший ответ не перезаписывает экран (номерЗапроса); состояния пусто/ошибка/новичок. Идентификаторы наружу не уходят вовсе (строже макета); исторические очки скрытой игры сохраняются.

## Не сошлось или не проверить чтением
1. **Длинное имя не раскрывается нигде** — обрезка есть (style.css ~7147–7153, ~11943–11950), но ни title, ни aria-label ни в строке (js/рейтинг-экран.js ~1211–1222), ни в карточке «Моё место». Игрок теряет хвост имени без возможности увидеть. → **10-А**.
2. **Выбранная игра в шторке без aria-selected/aria-current** — помечена классом и текстовым «✓» (~3024, ~3072–3076). Мелочь для читалки экрана. → **10-А** (тот же файл).
3. Клавиатура не перекрывает поиск — механика есть (~3221 не фокусирует поле на тач-устройстве), визуально не проверено → снимок при случае.
4. Ловушка Tab внутри шторки — отложено сознательно (план партии 8, п. 7).
5. Две колонки на больших экранах — сознательно не делаем (план партии 7, п. 4, решение владельца).
6. Нет горизонтальной прокрутки/обрезанных очков, чёткость SVG — только снимком (все-экраны-в-полном-экране.js 03:18 — 0 провалов; снимки css-Б2 → смотритель).

## Этапы
- **10-А (developer, js/рейтинг-экран.js + tests/рейтинг-партия-10.js)** — после коммита 9-Б (тот же файл): (1) полное имя доступно: title на узле имени в строке списка, в карточке «Моё место» и на подиуме, когда имя обрезано (или всегда — проще и честнее: title = полное имя); (2) активному пункту шторки выбора игры aria-selected="true" (role у списка — проверить, listbox/option или aria-current="true" на кнопке), у остальных — false/нет; проверка в песочнице vm: title совпадает с полным именем у строк, карточки и подиума; ровно один пункт с aria-selected="true", и он же с классом активного; ломающий запуск на копии. Вид из js не задавать.

## Журнал
- 03:26 — сверщик сдал отчёт; план записан. 10-А ждёт свободного js/рейтинг-экран.js (9-Б запущен 03:29).
