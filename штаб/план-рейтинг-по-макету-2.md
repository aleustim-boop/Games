# План «Рейтинг по макету», заход 2 — сходство с макетом владельца

Составлен 22.09.2026 17:30. Повод: владелец сравнил свой макет экрана рейтинга и наш живой
экран на ПК — «не похоже». Первый заход (`штаб/план-рейтинг-по-макету.md`, коммиты 4519fef
«золото» и c1d3b40 «подиум») дал цвет, но не устройство экрана.

Макет: `C:/Users/Admin/Downloads/leaderboard_cloudcode_pack_v1/leaderboard_cloudcode_pack_v1/`
(`prototype/app.js` — порядок узлов, `prototype/styles.css` — числа, `references/screens/` — картинки).

Главное решение: **разметку чинит только js.** `index.html` и страницы игр не правим —
статическую разметку рейтинга js уже достраивает (`оживитьКругИгроков`,
`убедитьсяЧтоЕстьВходНаДрузей`), тем же приёмом достроит и остальное. Иначе четыре страницы
разъедутся и появится лишний исполнитель.

## 1. Пункт макета → что у нас → что сделать

| # | В макете | У нас сейчас (файл:строки) | Что сделать | Файл |
|---|---|---|---|---|
| 1 | Сегмент «Все игроки \| Друзья», активная половина золотая, во всю колонку | Пара «Круг игроков» спрятана внутри `#рейтинг-фильтры` и снимается для всех игр, кроме дурака: `отметитьИгру` js:1967–1985 (`фильтры.classList.toggle('скрыт', игра !== 'дурак')`), второй замок `кругДляЗапроса` js:2013–2015; вместо сегмента — широкая серая кнопка `кнопкаМоиДрузья` js:1630–1632, вставка js:1648–1653 | Вынести пару круга в свой узел `#рейтинг-круг` (новая `вынестиКругИгроков()`: переносит существующий узел «Круг игроков» из `#рейтинг-фильтры`, ставит сразу под `#рейтинг-выбор-игры`); `отметитьИгру` его больше не скрывает; `кругДляЗапроса` возвращает `кругСейчас` для любой игры; «Мои друзья» — тихой мелкой ссылкой под сегментом (`.рейтинг-круг__друзья`) | А: `js/рейтинг-экран.js`; Б: `style.css` |
| 2 | «МОЁ МЕСТО» — последняя строка списка в золотой рамке (место, аватар, имя, «Вы», очки, ↑12) | Карточка `#рейтинг-моё-место` с кубком НАД лидерами: `нарисоватьОчки` js:1296–1340, `карточкаТекст`/`карточкаОчки` js:1348–1364; css 11444–11545 (кубок `::before`, `min-height:96px`) и 11557–11645. Нужная строка у нас уже есть: `мояСтрокаВнизу` js:448–489 и js:1324, вид `.рейтинг-строка--я` css 12161–12290 | При мере «очки» карточку сверху не заполнять, узлу ставить класс `скрыт` (честные фразы-беды «Вас в таблице пока нет» оставить); в `строкаТаблицы` js:1086–1115 заменить текст «(вы)» на отдельный `<span class="рейтинг-имя__вы">Вы</span>` | А: `js/рейтинг-экран.js`; Б: `style.css` |
| 3 | Имя и очки ВНУТРИ тёмной тумбы, аватар в кольце медали, кружок-номер на нижнем крае, корона над первым | `карточкаПодиума` js:1245–1289: порядок аватар → медаль → имя → очки → ПУСТОЙ `<span class="рейтинг-подиум-основание">`; css 11971–12157 (основание — плашка высотой 40/26/18px) | Основание сделать контейнером и переложить в него `.рейтинг-подиум-имя` и `.рейтинг-подиум-очки`; в карточку первого места добавить `<span class="рейтинг-подиум-корона">` | А: `js/рейтинг-экран.js`; Б: `style.css` |
| 4 | Шапка-карточка «‹ Общий рейтинг (i)», название 22px | Крупный заголовок слева `clamp(30px, 9.6vw, 42px)` css 11425–11433; кнопки «i» нет, вместо неё «Как считается» внизу экрана | Обернуть существующий `h2.заголовок` в `.рейтинг-шапка` (новая `убедитьсяЧтоЕстьШапка()`), справа кнопка `#кнопка-рейтинг-инфо` с тем же обработчиком, что у `#кнопка-рейтинг-как-считается`; саму «Как считается» оставить внизу (дверь не теряем). Стрелку даёт Telegram — не рисуем | А: `js/рейтинг-экран.js`; Б: `style.css` |
| 5 | Заголовок списка «МЕСТО / ИГРОК / ОЧКИ» | `шапкаТаблицы(столбцы)` js:1014–1022 есть, но зовётся только у меры «сила» (js:1210); в `нарисоватьОчки` её нет | В `нарисоватьОчки` после подиума и перед `вид.лидерыПодПодиумом` (js:1321) добавить `таблица.appendChild(шапкаТаблицы('очки'))`; текст первой ячейки «#» → «Место» | А: `js/рейтинг-экран.js`; Б: `style.css` |
| 6 | Нижняя полоса, активная «Рейтинг» золотая | На экране рейтинга полосы НЕТ вовсе: `<nav data-вкладки>` стоит только в витрине и лобби (index.html:135, 244); набора «рейтинг» в `НАБОРЫ` нет (js/нижние-вкладки.js:68–140); `заполнитьПолосу` (там же:210–225) не экспортируется — наружу отдано только `{вкладкиНабора, кнопкаHTML}` (строка 236) | В: добавить набор `рейтинг` (Игры — действие `к-играм`, Рейтинг — `активна:true`, Профиль — `мой-счёт`) и отдать наружу `заполнитьПолосу`. А: в `построитьРазметкуРейтингаИгроков` js:1683–1721 добавить последним узлом `<nav class="нижние-вкладки" data-вкладки="рейтинг">` и заполнять его ЛЕНИВО, в момент открытия экрана | В: `js/нижние-вкладки.js`; А: `js/рейтинг-экран.js`; Б: `style.css` |
| 7 | Телефонная колонка | Своя ширина литералом: `max-width: 520px` css:11419–11423 | См. раздел 3 | Б: `style.css` |

Фиолетовый фон — чужая беда (тема оформления), в этот план не входит.
«Очки за все игры» и выпадашка «Все игры» уже похожи — не трогаем.

## 2. Этапы (один файл — один исполнитель, идут параллельно)

Договор имён между А и Б (чтобы не ждать друг друга): `.рейтинг-шапка`,
`.рейтинг-шапка__название`, `#кнопка-рейтинг-инфо`, `#рейтинг-круг`,
`.рейтинг-круг__друзья`, `.рейтинг-подиум-основание` (теперь контейнер),
`.рейтинг-подиум-корона`, `.рейтинг-имя__вы`, `.рейтинг-строка--шапка` (уже есть),
`.нижние-вкладки[data-вкладки="рейтинг"]`. Эти имена — закон для обоих этапов.

**Этап А — `js/рейтинг-экран.js`** (разработчик; разметка, данные, классы; ни цветов, ни размеров).
1. `вынестиКругИгроков()` плюс правки `отметитьИгру` (1967–1985) и `кругДляЗапроса` (2013–2015) — пункт 1.
2. Кнопку «Мои друзья» (1630–1653) переставить под сегмент, класс `кнопка кнопка--мелкая рейтинг-круг__друзья`.
3. В `нарисоватьОчки` (1296–1340): не заполнять `строкаСверху`, ставить ей `скрыт`; после подиума звать `шапкаТаблицы('очки')` — пункты 2 и 5.
4. `карточкаПодиума` (1245–1289): имя и очки внутрь основания, корона первому — пункт 3.
5. `строкаТаблицы` (1086–1115): «Вы» отдельным `span` — пункт 2.
6. `убедитьсяЧтоЕстьШапка()` и `#кнопка-рейтинг-инфо` — пункт 4.
7. `<nav class="нижние-вкладки" data-вкладки="рейтинг">` в конец экрана и ленивый вызов
   `window.НижниеВкладки.заполнитьПолосу(nav)` при открытии (на шахматы.html:594/608 и
   нарды.html:608/611 наш файл подключён РАНЬШЕ вкладок — при загрузке двери ещё нет) — пункт 6.
Объём: ~130–160 строк правок.

**Этап Б — `style.css`** (оформитель; только вид, разметку не трогает). Числа берём из
`prototype/styles.css` макета:
1. Сегмент (11792–11840): полоса `padding:3px; gap:3px; border:1px solid #404040; border-radius:14px; background:#111113`, активная половина — градиент `#f1db9c → #c5a35d`, текст `#17140e`.
2. `#рейтинг-моё-место` в режиме очков — без рамки и кубка (11444–11545); своей строке — золотая рамка, скругление и надпись «МОЁ МЕСТО» (12161–12290, уже есть); по желанию `position:sticky; bottom:0` внутри `#рейтинг-таблица` — это макетный `my-dock`.
3. Подиум (11971–12157): основание — тумба `padding:19px 4px 10px`, градиент `#171716 → #080809`, рамка `var(--рейтинг-золото-край)`, радиус `30px 30px 0 0`, без нижней границы; аватар 73px (первый 86px) в кольце медали; кружок-номер 35px на нижнем крае; корона 38×33 маской-svg над первым.
4. Шапка (11425–11433): карточка `min-height:64px`, название 22px/700 (никакого `clamp` до 42px), «i» — золотая круглая кнопка справа.
5. Заголовок списка: фон `#151517`, радиус `8px 8px 0 0`, 10px, `letter-spacing:.5px`, `text-transform:uppercase`.
6. Полоса вкладок на экране рейтинга: прижата к низу, активная — `var(--рейтинг-золото)`.
7. Ширина — раздел 3.
Золото не выдумывать: одно число `--рейтинг-золото-общее` на `#приложение` (327–335).
Объём: ~180–220 строк правок.

**Этап В — `js/нижние-вкладки.js`** (разработчик, маленький, делать первым):
набор `рейтинг` в `НАБОРЫ` (после 140) и `заполнитьПолосу` в экспорт (236). ~15 строк.

**Этап Г — `tests/`** (тестировщик, тем же коммитом) — см. раздел 4.

## 3. Ширина колонки на ПК

Что в файлах: экран рейтинга **не пользуется общими переменными сборника**. У его прямых детей
своя ширина литералом — `style.css:11419–11423`:
`:is(#экран-рейтинга-игроков, #экран-рейтинга) > :not(:is(#кнопка-рейтинг-игроков-назад, #кнопка-рейтинг-назад)) { width:100%; max-width:520px }`,
дети центрированы (`align-items:center`, 11367). В широком окне
(`@media (min-width:700px) and (min-height:600px)`, 12723–12801) заданы `--колонка-широкая: 520px`
и `--колонка-витрина-широкая: 760px`, но экрана рейтинга в этом блоке нет; телефонная
`--колонка: 360px` (620–690) до него тоже не достаёт.

Честно: по коду колонка НЕ шире 520 точек — правила, поднимающего её до ~700, в `style.css` нет.
Владелец видел иное, а живьём проверить нечем (браузер занят). Поэтому:
- Правка (этап Б): литерал заменить на переменную сборника —
  `max-width: min(100%, var(--колонка-широкая, 520px))`, а внутри широкого медиазапроса
  для `#экран-рейтинга-игроков` задать телефонную ширину макета (≈440px), чтобы на ПК
  колонка читалась как телефонная.
- Две колонки макета на ПК (`.leader-layout`, `max-width:1248px`) **не копируем** — закон
  сборника «все игры выглядят одинаково».
- Замер обязателен на приёмке: снимок 1280×800 плюс `offsetWidth` у `#экран-рейтинга-игроков`
  и `#рейтинг-таблица` в отчёт. Если больше 520 — правило перебито вне `style.css`, искать
  на конкретной странице и доложить штабу.

## 4. Как принимать

Снимки Playwright (тестировщик, один браузер за раз), экран рейтинга:
- 390×844: «Все игры» (мера «очки») — главный снимок; дурак с фильтрами; круг «Друзья» при пустом списке друзей.
- 1280×800: «Все игры» плюс замер ширины (выше).
Сравнивать со `references/screens/01_global.png` (общий рейтинг) и `03_durak.png` (дурак),
крупные детали — с `references/component_crops/`. Смотритель отвечает «да/нет» по каждому
из семи пунктов раздела 1 — штаб картинки не открывает.

Обновить тем же коммитом:
- `tests/снимок-рейтинг-золото.js` — **обязательно**: выборки строк (476, 509, 850) берут
  `.рейтинг-строка` и смотрят `[0]`, а новая шапка списка несёт базовый класс
  `рейтинг-строка` (js:1016) → добавить `:not(.рейтинг-строка--шапка)`, иначе покраснеет зря.
  Плюс новые приёмы: карточки сверху при мере «очки» нет, имя и очки — внутри основания.
- `tests/подиум-2-1-3.js` (235–255) — имя и очки теперь потомки основания (`querySelector`
  это переживёт), добавить проверку вложенности и короны у первого места.
- `tests/проверка-нижних-вкладок.js` — набор `рейтинг`, активная вкладка «Рейтинг».
- `tests/браузер-рейтинг-круг-друзей.js`, `tests/рейтинг-состав.js`,
  `tests/браузер-рейтинг-состав.js` — сегмент круга теперь виден на всех играх, не только у дурака.
- `tests/рейтинг-экран-четыре-игры.js`, `tests/карта-входов-в-рейтинг.js` — переехавшая
  «Мои друзья» и новая дверь `#кнопка-рейтинг-инфо`.
- `tests/все-экраны-в-полном-экране.js`, `tests/кнопки-назад-везде.js` — на экране появился `nav`.
- `tests/версии-не-разъехались.js` — `?v=` у `js/рейтинг-экран.js` и `js/нижние-вкладки.js`
  поднимает штаб СРАЗУ на всех страницах: index.html, шашки, шахматы, нарды.
Каждая новая проверка — с ломающим запуском по КОПИИ файла во временной папке.

## 5. Риски и что не трогать

- **Сервер не трогаем совсем.** Срез друзей по всем играм уже приходит
  (`server/сервер.js` ~2700–2765, `рейтинг.срезПоВсемИграмПоСписку`, поле
  `друзья:{естьДрузья, всего, яВТаблице, я, верхушка, вокругМеня}`). Договор `видДляИгрока`
  (`server/партия-по-сети.js`) — вне плана.
- **`index.html` и страницы игр не правим** (кроме `?v=`, их поднимает штаб при приёме):
  разметку рейтинга достраивает js, иначе четыре страницы разъедутся.
- Общие классы `.кнопка`, `.переключатель`, `.таблица` глобально не переопределять — только
  внутри `#экран-рейтинга-игроков`, иначе поедут другие игры.
- Круг друзей для всех игр: если друзей нет, уже есть ветка «друзья-нет» (js:711–727) с
  кнопкой «Добавить друга» — проверить, что она не ломает подиум
  (`tests/подиум-2-1-3.js`:295 ждёт отсутствие подиума при беде).
- Полоса вкладок внизу отъедает высоту — убедиться, что своя строка списка видна на 390×844.
- Порядок скриптов на шахматы.html и нарды.html: рейтинг-экран.js грузится раньше вкладок —
  полосу заполнять лениво, с проверкой `window.НижниеВкладки`.
- Фиолетовый фон страницы — чужая беда (тема оформления), в этом плане её не чинят.
