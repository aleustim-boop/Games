# План: «Онлайн» в дашборде (просьба владельца 23.09 11:34)

Кто сейчас в игре, кто заходил за сегодня / 7 дней, сколько раз, суммарное время, во что играл. Отдельная кнопка в дашборде. Утверждено штабом 23.09 11:37.

## Главное
- Пульс браузера: POST /пульс {initData, игра} при открытии и раз в 60 с, пока вкладка видна; скрыта — тишина. Без подписи Telegram — не учитывается.
- Учёт — новый server/посещения.js: номер только из ктоПрислал; игра из набора {дурак, шашки, нарды, шахматы}; имя из подписи ≤ 40 знаков; пауза > 3 мин — новая сессия {номер, имя, начало, конец, минуты по играм}; пульс чаще 30 с от номера — отбрасывается; посещения.json в папке данных, отложенная запись через «.пишем» + переименование (как статистика.js); --без-памяти — без диска; 30 дней, ≤ 200 сессий на номер в день.
- ЗАЩИТА (дашборд открыт наружу туннелем без пароля!): страница дашборда /онлайн — 403 при заголовках cf-connecting-ip, cf-ray, x-forwarded-for, x-real-ip. Маршрут сервера GET /штаб-онлайн — только с заголовком x-ключ-штаба (файл server/данные/ключ-штаба, 32 случайных байта, создаётся сервером при запуске, вне git; сравнение crypto.timingSafeEqual) И без заголовков туннеля; в server/nginx/igra.conf — location = /штаб-онлайн { return 404; }. bot/.env не трогаем.
- Номера Telegram — только в /штаб-онлайн и на локальной /онлайн. /статистика остаётся без номеров. В журнал сервера номера не пишем.
- Не из Telegram — не считаются (на экране так и написать). Точность — минута.

## Этап 1 — модуль (developer)
Файлы: server/посещения.js (новый), tests/посещения.js (новый).
учесть(номер, имя, игра, сейчас), сводка(сейчас) → {сейчас[], сегодня[], неделя[]}, дописать(); подставные часы. Проверка: 3 пульса подряд → одна сессия 2 мин; пауза 4 мин → две; > 30 дней уходят; чужая игра отброшена; частый пульс отброшен; запись на диск во временную папку. Ломающий: копия, окно сессии 3 мин → 0 — краснеет.

## Этап 2 — сервер (developer, полная модель; ревьюер обязателен)
Файлы: server/сервер.js (+ server/nginx/igra.conf).
ВНИМАНИЕ: сервер.js нужен и этапу 2 плана фото — делать строго по очереди, не параллельно.
case '/пульс' (≈818), GET /штаб-онлайн рядом с /статистика (≈793), ключ при запуске. Проверка на 8799 с ДАННЫЕ_ИГРЫ во временной папке и оснасткаДляПроверки (как tests/проверка-статистики.js): подписанный пульс → в сводке; без ключа → 403; ключ + cf-connecting-ip → 403; без подписи → не учтён. Ломающий: копия без проверки ключа → краснеет. Перезапуск 8790 — только штаб.

## Этап 3 — пульс в браузере (developer)
Файлы: js/пульс.js (новый); подключение на 4 страницах и ИграСтраницы для шахматы.html и index.html — штаб при приёме (?v=).
Проверка на node с поддельными window/document/fetch: видима — 1 запрос в минуту; скрыта — 0; без initData — 0. Ломающий: без остановки при скрытии — краснеет.

## Этап 4 — страница дашборда (developer)
Файлы: штаб/онлайн-страница.js (новый), штаб/дашборд.js (маршрут GET /онлайн и кнопка «Онлайн», как важное-страница.js / кнопкаЗаданий ≈837; порт сервера комнат — переменная).
Экран: «Сейчас онлайн: N» (имя, игра, минут), вкладки «Сегодня / 7 дней» (имя, заходов, время, игры). Сервер молчит — так и написать. Проверка: поддельный сервер → /онлайн 200 с именем; с cf-connecting-ip → 403. Ломающий: копия без проверки заголовков → краснеет. Перезапуск дашборда — штаб/перезапуск-дашборда.js.

## Риски
Нагрузка ничтожна (1/мин на человека); рост журнала ограничен; сессии в памяти теряются при перезапуске 8790 (на диске — что успело); при переезде на ВМ дашборд до её 127.0.0.1 не дотянется — вопрос на потом.

## Журнал
- 23.09 11:37 — опись и план (Plan), утверждено. Этап 1 запущен.
- 23.09 12:03 — заголовок ключа x-klyuch-shtaba (кириллица в имени HTTP-заголовка недопустима). Проверка сервера — запускалка во временной папке с оснасткаДляПроверки. Этап 1 закоммичен 31a62cf; этапы 2 и 4 в работе.
