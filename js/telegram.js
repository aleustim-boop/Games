/* Связь игры с Telegram и хранение личных рекордов.

   Игра общается с этим файлом только тремя функциями:
     isReady()                — готово ли хранилище рекордов (обычное да/нет, сразу)
     saveScore(очки, сложность, игра) — записать результат, возвращает обещание (Promise)
     loadScore(игра)          — прочитать рекорды, возвращает обещание (Promise)

   Эти три — договор, они не меняются никогда: игра написана под них.
   Довод «игра» появился позже и НЕОБЯЗАТЕЛЕН: не назвали игру — это «дурак»,
   и старые вызовы saveScore(очки, сложность) и loadScore() работают как прежде.
   У шашек таблица рекордов своя: они зовут те же функции со словом «шашки».

   Рядом с ними, ОТДЕЛЬНОЙ дверью, живёт личный рейтинг игрока — счёт партий,
   побед и серий по разрезам (уровень соперника, стол, живые люди или боты):
     window.Рейтинг.готов()             — есть ли куда писать
     window.Рейтинг.записатьПартию(...) — записать законченную партию
     window.Рейтинг.прочитать(игра)     — готовая сводка для показа
     window.Рейтинг.очистить(игра)      — начать счёт заново
   Подробности — в разделе «Личный рейтинг игрока» ниже. Рейтинг лежит своей
   записью и своим ключом, поэтому таблице рекордов он ничем не мешает.

   Той же дверью живёт серия дней подряд — сколько дней кряду человек доигрывал
   хотя бы одну партию (раздел «Серия дней подряд» ниже):
     window.Рейтинг.отметитьДень()      — отметить сегодняшний день, вернёт сводку
     window.Рейтинг.серияДней()         — та же сводка, но ничего не отмечает
   Партия, записанная через «записатьПартию», отмечает день сама и приносит
   его сводку полем «серияДней» — игре звать «отметитьДень» отдельно не нужно.

   ИМЕНА КЛЮЧЕЙ — только латиницей, и это не вкусовщина. Облако Telegram
   отвергает ключ с русскими буквами, и из-за прежнего ключа рекорды не
   переезжали между устройствами вообще никогда. Все имена собираются в одном
   месте — раздел «Имена ключей»; там же объяснено, как старая запись игрока
   переживает переезд на новый ключ.

   Третья дверь — window.Телеграм — про сам экран: полный экран без шапки
   Telegram (Bot API 8.0+) просится при старте сам, а отступы под выемку,
   часы и кнопки Telegram уходят оформителю переменными CSS «--безопасно-…».
   Там же — замок вертикали, чтобы стол не ложился набок от наклона телефона.
   Подробности — в разделах «Полный экран и безопасные отступы» и «Замок поворота».

   Той же дверью живёт отдача под пальцем — короткая вибрация Telegram:
     window.Телеграм.отклик('карта')        — тряхнуть телефон под палец
     window.Телеграм.вибрацияВключена()     — включена ли она сейчас
     window.Телеграм.включитьВибрацию(да/нет) — выключатель, выбор запоминается
   Виды отклика ('выбор', 'карта', 'взятка', 'победа', 'проигрыш', 'нельзя'),
   паузы против дребезга и поведение вне Telegram — в разделе
   «Отдача под пальцем (вибрация)».

   Четвёртая дверь — window.Оформление — про внешний вид, который выбирает
   сам игрок: рубашка карт, доска и фон.
     window.Оформление.поставить('фон', 'дыхание') — выбрать и запомнить
     window.Оформление.какое('фон')                — что стоит сейчас
     window.Оформление.всё()                       — все три разом
     window.Оформление.загрузить()                 — подтянуть выбор из облака
   Выбранное слово ложится признаком на <html> (data-фон, data-рубашка,
   data-доска), остальное делают стили. Там же — признак data-страница="скрыта"
   для свёрнутой игры, чтобы живой фон не грел телефон в кармане.
   Подробности — в разделе «Оформление: рубашка карт, доска шашек, фон».

   Пятая дверь — window.ОбщийФон — готовый список фонов для экрана выбора:
     window.ОбщийФон.фоны()           — [{ ключ, имя, образец() }, …], «Сукно» первым
     window.ОбщийФон.выбрать('туман') — выбрать фон
     window.ОбщийФон.сейчас()         — какой фон стоит сейчас
   Своего хранения у неё нет: «выбрать» зовёт ту же дверь Оформления.
   Подробности — в разделе «Пять фонов на выбор».

   Всё остальное — раскрытие окна, цвета темы Telegram и выбор
   «облако или память браузера» — делается здесь и наружу не торчит.
   Вне Telegram файл работает точно так же: рекорды просто ложатся в память
   браузера (localStorage), и ни одной ошибки в консоли при этом нет. */

(function () {
  'use strict';

  /* Ключ, под которым лежат рекорды и в облаке Telegram, и в памяти браузера,
     у каждой игры свой и собирается по имени игры — «ключРекордов(игра)» в разделе
     «Имена ключей». Собирается из латинских букв — почему именно так, подробно
     написано там же. Коротко: с прежним ключом облако отказывало, и рекорды
     не переезжали между устройствами вовсе.

     СТАРЫЙ_КЛЮЧ_РЕКОРДОВ — та самая прежняя запись, и она только ДУРАКОВА:
     других игр тогда не было. У живых игроков она лежит в памяти телефона
     прямо сейчас, и там весь их прошлый труд. Мы её читаем всегда и никогда
     не стираем (см. «местныеРекорды»). */
  var СТАРЫЙ_КЛЮЧ_РЕКОРДОВ = 'подкидной-дурак:рекорды';
  var МАКСИМУМ_ЗАПИСЕЙ = 10;                 // сколько строк держим в таблице рекордов
  var СЛОЖНОСТЬ_ПО_УМОЛЧАНИЮ = 'обычный';    // если игра не передала сложность
  var ДЛИНА_НОМЕРА_СЕРИИ = 40;               // длиннее номер быть не может — значит, это мусор
  // Сколько ждём ответа от облака Telegram. Не дождались — берём память браузера,
  // чтобы экран результата не завис в ожидании навсегда. Больше двух с половиной
  // секунд ждать нельзя: при сохранении подряд идут чтение и запись, а человек
  // всё это время смотрит на надпись «записываем» и решает, что игра сломалась.
  var ОЖИДАНИЕ_ОБЛАКА = 2500;
  // Облако не ответило — значит, связи сейчас нет. Полминуты к нему не ходим,
  // иначе каждая победа снова упирается в ожидание.
  var ПЕРЕРЫВ_ПОСЛЕ_ОСЕЧКИ = 30000;
  var облакоОтдыхаетДо = 0;

  // ---------------------------------------------------------------------------
  // Где мы находимся: есть ли Telegram и его облако
  // ---------------------------------------------------------------------------

  /** Объект Telegram WebApp, если игра открыта внутри Telegram. Иначе null. */
  function получитьWebApp() {
    try {
      if (window.Telegram && window.Telegram.WebApp) return window.Telegram.WebApp;
    } catch (ошибка) { /* нет так нет */ }
    return null;
  }

  // Облако появилось в Telegram начиная с этой версии
  var ВЕРСИЯ_С_ОБЛАКОМ = '6.9';

  /** Облачное хранилище Telegram, если оно доступно в этой версии приложения. Иначе null.
      Версию спрашиваем заранее: в обычном браузере скрипт Telegram делает вид, что облако
      есть, но на любое обращение ругается в консоль. Нам нужна чистая консоль. */
  function получитьОблако() {
    // Недавняя осечка: связи нет, лишний раз не ждём
    if (Date.now() < облакоОтдыхаетДо) return null;

    var приложение = получитьWebApp();
    if (!приложение || !приложение.CloudStorage) return null;

    try {
      if (typeof приложение.isVersionAtLeast === 'function' && !приложение.isVersionAtLeast(ВЕРСИЯ_С_ОБЛАКОМ)) {
        return null;
      }
    } catch (ошибка) {
      return null;
    }

    var облако = приложение.CloudStorage;
    if (typeof облако.getItem !== 'function' || typeof облако.setItem !== 'function') return null;
    return облако;
  }

  /** Проверяем память браузера один раз при загрузке: в некоторых режимах она запрещена. */
  function проверитьПамять() {
    try {
      var проба = '__проба-записи__';
      window.localStorage.setItem(проба, '1');
      window.localStorage.removeItem(проба);
      return true;
    } catch (ошибка) {
      return false;
    }
  }

  var памятьРаботает = проверитьПамять();

  // ---------------------------------------------------------------------------
  // Имена ключей: под чем что лежит
  // ---------------------------------------------------------------------------

  /* ГЛАВНОЕ ПРАВИЛО, ИЗ-ЗА КОТОРОГО ЗДЕСЬ ВООБЩЕ ЕСТЬ ЭТОТ РАЗДЕЛ.
     Облако Telegram принимает в имени ключа ТОЛЬКО латинские буквы, цифры,
     «_» и «-», длиной от 1 до 128 знаков. Ключ с русскими буквами или
     двоеточием оно отвергает — и делает это молча для игрока: отказ приходит
     в ответ, наш код гасит его и уходит в память телефона.

     Так и вышло: рекорды лежали под ключом «подкидной-дурак:рекорды» и в облако
     не попадали НИКОГДА. Человек играл на телефоне, открывал игру на планшете
     и видел пустую таблицу — при том, что облако мы брали ровно ради этого.
     Проверить это можно поддельным облаком, которое живёт по правилу Telegram:
     до починки в него не ложится ни одной записи (см. отчёт по задаче).

     Поэтому имена ключей собираются здесь и только здесь, латиницей, а перед
     походом в облако ключ ещё и проверяется (см. «ключГодитсяДляОблака»). */

  var ИГРА_ПО_УМОЛЧАНИЮ = 'дурак';
  var ДЛИНА_ИМЕНИ_ИГРЫ = 24;

  /* Как игра зовётся в имени ключа. Новая игра — допишите сюда строку:
     тогда ключ будет читаемым («records_shashki»).

     Игре, которой здесь нет, ключ соберётся из числа — и в консоли один раз
     появится предупреждение разработчику. Почему именно так, а не откат
     на дурака: чужие победы легли бы в дуракову таблицу, и игрок увидел бы
     там серию, которой у него в дураке не было. Это враньё в рекордах, оно
     хуже неудобного имени ключа. А ключ из числа — латинский, в облако
     проходит, и записи двух разных игр никогда не смешаются. */
  var ЛАТИНСКОЕ_ИМЯ_ИГРЫ = {
    'дурак': 'durak',
    'шашки': 'shashki',
    'шахматы': 'shahmaty',
    'нарды': 'nardy'      // игры ещё нет, имя вписано заранее — см. заметку ниже
  };

  /* ЗАЧЕМ ИМЯ ВПИСАНО ДО ТОГО, КАК ИГРА ПОЯВИЛАСЬ. Забыть эту строку легко —
     забывали два раза из двух. А цена забывчивости не «ключ некрасивый»:
     пока игры нет в списке, её записи ложатся под ключ из числа, и в день,
     когда имя допишут, ключ сменится. Рекорды при этом не сломаются —
     они потеряются молча: старые останутся лежать под числом, новые начнут
     копиться под именем, и человек увидит пустую таблицу вместо своих побед.
     Вернуть их будет неоткуда. Поэтому имя дешевле вписать заранее, чем
     разбираться потом, и поэтому же забытую игру ловит отдельная проверка —
     tests/имена-игр-в-ключах.js. */

  var ПРАВИЛО_КЛЮЧА_ОБЛАКА = /^[A-Za-z0-9_-]{1,128}$/;

  var ужеПредупреждали = {};   // о каждой беде говорим один раз, а не на каждую партию

  /** Предупреждение разработчику в консоль — один раз на метку.
      У игрока консоли нет, ему это не мешает; разработчику хватит и одного раза. */
  function предупредитьОдинРаз(метка, текст) {
    if (ужеПредупреждали[метка]) return;
    ужеПредупреждали[метка] = true;
    try {
      console.warn(текст);
    } catch (ошибка) { /* консоли может не быть — не беда */ }
  }

  /** Название игры: непустая короткая строка, иначе «дурак». */
  function привестиИгру(значение) {
    if (typeof значение !== 'string') return ИГРА_ПО_УМОЛЧАНИЮ;
    var имя = значение.trim();
    if (!имя || имя.length > ДЛИНА_ИМЕНИ_ИГРЫ) return ИГРА_ПО_УМОЛЧАНИЮ;
    return имя;
  }

  /** Имя ключа: вид записи и игра. Например «records_durak», «rating_shashki». */
  function ключХранения(вид, игра) {
    if (Object.prototype.hasOwnProperty.call(ЛАТИНСКОЕ_ИМЯ_ИГРЫ, игра)) {
      return вид + '_' + ЛАТИНСКОЕ_ИМЯ_ИГРЫ[игра];
    }
    /* Текст нарочно резкий и с последствием, а не вежливое «допишите игру
       в список». Прежнюю мягкую строку дважды проглядели — на шашках
       и на шахматах, — и дело было не в цвете строки, а в том, что беда
       выглядела косметической. Она не косметическая: см. заметку у списка
       ЛАТИНСКОЕ_ИМЯ_ИГРЫ. Игроку эта строка не мешает: консоли у него нет.
       А чтобы забывчивость вообще не доходила до игроков, забытую игру
       ловит проверка tests/имена-игр-в-ключах.js — на неё надежды больше,
       чем на любую строку в консоли. */
    предупредитьОдинРаз('игра:' + игра,
      'ХРАНИЛИЩЕ: игра «' + игра + '» не вписана в список ЛАТИНСКОЕ_ИМЯ_ИГРЫ ' +
      '(js/telegram.js). Её рекорды лягут под ключ из числа. Впишите игру ' +
      'в список СЕЙЧАС: если сделать это позже, ключ сменится и все ' +
      'накопленные рекорды этой игры пропадут для человека навсегда.');
    return вид + '_' + числоИзНазвания(игра);
  }

  function ключРекордов(игра) { return ключХранения('records', игра); }
  function ключРейтинга(игра) { return ключХранения('rating', игра); }

  /* Серия дней подряд — одна на весь сборник, поэтому имени игры в ключе нет
     (почему одна — в разделе «Серия дней подряд»). */
  var КЛЮЧ_СЕРИИ_ДНЕЙ = 'days_streak';

  /** Короткое число по названию игры. Нужно ровно затем, чтобы две игры,
      которых нет в списке имён, не улеглись в одну запись. */
  function числоИзНазвания(имя) {
    var число = 0;
    for (var i = 0; i < имя.length; i++) {
      число = (число * 31 + имя.charCodeAt(i)) % 100000000;
    }
    return число.toString(36);
  }

  /** Годится ли имя ключа для облака Telegram.
      Это сторож на будущее: негодный ключ в облако не понесут вовсе — вместо
      бесполезного отказа (который ещё и глушит облако на полминуты, см.
      ПЕРЕРЫВ_ПОСЛЕ_ОСЕЧКИ) в консоли появится понятное предупреждение
      разработчику. У игрока такого предупреждения не будет никогда: все наши
      ключи собираются выше и правилу отвечают. */
  function ключГодитсяДляОблака(ключ) {
    if (ПРАВИЛО_КЛЮЧА_ОБЛАКА.test(ключ)) return true;
    предупредитьОдинРаз('ключ:' + ключ,
      'Хранилище: ключ «' + ключ + '» не годится для облака Telegram ' +
      '(там можно только латинские буквы, цифры, «_» и «-»). ' +
      'Записи под этим ключом останутся только в памяти этого устройства.');
    return false;
  }

  // ---------------------------------------------------------------------------
  // Разбор и приведение данных в порядок
  // ---------------------------------------------------------------------------

  /** Пустой ответ: так выглядят рекорды у игрока, который ещё ничего не выиграл.
      «последняяСерия» — заметка о том, какую строку таблицы мы растили в прошлый раз;
      она лежит рядом с рекордами и потому переживает перезагрузку страницы. */
  function пустыеРекорды() {
    return { лучший: 0, таблица: [], последняяСерия: null };
  }

  /** Любое значение превращаем в неотрицательное целое число очков. */
  function целоеЧисло(значение) {
    var число = Number(значение);
    if (!isFinite(число) || число <= 0) return 0;
    return Math.floor(число);
  }

  /** Любая дата в привычном виде 18.08.2026. */
  function датаСловами(дата) {
    var день = дата.getDate();
    var месяц = дата.getMonth() + 1;
    return (день < 10 ? '0' : '') + день + '.' + (месяц < 10 ? '0' : '') + месяц + '.' + дата.getFullYear();
  }

  /** Сегодняшняя дата в привычном виде 18.08.2026. Дату проставляем сами — игра её не передаёт. */
  function сегодняшняяДата() {
    return датаСловами(new Date());
  }

  /** Строка сложности: пустую или не-строку заменяем на «обычный». */
  function привестиСложность(значение) {
    if (typeof значение === 'string' && значение.trim()) return значение.trim();
    return СЛОЖНОСТЬ_ПО_УМОЛЧАНИЮ;
  }

  /** Номер серии из хранилища: годится только непустая строка, всё остальное — «номера нет».
      Длину ограничиваем, чтобы испорченный файл не раздул таблицу. */
  function привестиНомерСерии(значение) {
    if (typeof значение !== 'string') return '';
    var номер = значение.trim();
    if (!номер || номер.length > ДЛИНА_НОМЕРА_СЕРИИ) return '';
    return номер;
  }

  /**
   * Разбираем заметку о последней записанной серии.
   * У игроков, которые играли до появления этой заметки, в хранилище её просто нет —
   * тогда возвращаем null, и всё остальное читается как раньше. Мусор тоже даёт null.
   */
  function разобратьПамятьСерии(значение) {
    if (!значение || typeof значение !== 'object') return null;
    var очки = целоеЧисло(значение.очки);
    if (очки <= 0) return null;

    var заметка = {
      очки: очки,
      дата: typeof значение.дата === 'string' ? значение.дата : '',
      сложность: привестиСложность(значение.сложность),
      номерСерии: привестиНомерСерии(значение.номерСерии)
    };
    var прежние = разобратьПрежниеПриметы(значение.прежниеПриметы);
    if (прежние) заметка.прежниеПриметы = прежние;
    return заметка;
  }

  /**
   * Приметы, по которым строка этой же серии выглядела ДО того, как получила номер.
   * Нужны ровно на один случай: игрок обновил игру посреди серии. В другом хранилище
   * может лежать её прежний вид — строка без номера, и узнать её больше нечем.
   */
  function разобратьПрежниеПриметы(значение) {
    if (!значение || typeof значение !== 'object') return null;
    var очки = целоеЧисло(значение.очки);
    if (очки <= 0) return null;
    return {
      очки: очки,
      дата: typeof значение.дата === 'string' ? значение.дата : '',
      сложность: привестиСложность(значение.сложность)
    };
  }

  /** Превращаем сохранённый текст в объект рекордов. Мусор и поломанный текст не роняют игру. */
  function разобратьРекорды(текст) {
    if (!текст || typeof текст !== 'string') return пустыеРекорды();

    var данные;
    try {
      данные = JSON.parse(текст);
    } catch (ошибка) {
      return пустыеРекорды();
    }
    if (!данные || typeof данные !== 'object') return пустыеРекорды();

    var исходныйСписок = Array.isArray(данные.таблица) ? данные.таблица : [];
    var таблица = [];
    for (var i = 0; i < исходныйСписок.length && таблица.length < МАКСИМУМ_ЗАПИСЕЙ; i++) {
      var запись = исходныйСписок[i];
      if (!запись || typeof запись !== 'object') continue;
      var строка = {
        очки: целоеЧисло(запись.очки),
        дата: typeof запись.дата === 'string' ? запись.дата : '',
        сложность: привестиСложность(запись.сложность)
      };
      // Номер серии дописываем, только если он в записи есть. У строк, записанных
      // старой версией игры, его нет — и пусть не появляется: пустое поле в таблице
      // ни о чём не говорит, а место в хранилище занимает.
      var номер = привестиНомерСерии(запись.номерСерии);
      if (номер) строка.номерСерии = номер;
      таблица.push(строка);
    }

    var лучший = целоеЧисло(данные.лучший);
    for (var j = 0; j < таблица.length; j++) {
      if (таблица[j].очки > лучший) лучший = таблица[j].очки;
    }

    return {
      лучший: лучший,
      таблица: таблица,
      последняяСерия: разобратьПамятьСерии(данные.последняяСерия)
    };
  }

  // ---------------------------------------------------------------------------
  // Память браузера (запасной вариант)
  // ---------------------------------------------------------------------------

  /* Ключ здесь довод, а не постоянная: рядом с рекордами в той же памяти
     лежит ещё и личный рейтинг, и хранится он отдельной записью. */

  function прочитатьИзПамяти(ключ) {
    if (!памятьРаботает) return '';
    try {
      return window.localStorage.getItem(ключ) || '';
    } catch (ошибка) {
      return '';
    }
  }

  function записатьВПамять(ключ, текст) {
    if (!памятьРаботает) return false;
    try {
      window.localStorage.setItem(ключ, текст);
      return true;
    } catch (ошибка) {
      return false;
    }
  }

  // ---------------------------------------------------------------------------
  // Облако Telegram (основной вариант)
  // ---------------------------------------------------------------------------

  /** Обёртка над облаком: любой сбой или молчание превращаются в отказ обещания. */
  function спроситьОблако(действие) {
    return new Promise(function (успех, отказ) {
      var облако = получитьОблако();
      if (!облако) {
        отказ(new Error('облака Telegram нет'));
        return;
      }

      var ответУжеБыл = false;
      var часы = setTimeout(function () {
        if (ответУжеБыл) return;
        ответУжеБыл = true;
        облакоОтдыхаетДо = Date.now() + ПЕРЕРЫВ_ПОСЛЕ_ОСЕЧКИ;
        отказ(new Error('облако не ответило вовремя'));
      }, ОЖИДАНИЕ_ОБЛАКА);

      function ответить(сбой, значение) {
        if (ответУжеБыл) return;
        ответУжеБыл = true;
        clearTimeout(часы);
        if (сбой) {
          облакоОтдыхаетДо = Date.now() + ПЕРЕРЫВ_ПОСЛЕ_ОСЕЧКИ;
          отказ(new Error(String(сбой)));
        } else {
          успех(значение);
        }
      }

      try {
        действие(облако, ответить);
      } catch (ошибка) {
        if (!ответУжеБыл) {
          ответУжеБыл = true;
          clearTimeout(часы);
          отказ(ошибка);
        }
      }
    });
  }

  /* Ключ проверяем ДО похода в облако. Иначе получается худшее из возможного:
     облако отвечает отказом (имя ключа ему не годится), мы считаем это обрывом
     связи и на полминуты перестаём ходить туда вовсе — вместе с теми ключами,
     которые были в полном порядке. Именно так одна кириллическая строка глушила
     облако целиком. */

  function прочитатьИзОблака(ключ) {
    if (!ключГодитсяДляОблака(ключ)) return Promise.reject(new Error('ключ не для облака'));
    return спроситьОблако(function (облако, ответить) {
      облако.getItem(ключ, ответить);
    }).then(function (значение) {
      return typeof значение === 'string' ? значение : '';
    });
  }

  function записатьВОблако(ключ, текст) {
    if (!ключГодитсяДляОблака(ключ)) return Promise.reject(new Error('ключ не для облака'));
    return спроситьОблако(function (облако, ответить) {
      облако.setItem(ключ, текст, ответить);
    });
  }


  // ---------------------------------------------------------------------------
  // Досылка в облако: записи переезжают и без новой победы
  // ---------------------------------------------------------------------------

  /* ЗАЧЕМ ЭТО ЕСТЬ.
     В облако мы писали только при сохранении результата, то есть после победы.
     Человек, который уже наигрался и просто открыл игру на втором устройстве,
     видел пустую таблицу — и решал, что рекорды пропали. А они целы, просто
     лежат в памяти первого телефона и ждут ближайшей победы.
     Теперь так: прочитали облако, увидели, что там не хватает нашего, — тихо
     дослали.

     ТРИ ПРАВИЛА, КОТОРЫЕ ЗДЕСЬ НЕЛЬЗЯ НАРУШАТЬ.

     1. Ответа досылки никто не ждёт. Таблица к этому мгновению уже собрана
        из памяти телефона и показана. Досылка идёт своей дорогой: молчит
        облако или отказывает — человек этого не замечает вовсе.

     2. Досылаем только когда облако ОТВЕТИЛО. Отказ и молчание — это не
        «в облаке пусто», это «мы не знаем, что там лежит». На другом устройстве
        могла остаться запись, которой у нас нет, и залить её своей — значит
        стереть чужой труд. Поэтому досылка вызывается исключительно из ветки
        удачного чтения, и никогда — из «catch».

     3. Не долбимся. Сравниваем не тексты записей, а их суть (у рекордов —
        таблицу, у рейтинга — число сыгранных партий): иначе два устройства
        с разными служебными заметками принялись бы переписывать друг друга
        при каждом открытии таблицы. А если досылка не удалась — считаем неудачи
        и после второй прекращаем до перезапуска игры. */

  var МАКСИМУМ_НЕУДАЧНЫХ_ДОСЫЛОК = 2;
  var досылки = {};   // ключ записи -> идёт ли она сейчас и сколько раз не вышло

  function памятьДосылки(ключ) {
    var метка = 'ключ:' + ключ;   // приставка, чтобы имя ключа не притворилось служебным полем
    if (!досылки[метка]) досылки[метка] = { идёт: false, неудач: 0 };
    return досылки[метка];
  }

  /**
   * Дослать запись в облако, если там не хватает нашего.
   * вОблаке и уНас — короткие приметы содержимого, их сравниваем.
   * собратьТекст — что записать; вызывается только если досылка нужна,
   * чтобы зря не собирать текст при каждом открытии таблицы.
   * Наружу функция ничего не возвращает нарочно: ждать её нечего и незачем.
   */
  function дослатьВОблако(ключ, вОблаке, уНас, собратьТекст) {
    if (вОблаке === уНас) return;               // в облаке уже то же самое
    var память = памятьДосылки(ключ);
    if (память.идёт || память.неудач >= МАКСИМУМ_НЕУДАЧНЫХ_ДОСЫЛОК) return;

    память.идёт = true;
    записатьВОблако(ключ, собратьТекст()).then(function () {
      память.идёт = false;
      память.неудач = 0;
    }, function () {
      память.идёт = false;
      память.неудач++;
    });
  }

  // ---------------------------------------------------------------------------
  // Чтение и запись рекордов: сначала облако, при любой осечке — память браузера
  // ---------------------------------------------------------------------------

  /**
   * Складываем два списка рекордов в один.
   * Это главное правило хранения: облако и память браузера не спорят, кто главнее,
   * а объединяются. Иначе победа, добытая без связи (она легла только в память
   * телефона), исчезала бы при первом же удачном ответе облака.
   * Одинаковой считаем запись, у которой совпали все три приметы: очки, дата
   * и уровень сложности. Уровень обязателен: «3 победы подряд на сложном» и
   * «3 победы подряд на лёгком» в один день — разные достижения, и второе даётся
   * труднее. Если сравнивать только по очкам и дате, вторая строка молча пропадёт.
   *
   * ОТДЕЛЬНО — ДВЕ СТРОКИ ОДНОЙ И ТОЙ ЖЕ СЕРИИ.
   * Облако может отстать от памяти телефона: запись туда сорвалась, и там осталась
   * та же серия, но короче — «1 подряд» против «3 подряд». По очкам они разные,
   * поэтому раньше склеить их было нечем, и обрывок оставался в таблице навсегда.
   * Теперь у каждой серии есть свой номер, и две строки с одним номером — заведомо
   * одна серия: оставляем ту, где побед больше, она и есть настоящая её длина.
   * У строк, записанных старой версией игры, номера нет — они складываются
   * по-прежнему, только по трём приметам, и ничего не теряют.
   */
  function сложитьРекорды(первые, вторые) {
    var всё = первые.таблица.concat(вторые.таблица);
    var видели = {};
    var местоСерии = {};      // номер серии → где её строка уже лежит в таблице
    var таблица = [];

    for (var i = 0; i < всё.length; i++) {
      var запись = всё[i];

      if (запись.номерСерии) {
        // Приставка нужна, чтобы номер из испорченного файла не притворился
        // служебным полем объекта (например, «__proto__»).
        var ключСерии = 'серия:' + запись.номерСерии;
        var местоРанее = местоСерии[ключСерии];
        if (typeof местоРанее === 'number') {
          // Та же серия, но одна из копий отстала — берём ту, где побед больше
          if (запись.очки > таблица[местоРанее].очки) таблица[местоРанее] = запись;
          continue;
        }
        местоСерии[ключСерии] = таблица.length;
        таблица.push(запись);
        continue;
      }

      var примета = запись.очки + '|' + запись.дата + '|' + запись.сложность;
      if (видели[примета] === true) continue;
      видели[примета] = true;
      таблица.push(запись);
    }

    var память = выбратьЗаметку(первые.последняяСерия, вторые.последняяСерия);
    // Чистим до обрезания десятки: иначе обрывок успеет вытеснить настоящий рекорд
    таблица = убратьПрежнийВидСтроки(таблица, память);

    таблица.sort(function (первая, вторая) { return вторая.очки - первая.очки; });
    таблица = таблица.slice(0, МАКСИМУМ_ЗАПИСЕЙ);

    var лучший = Math.max(целоеЧисло(первые.лучший), целоеЧисло(вторые.лучший));
    for (var j = 0; j < таблица.length; j++) {
      if (таблица[j].очки > лучший) лучший = таблица[j].очки;
    }

    return { лучший: лучший, таблица: таблица, последняяСерия: память };
  }

  /**
   * Какую из двух заметок о серии взять.
   * Обычно — ту, что с этого устройства (вторая, из памяти браузера): счёт побед
   * подряд игра тоже держит только здесь, на другом телефоне серия начинается заново.
   * Но если обе заметки об ОДНОЙ И ТОЙ ЖЕ серии (номер совпал), берём ту, где побед
   * больше. Это страховка на случай, когда память браузера отказала в записи,
   * а облако записалось: иначе устаревшая заметка сочтёт свою же серию чужой
   * и заведёт ей вторую строку.
   */
  function выбратьЗаметку(облачная, местная) {
    if (!местная) return облачная || null;
    if (!облачная) return местная;
    if (местная.номерСерии && местная.номерСерии === облачная.номерСерии) {
      return облачная.очки > местная.очки ? облачная : местная;
    }
    return местная;
  }

  /**
   * Выбрасываем строку, которая осталась от прежнего вида той же серии.
   *
   * Зачем. Игрок обновил игру посреди серии: в обоих хранилищах лежала строка
   * без номера («6 подряд»). Первая же победа вырастила местную строку до «7»
   * и впервые дала ей номер, а запись в облако сорвалась — там осталась «6 подряд»
   * без номера. По номеру их не свести (у одной его нет), по очкам они разные,
   * и брошенная «6» осталась бы в таблице навсегда, заняв место в десятке.
   *
   * Выбрасываем строго осторожно: только ту строку, чьи приметы мы сами записали
   * в заметку как прежний вид ЭТОЙ серии, и только если рядом уже лежит её нынешняя
   * строка с номером. Просто «похожие дата и уровень» не годятся: две честные серии,
   * разорванные проигрышем в один день на одном уровне, так склеились бы в одну
   * и живой рекорд бы пропал.
   */
  function убратьПрежнийВидСтроки(таблица, заметка) {
    if (!заметка || !заметка.номерСерии || !заметка.прежниеПриметы) return таблица;
    var прежние = заметка.прежниеПриметы;

    var естьНынешняя = false;
    for (var i = 0; i < таблица.length; i++) {
      if (таблица[i].номерСерии === заметка.номерСерии) { естьНынешняя = true; break; }
    }
    if (!естьНынешняя) return таблица;

    var очищенная = [];
    for (var j = 0; j < таблица.length; j++) {
      var строка = таблица[j];
      var этоПрежнийВид = !строка.номерСерии &&
        строка.очки === прежние.очки &&
        строка.дата === прежние.дата &&
        строка.сложность === прежние.сложность;
      if (!этоПрежнийВид) очищенная.push(строка);
    }
    return очищенная;
  }

  /**
   * Рекорды из памяти этого устройства: новая запись плюс запись до переезда
   * на латинский ключ.
   *
   * ПОЧЕМУ ПЕРЕЕЗД СДЕЛАН ИМЕННО ТАК — чтением, а не переносом.
   * Обычный перенос («прочитать старое, записать новое, старое стереть») можно
   * прервать: человек закрыл игру ровно посередине, и рекорды пропали. Здесь
   * прерывать нечего. Старую запись мы просто ЧИТАЕМ при каждом обращении и
   * складываем с новой тем же способом, каким складываем облако и память
   * телефона. Записываем всегда только в новый ключ, старый не трогаем совсем.
   *
   * Отсюда три свойства, ради которых всё и затевалось:
   *   — прервать нечего: закрытая на полпути игра ничего не теряет;
   *   — повторить безопасно: одинаковые строки схлопываются, они и раньше
   *     схлопывались при встрече облака и памяти;
   *   — откат безопасен: если игру придётся вернуть к прежней версии, старая
   *     запись цела и человек увидит свои рекорды на день обновления.
   * Цена — лишнее чтение маленькой записи из памяти телефона. Копейки.
   *
   * Порядок доводов важен: старая запись идёт первой, новая второй, потому что
   * заметку о недописанной серии «сложитьРекорды» берёт у второй. Пока новой
   * записи нет (первый запуск после обновления), заметка достаётся от старой —
   * и серия, начатая до обновления, не разрывается на две строки.
   *
   * Старая запись — ТОЛЬКО У ДУРАКА: до переезда других игр не было. Шашкам
   * и любой следующей игре её не подкладываем, иначе дураковы серии всплыли бы
   * в чужой таблице.
   */
  function местныеРекорды(игра) {
    var новые = разобратьРекорды(прочитатьИзПамяти(ключРекордов(игра)));
    if (игра !== ИГРА_ПО_УМОЛЧАНИЮ) return новые;
    var доПереезда = прочитатьИзПамяти(СТАРЫЙ_КЛЮЧ_РЕКОРДОВ);
    if (!доПереезда) return новые;
    return сложитьРекорды(разобратьРекорды(доПереезда), новые);
  }

  function прочитатьРекорды(игра) {
    var ключ = ключРекордов(игра);
    // Местные рекорды читаем всегда: в них лежат победы, добытые без связи
    var местные = местныеРекорды(игра);

    return прочитатьИзОблака(ключ)
      .then(function (текст) {
        // Облачные ставим первыми: при равных очках они и останутся выше
        var облачные = разобратьРекорды(текст);
        var итог = сложитьРекорды(облачные, местные);
        // Облако ОТВЕТИЛО, и в нём не хватает наших строк — тихо дошлём.
        // Сравниваем таблицы, а не тексты записей: служебная заметка о серии
        // на каждом устройстве своя, и по текстам два телефона переписывали бы
        // друг друга без конца.
        if (итог.таблица.length > 0) {
          дослатьВОблако(ключ,
            JSON.stringify(ответДляИгры(облачные)),
            JSON.stringify(ответДляИгры(итог)),
            function () { return текстРекордов(итог); });
        }
        return итог;
      })
      .catch(function () {
        // Облака нет, оно молчит или отказало — работаем с памятью браузера
        return местные;
      });
  }

  /** Запись рекордов в том виде, в каком она ложится в хранилище. */
  function текстРекордов(рекорды) {
    return JSON.stringify(рекорды);
  }

  function записатьРекорды(рекорды, игра) {
    var ключ = ключРекордов(игра);
    var текст = текстРекордов(рекорды);
    // В память браузера пишем всегда: если облако промолчит, рекорд всё равно уцелеет.
    записатьВПамять(ключ, текст);
    return записатьВОблако(ключ, текст).catch(function () {
      // Облака нет или оно отказало — это нормально, в памяти браузера уже сохранено.
      return null;
    });
  }

  // ---------------------------------------------------------------------------
  // Серия побед подряд занимает одну строку, а не десять ступенек
  // ---------------------------------------------------------------------------

  // Игра сообщает результат после каждой победы: 1, 2, 3… Если записывать каждый
  // раз новую строку, серия из десяти побед займёт всю таблицу («10 подряд»,
  // «9 подряд», «8»…), хотя результат по сути один. Поэтому помним последнюю
  // записанную серию и растим её строку.
  //
  // Заметку о ней держим НЕ в переменной страницы, а в самом хранилище рядом
  // с таблицей. Иначе выходила беда: игра длину серии помнит (она лежит в памяти
  // браузера), а рекорды после перезагрузки — уже нет, и заводят новую строку.
  // В Telegram страница запускается заново при каждом открытии из бота, так что
  // ступеньки получались почти у каждого игрока.

  /**
   * Придумать номер новой серии — примету, по которой её строку потом узнают.
   *
   * Из чего собран и почему не повторится:
   *   — время начала серии (миллисекунды, записанные покороче) — две серии на одном
   *     устройстве не начинаются в одну и ту же миллисекунду;
   *   — две случайные добавки — они разводят устройства одного и того же игрока.
   * Вторая часть здесь важнее краткости: телефон и планшет, подключённые к одному
   * облаку, часы имеют почти одинаковые. Совпади номера у РАЗНЫХ серий — их бы
   * склеило в одну и один рекорд пропал бы, а это хуже, чем длинная строка в файле.
   */
  function новыйНомерСерии() {
    // Нули в хвосте не для красоты: у «круглого» случайного числа запись короткая
    // (0.5 — это «0.i»), и без них добавка иногда выходила короче шести знаков.
    var случайная = function () {
      return (Math.random().toString(36) + '0000000').slice(2, 8);
    };
    return Date.now().toString(36) + '-' + случайная() + случайная();
  }

  // Уровни соперника от самого лёгкого к самому трудному — так их называет игра.
  //
  // ЭТОТ СПИСОК ОБЯЗАН СЛОВО В СЛОВО СОВПАДАТЬ со списком «СЛОЖНОСТИ» в js/game.js —
  // вплоть до буквы «ё» в «лёгкий». Этими словами игра называет уровень соперника,
  // и по ним мы решаем, какой уровень легче.
  // Вторым доводом saveScore приходит не одно такое слово, а ПОДПИСЬ: за столом
  // компанией игра дописывает к уровню стол — «сложный · вчетвером». Уровень в ней
  // стоит первым, до разделителя, и достаётся отсюда (см. «уровеньИзПодписи» ниже).
  // Если списки разойдутся (уровень переименуют там и забудут здесь), ничего не сломается
  // заметно и в консоли не будет ни одной ошибки: незнакомое слово просто не пойдёт в счёт,
  // и подписи серий начнут молча врать — покажут не самый лёгкий уровень, а какой попало.
  // Переименовали уровень в игре — переименуйте и здесь.
  var ПОРЯДОК_УРОВНЕЙ = ['лёгкий', 'обычный', 'сложный'];

  /* Подпись, которую присылает игра, бывает СОСТАВНОЙ: «сложный · вчетвером».
     Уровень в ней стоит первым, до разделителя, а дальше — за каким столом
     набрана серия.

     Откуда это взялось. Победы за столом компанией теперь тоже идут в рекорды,
     а выигрывать компанией заметно легче (по замеру вшестером человек берёт
     149 партий из 200, один на один — 67). В таблице обе выглядели одинаково,
     поэтому игра стала дописывать стол к уровню. Игра один на один подписана
     по-прежнему одним словом — «сложный», без добавки.

     Почему без этого разбора подписи молча врут — ровно то, о чём предупреждает
     заметка выше: «сложный · вчетвером» в списке уровней не находится, счёт
     самого лёгкого ломается, и серия, начатая на лёгком сопернике, получает
     подпись «сложный». Это приписка заслуги, от которой мы и защищаемся. */
  var РАЗДЕЛИТЕЛЬ_ПОДПИСИ = ' · ';

  /** Уровень из подписи: то, что стоит до разделителя (или вся подпись). */
  function уровеньИзПодписи(подпись) {
    var строка = String(подпись);
    var место = строка.indexOf(РАЗДЕЛИТЕЛЬ_ПОДПИСИ);
    return место < 0 ? строка : строка.slice(0, место);
  }

  /** Хвост подписи: за каким столом. Пусто — значит игра была один на один. */
  function столИзПодписи(подпись) {
    var строка = String(подпись);
    var место = строка.indexOf(РАЗДЕЛИТЕЛЬ_ПОДПИСИ);
    return место < 0 ? '' : строка.slice(место + РАЗДЕЛИТЕЛЬ_ПОДПИСИ.length);
  }

  /**
   * Чем подписать серию, сыгранную двумя подписями подряд.
   *
   * Имя нарочно про подпись, а не про уровень: возвращается целая подпись,
   * вместе со столом. Прежнее имя обещало одно слово и сбивало читателя с толку.
   *
   * Уровень берём САМЫЙ ЛЁГКИЙ из двух: незнакомое слово в счёт не идёт,
   * берём тот уровень, который мы знаем.
   * Стол берём у ПОСЛЕДНЕЙ партии (второй довод) — так решено в игре:
   * обрывать серию из-за смены стола нечестно, а пометить её составом
   * последней партии честнее, чем не пометить вовсе.
   */
  function подписьПродолженнойСерии(первый, второй) {
    var уровеньПервого = уровеньИзПодписи(первый);
    var уровеньВторого = уровеньИзПодписи(второй);
    var местоПервого = ПОРЯДОК_УРОВНЕЙ.indexOf(уровеньПервого);
    var местоВторого = ПОРЯДОК_УРОВНЕЙ.indexOf(уровеньВторого);
    var лёгкий;
    if (местоПервого < 0) лёгкий = уровеньВторого;
    else if (местоВторого < 0) лёгкий = уровеньПервого;
    else лёгкий = местоПервого <= местоВторого ? уровеньПервого : уровеньВторого;
    var стол = столИзПодписи(второй);
    return стол ? лёгкий + РАЗДЕЛИТЕЛЬ_ПОДПИСИ + стол : лёгкий;
  }

  /**
   * Та же ли это серия, что мы записывали в прошлый раз.
   * Продолжением считаем результат ровно на одну победу больше предыдущего.
   * Уровень при этом может смениться: игра из-за смены соперника серию не
   * обрывает, значит, и таблица не должна разбивать её на две строки.
   * Новая серия всегда начинается с меньшего числа, поэтому чужие рекорды
   * такое правило не затрагивает.
   *
   * Вопрос «та же ли серия» намеренно отделён от вопроса «есть ли ещё её строка
   * в таблице». У игрока с богатой историей короткие серии в десятку не влезают
   * и вылетают, но серия при этом продолжается — и подписать её надо всё равно
   * по заметке, а не по сегодняшнему сопернику.
   */
  function серияПродолжается(рекорды, результат) {
    var память = рекорды.последняяСерия;
    if (!память) return false;
    return результат === память.очки + 1;
  }

  /**
   * Ищем в таблице строку той серии, о которой говорит заметка.
   * Сначала по номеру — это самая надёжная примета. Не нашли (номера в заметке нет
   * или он пропал из строки, например запись в хранилище испортилась) — ищем
   * по-старому, по трём приметам: так файл работал до появления номеров, и хуже
   * от запасного хода не станет. Без него та же серия завела бы себе вторую строку,
   * и игрок увидел бы сразу «3 подряд» и «4 подряд».
   * Нашли — вернём саму строку, нет — null (строка выпала из десятки, нужна новая).
   */
  function найтиСтрокуСерии(рекорды) {
    var память = рекорды.последняяСерия;
    if (!память) return null;
    var i, строка;

    if (память.номерСерии) {
      for (i = 0; i < рекорды.таблица.length; i++) {
        строка = рекорды.таблица[i];
        if (строка.номерСерии === память.номерСерии) return строка;
      }
    }

    for (i = 0; i < рекорды.таблица.length; i++) {
      строка = рекорды.таблица[i];
      // Строка с чужим номером — заведомо другая серия, её приметы ни о чём не говорят
      if (строка.номерСерии && строка.номерСерии !== память.номерСерии) continue;
      if (строка.очки === память.очки &&
          строка.дата === память.дата &&
          строка.сложность === память.сложность) {
        return строка;
      }
    }
    return null;
  }

  /**
   * Номер продолжающейся серии.
   * Обычно он лежит в заметке. Если заметка досталась от прежней версии игры,
   * номера в ней нет — тогда придумываем его прямо сейчас: серия та же, просто
   * до этой минуты её нечем было пометить.
   */
  function номерПродолжаемойСерии(рекорды) {
    var память = рекорды.последняяСерия;
    if (память && память.номерСерии) return память.номерСерии;
    return новыйНомерСерии();
  }

  /**
   * Чем подписать строку серии: уровнем, а за столом компанией — уровнем и столом.
   * Уровень берём САМЫЙ ЛЁГКИЙ из сыгранных в этой серии, а не последний. Рекорд не должен
   * приписывать игроку заслугу, которой не было: если серию начали на лёгком сопернике,
   * а доиграли на сложном — это не «серия на сложном», и подписывать её так нечестно.
   */
  function уровеньСтрокиСерии(рекорды, режим) {
    if (!рекорды.последняяСерия) return режим;
    return подписьПродолженнойСерии(рекорды.последняяСерия.сложность, режим);
  }

  /** Кладём заметку о серии рядом с таблицей — чтобы она пережила перезагрузку.
      Имя нарочно длинное: в js/game.js есть своя «запомнитьСерию», и она про другое —
      пишет длину серии в память браузера. Похожие имена легко перепутать. */
  function запомнитьЗаметкуОСерии(рекорды, результат, дата, режим, номер, прежние) {
    var заметка = {
      очки: результат, дата: дата, сложность: режим, номерСерии: номер
    };
    if (прежние) заметка.прежниеПриметы = прежние;
    рекорды.последняяСерия = заметка;
  }

  /**
   * Как выглядела строка серии до того, как получила номер.
   * Пока серия идёт, эту примету не теряем: копия старого вида может лежать
   * в другом хранилище и всплыть позже — например, когда вернётся связь.
   * Серия оборвалась — заметка заводится заново, и примета уходит вместе с ней.
   */
  function прежнийВидСтроки(рекорды, продолжаем, строкаСерии) {
    if (строкаСерии && !строкаСерии.номерСерии) {
      // Первый рост строки, доставшейся от прежней версии игры: снимаем её приметы,
      // потому что сейчас мы их перепишем и узнать старую копию будет больше нечем.
      return {
        очки: строкаСерии.очки,
        дата: строкаСерии.дата,
        сложность: строкаСерии.сложность
      };
    }
    if (продолжаем && рекорды.последняяСерия && рекорды.последняяСерия.прежниеПриметы) {
      return рекорды.последняяСерия.прежниеПриметы;
    }
    return null;
  }

  // ---------------------------------------------------------------------------
  // Три функции, которыми пользуется игра
  // ---------------------------------------------------------------------------

  /**
   * Ответ для игры: ровно то, что обещано в договоре, —
   * { лучший, таблица: [ { очки, дата, сложность } ] }.
   * Служебное наружу не отдаём: ни заметку о последней серии, ни номера серий
   * в строках. Это наша внутренняя кухня хранения, игре она не нужна и в договоре
   * её нет. Поэтому строки пересобираем по одному полю, а не отдаём как есть.
   */
  function ответДляИгры(рекорды) {
    var таблица = [];
    for (var i = 0; i < рекорды.таблица.length; i++) {
      var строка = рекорды.таблица[i];
      таблица.push({ очки: строка.очки, дата: строка.дата, сложность: строка.сложность });
    }
    return { лучший: рекорды.лучший, таблица: таблица };
  }

  /** Готово ли хранилище рекордов к работе. Обычное да/нет, без обещаний. */
  function isReady() {
    return памятьРаботает || получитьОблако() !== null;
  }

  /**
   * Записать результат партии.
   * очки — длина серии побед подряд (число).
   * сложность — необязательная строка; не передали — считаем «обычный».
   * игра — необязательная строка («дурак», «шашки»); не передали — «дурак».
   *        У каждой игры своя таблица: серии шашек в дуракову не попадают.
   * Дату проставляем здесь сами. Обещание никогда не «падает»: даже если сохранить
   * не удалось, игра получит объект рекордов и спокойно покажет экран результата.
   */
  function saveScore(очки, сложность, игра) {
    var результат = целоеЧисло(очки);
    var режим = привестиСложность(сложность);
    var имяИгры = привестиИгру(игра);

    return прочитатьРекорды(имяИгры)
      .then(function (рекорды) {
        var дата = сегодняшняяДата();
        // Продолжение серии определяем по заметке, а не по наличию строки в таблице:
        // строка могла вылететь из десятки, но серия от этого не оборвалась.
        var продолжаем = серияПродолжается(рекорды, результат);
        var уровень = продолжаем ? уровеньСтрокиСерии(рекорды, режим) : режим;
        // Новая серия получает свой номер, продолжение — оставляет прежний
        var номер = продолжаем ? номерПродолжаемойСерии(рекорды) : новыйНомерСерии();
        var строкаСерии = продолжаем ? найтиСтрокуСерии(рекорды) : null;
        // Снимаем приметы ДО того, как перепишем строку: потом узнать её будет нечем
        var прежние = прежнийВидСтроки(рекорды, продолжаем, строкаСерии);

        if (строкаСерии) {
          // Та же серия стала длиннее — растим её строку, новую не заводим
          строкаСерии.очки = результат;
          строкаСерии.дата = дата;
          строкаСерии.сложность = уровень;
          строкаСерии.номерСерии = номер;
        } else {
          рекорды.таблица.push({
            очки: результат, дата: дата, сложность: уровень, номерСерии: номер
          });
        }
        запомнитьЗаметкуОСерии(рекорды, результат, дата, уровень, номер, прежние);

        // Лучшие сверху. При равных очках выше остаётся тот, кто добился раньше:
        // сортировка в браузере устойчивая и порядок одинаковых записей не меняет.
        рекорды.таблица.sort(function (первая, вторая) { return вторая.очки - первая.очки; });
        рекорды.таблица = рекорды.таблица.slice(0, МАКСИМУМ_ЗАПИСЕЙ);
        if (результат > рекорды.лучший) рекорды.лучший = результат;

        return записатьРекорды(рекорды, имяИгры).then(function () { return ответДляИгры(рекорды); });
      })
      .catch(function () {
        return ответДляИгры(пустыеРекорды());
      });
  }

  /**
   * Прочитать рекорды.
   * игра — необязательная строка («дурак», «шашки»); не передали — «дурак».
   * Обещание всегда приходит с объектом { лучший: число, таблица: [ { очки, дата, сложность } ] },
   * до 10 записей, лучшие сверху. Рекордов нет — { лучший: 0, таблица: [] }.
   */
  function loadScore(игра) {
    return прочитатьРекорды(привестиИгру(игра))
      .then(ответДляИгры)
      .catch(function () { return ответДляИгры(пустыеРекорды()); });
  }

  // ===========================================================================
  // ЛИЧНЫЙ РЕЙТИНГ ИГРОКА
  // ===========================================================================

  /* Что это такое. Таблица рекордов отвечает на один вопрос — «какая серия побед
     была самой длинной». Рейтинг отвечает на остальные: сколько всего партий,
     сколько выиграно и проиграно, с кем — с ботами или с живыми людьми, за каким
     столом и против какого уровня.

     Рейтинг ЛИЧНЫЙ: он лежит у самого игрока (облако Telegram, а без него —
     память браузера) и потому работает всегда, без сервера и без затрат. Общий
     рейтинг («кто сильнее всех») — второй заход, для него нужен сервер, который
     не выключается. Мы к нему готовимся: живые партии складываются в журнал
     (см. ниже), чтобы силу игрока потом можно было ПЕРЕСЧИТАТЬ, а не начать
     с нуля.

     Почему отдельной записью, а не внутри рекордов. Таблица рекордов работает
     у живых игроков давно. Дописать в неё новые поля — значит рискнуть чужими
     рекордами ради счётчика. Здесь своя запись и свой ключ: испортится рейтинг —
     рекорды этого даже не заметят, и наоборот.

     Как выглядит запись в хранилище (внутренний вид, наружу он не торчит):
       {
         версия: 1,
         игра: 'дурак',
         всего:  [партий, побед, поражений, ничьих],
         серия:  [сейчас, лучшая],
         клетки: { 'уровень|стол|соперники': [партий, побед, поражений, ничьих] },
         серии:   { 'уровень:лёгкий': [сейчас, лучшая], 'стол:вчетвером': […] },
         партии: [ [день, исход, стол, соперники, уровень, кто, ходов, раздача], … ],
         начало: 'дд.мм.гггг', обновлено: 'дд.мм.гггг',
         обнулено: 1757260000000   // когда счёт стирали (миллисекунды); нет — не стирали
       }

     Счётчики держим врозь по трём приметам сразу («сложный|вшестером|боты»),
     потому что победа вдвоём над лёгким ботом и победа вшестером над сложным —
     разные победы, и складывать их в одно число нечестно. Привычные разрезы
     (по уровням, по столам, по соперникам) считаются из этих клеток при чтении:
     так в хранилище лежит меньше чисел, а показать можно больше. */

  var ВЕРСИЯ_РЕЙТИНГА = 1;          // выросла — значит, запись пишется иначе
  var ДЛИНА_ПРИМЕТЫ = 24;           // длиннее имени уровня или стола быть не должно
  var ДЛИНА_ПРИМЕТЫ_СОПЕРНИКА = 48; // кто сидел напротив — для будущего общего рейтинга

  /* Сколько клеток и серий храним. Ограничения не от жадности: испорченная или
     чужая запись не должна раздуть хранилище до отказа облака. Живой игре хватает
     с большим запасом — у дурака клеток самое большее 24 (3 уровня × 6 столов
     против ботов плюс 6 столов против людей). */
  var МАКСИМУМ_КЛЕТОК = 60;
  var МАКСИМУМ_СЕРИЙ = 30;

  /* ЖУРНАЛ ПАРТИЙ — короткая память о том, как игрок играл в последнее время.
     Счётчики отвечают на вопрос «сколько всего», журнал — на вопрос «а стало ли
     лучше»: по нему считается доля побед за последние 20 и 50 партий, и её есть
     с чем сравнить.

     Живые и ботовые партии считаются врозь и друг друга не вытесняют:
       — живых храним больше, они ещё и запас для будущего общего рейтинга
         (чтобы пересчитать силу игрока, нужны сами партии, а не итоговые числа);
       — ботовых хватает сорока: они нужны только для свежей доли побед,
         а играют с ботами часто, и старые партии быстро теряют смысл.

     Переполнился журнал — уходят самые старые записи своего же вида.
     СЧЁТЧИКИ при этом не страдают никогда: они и есть рейтинг. */
  var ЖУРНАЛ_ЖИВЫХ = 60;
  var ЖУРНАЛ_БОТОВЫХ = 40;
  /* Столько ботовых партий бережём, даже когда не хватает места в облаке:
     меньше двадцати — и «доля побед за последние 20 партий» станет неправдой. */
  var МИНИМУМ_БОТОВЫХ = 20;

  /* Предел одной записи в облаке Telegram — 4096 знаков (это его правило, а не
     совет: длиннее просто не запишется). Держимся заметно ниже сразу по двум
     меркам, потому что русские буквы в передаче занимают по два байта, и что
     именно считает Telegram — знаки или байты, — проверить неоткуда. */
  var ПРЕДЕЛ_ЗНАКОВ = 3500;
  var ПРЕДЕЛ_БАЙТОВ = 4000;

  /* Ключ хранения зависит от ИГРЫ — иначе счёт дурака и шашек сложился бы
     в одну кашу. Собирает его «ключРейтинга» из раздела «Имена ключей»:
     там же лежит ключ рекордов и правило облака про латиницу. */

  // Как называются стороны и исходы. Порядок в ИСХОДЫ — это ещё и код в журнале.
  var СОПЕРНИКИ_ЛЮДИ = 'люди';
  var СОПЕРНИКИ_БОТЫ = 'боты';
  var ИСХОДЫ = ['поражение', 'победа', 'ничья'];
  var КОД_БОТОВ = 0;
  var КОД_ЖИВЫХ = 1;

  // Стол словами — теми же, что и везде в игре («вчетвером», «парами»)
  var СТОЛ_ПАРАМИ = 'парами';
  var СЛОВО_СТОЛА = { 2: 'вдвоём', 3: 'втроём', 4: 'вчетвером', 5: 'впятером', 6: 'вшестером' };
  var ИГРОКОВ_ЗА_СТОЛОМ = { 'вдвоём': 2, 'втроём': 3, 'вчетвером': 4, 'впятером': 5, 'вшестером': 6 };
  var ПОРЯДОК_СТОЛОВ = ['вдвоём', 'втроём', 'вчетвером', 'впятером', 'вшестером', СТОЛ_ПАРАМИ];
  var КОД_СТОЛА_ПАРАМИ = 7;   // в журнале стол — число, а «парами» числом не назовёшь

  // Имя клетки — ровно три части через палку. Мусорные имена (в том числе
  // служебные вроде «__proto__») сюда не пролезают: в них нет двух палок.
  var ИМЯ_КЛЕТКИ = /^[^|]{0,24}\|[^|]{1,24}\|[^|]{1,24}$/;
  var ИМЯ_СЕРИИ = /^(уровень|стол|соперники):[^|:]{1,24}$/;

  var ДЕНЬ = 86400000;                        // миллисекунд в сутках
  var НАЧАЛО_ОТСЧЁТА = Date.UTC(2020, 0, 1);  // от этого дня считаем дни в журнале

  // ---------------------------------------------------------------------------
  // Рейтинг: где лежит и как читается
  // ---------------------------------------------------------------------------

  /** Пустой рейтинг: так выглядит счёт игрока, который ещё не сыграл ни партии. */
  function пустойРейтинг(игра) {
    return {
      игра: игра,
      всего: [0, 0, 0, 0],
      серия: [0, 0],
      клетки: Object.create(null),
      серии: Object.create(null),
      партии: [],
      начало: '',
      обновлено: '',
      обнулено: 0
    };
  }

  /** Ряд неотрицательных целых заданной длины: чего нет — то ноль. */
  function рядЧисел(значение, сколько) {
    var ряд = [];
    for (var i = 0; i < сколько; i++) {
      ряд.push(Array.isArray(значение) ? целоеЧисло(значение[i]) : 0);
    }
    return ряд;
  }

  /** Строка-примета из хранилища: коротко и без палок, иначе — пусто. */
  function привестиПримету(значение, предел) {
    if (typeof значение !== 'string') return '';
    var примета = значение.trim();
    if (!примета || примета.length > предел || примета.indexOf('|') >= 0) return '';
    return примета;
  }

  /** Клетки счёта из хранилища. Имена проверяем по образцу, число клеток ограничиваем. */
  function разобратьКлетки(значение) {
    var клетки = Object.create(null);
    if (!значение || typeof значение !== 'object') return клетки;
    var сколько = 0;
    for (var имя in значение) {
      if (!Object.prototype.hasOwnProperty.call(значение, имя)) continue;
      if (!ИМЯ_КЛЕТКИ.test(имя)) continue;
      if (сколько >= МАКСИМУМ_КЛЕТОК) break;
      клетки[имя] = рядЧисел(значение[имя], 4);
      сколько++;
    }
    return клетки;
  }

  /** Серии по разрезам из хранилища: имя → [сейчас, лучшая]. */
  function разобратьСерии(значение) {
    var серии = Object.create(null);
    if (!значение || typeof значение !== 'object') return серии;
    var сколько = 0;
    for (var имя in значение) {
      if (!Object.prototype.hasOwnProperty.call(значение, имя)) continue;
      if (!ИМЯ_СЕРИИ.test(имя)) continue;
      if (сколько >= МАКСИМУМ_СЕРИЙ) break;
      серии[имя] = рядЧисел(значение[имя], 2);
      сколько++;
    }
    return серии;
  }

  /* Одна запись журнала — ряд чисел, чтобы занимать поменьше места:
       [день, исход, стол, соперники, уровень, кто, ходов, раздача]
     Что означают числа — см. «кодСтола», «кодСоперников», «кодУровня».
     Строкой в записи бывает только «кто» (у живых партий), и только на шестом
     месте. «ходов» — сколько ходов заняла партия, «раздача» — номер дня, чью
     раздачу дня играли (тем же счётом дней, что и первое место); в обычной
     партии обоих нет. Хвост записи обрезается по последнему непустому месту —
     см. «собратьЗаписьЖурнала». */

  /** Журнал партий из хранилища. Мусорные строки просто выпадают. */
  function разобратьЖурналПартий(значение) {
    var журнал = [];
    if (!Array.isArray(значение)) return журнал;
    for (var i = 0; i < значение.length; i++) {
      var запись = разобратьЗаписьЖурнала(значение[i]);
      if (запись) журнал.push(запись);
    }
    return журнал;
  }

  function разобратьЗаписьЖурнала(запись) {
    if (!Array.isArray(запись)) return null;
    var исход = целоеЧисло(запись[1]);
    if (исход >= ИСХОДЫ.length) return null;

    // Записи прежней версии игры выглядели иначе: [день, исход, стол, кто],
    // и все до одной были о партиях с живыми людьми. Узнаём их по строке
    // на четвёртом месте и переводим в нынешний вид, ничего не теряя.
    var староеМесто = typeof запись[3] === 'string';
    var соперники = староеМесто ? КОД_ЖИВЫХ : целоеЧисло(запись[3]);
    var уровень = староеМесто ? 0 : целоеЧисло(запись[4]);
    var кто = привестиПримету(староеМесто ? запись[3] : запись[5], ДЛИНА_ПРИМЕТЫ_СОПЕРНИКА);
    var ходов = староеМесто ? 0 : привестиХоды(запись[6]);
    var раздача = староеМесто ? 0 : целоеЧисло(запись[7]);

    return собратьЗаписьЖурнала(
      целоеЧисло(запись[0]),
      исход,
      целоеЧисло(запись[2]),
      соперники === КОД_ЖИВЫХ ? КОД_ЖИВЫХ : КОД_БОТОВ,
      уровень > ПОРЯДОК_УРОВНЕЙ.length ? 0 : уровень,
      кто, ходов, раздача);
  }

  /**
   * Собрать запись журнала.
   * Хвост — «кто», «ходов», «раздача» — пишем только до последнего непустого
   * места: пустые кавычки и нули на каждой обычной партии — впустую занятое
   * место в облаке. Поэтому обычная партия с ботом по-прежнему выглядит как
   * [день, исход, стол, соперники, уровень] и не выросла ни на знак, а пустое
   * «кто» перед непустым хвостом заменяет ноль — «кто» строкой числом не бывает,
   * так что при чтении путаницы нет.
   */
  function собратьЗаписьЖурнала(день, исход, стол, соперники, уровень, кто, ходов, раздача) {
    var запись = [день, исход, стол, соперники, уровень];
    if (раздача > 0) запись.push(кто || 0, ходов, раздача);
    else if (ходов > 0) запись.push(кто || 0, ходов);
    else if (кто) запись.push(кто);
    return запись;
  }

  var ПРЕДЕЛ_ХОДОВ = 9999;   // больше ходов в партии не бывает; испорченная запись журнал не раздует

  function привестиХоды(значение) {
    return Math.min(целоеЧисло(значение), ПРЕДЕЛ_ХОДОВ);
  }

  /**
   * Прочитать запись рейтинга из текста.
   *
   * Ни одна поломка здесь не роняет игру: не разобрался текст, лежит мусор,
   * пришла запись от БУДУЩЕЙ версии игры (номер версии больше нашего) — молча
   * начинаем с чистого листа. Показать человеку ошибку хуже, чем показать нули:
   * с нулями он просто играет дальше, а на ошибку ему нечем ответить.
   * Появится версия 2 — здесь встанет перевод старой записи в новую, и тогда
   * прошлый счёт переживёт обновление.
   */
  function разобратьРейтинг(текст, игра) {
    if (!текст || typeof текст !== 'string') return пустойРейтинг(игра);

    var данные;
    try {
      данные = JSON.parse(текст);
    } catch (ошибка) {
      return пустойРейтинг(игра);
    }
    if (!данные || typeof данные !== 'object') return пустойРейтинг(игра);
    if (целоеЧисло(данные.версия) !== ВЕРСИЯ_РЕЙТИНГА) return пустойРейтинг(игра);

    var рейтинг = пустойРейтинг(игра);
    рейтинг.всего = рядЧисел(данные.всего, 4);
    рейтинг.серия = рядЧисел(данные.серия, 2);
    рейтинг.клетки = разобратьКлетки(данные.клетки);
    рейтинг.серии = разобратьСерии(данные.серии);
    // «партии» — нынешнее поле, «живые» — как журнал звался в прошлой версии.
    // Читаем оба и складываем: у игрока, обновившего игру, прошлые партии
    // не должны пропасть только оттого, что поле переименовали.
    рейтинг.партии = разобратьЖурналПартий(данные.живые).concat(разобратьЖурналПартий(данные.партии));
    подрезатьЖурнал(рейтинг);
    рейтинг.начало = привестиДату(данные.начало);
    рейтинг.обновлено = привестиДату(данные.обновлено);
    рейтинг.обнулено = целоеЧисло(данные.обнулено);

    // Испорченная запись могла разойтись сама с собой: партий меньше, чем исходов.
    // Верим большему числу — иначе доля побед вышла бы больше ста процентов.
    var поИсходам = рейтинг.всего[1] + рейтинг.всего[2] + рейтинг.всего[3];
    if (поИсходам > рейтинг.всего[0]) рейтинг.всего[0] = поИсходам;
    return рейтинг;
  }

  /** Дата из хранилища: короткая строка или пусто. Разбирать её нам не нужно — только показать. */
  function привестиДату(значение) {
    if (typeof значение !== 'string') return '';
    var дата = значение.trim();
    return дата.length <= 10 ? дата : '';
  }

  /**
   * Какую запись взять, когда облако и память браузера разошлись.
   *
   * СКЛАДЫВАТЬ ИХ НЕЛЬЗЯ — и это главное правило здесь. Рекорды сложить можно
   * (лишняя одинаковая строка просто схлопывается), а счётчики сложились бы
   * вместе с партиями, которые уже посчитаны в обоих местах: сыграл сто партий —
   * увидел двести. Поэтому берём одну запись целиком: ту, где партий больше.
   * Счётчики только растут, значит, большее число — это более поздний счёт.
   * При равенстве остаётся местная: она с этого устройства и заведомо свежая.
   *
   * Чем платим: партии, сыгранные на другом устройстве без связи, в общий счёт
   * не попадут. Это честная и понятная потеря, а двойной счёт — враньё, которое
   * человек заметит и перестанет верить всему рейтингу.
   *
   * ОБНУЛЕНИЕ СИЛЬНЕЕ СЧЁТА. Правило «больше партий — новее» ломается ровно
   * в одном месте: человек нажал «стереть», а облако в эту минуту молчало.
   * В памяти ноль, в облаке сто — и по правилу выше сотня «новее»: через минуту
   * старый счёт возвращался из облака сам, будто кнопки не было. Поэтому у
   * стёртого счёта есть отметка времени «обнулено», и запись с более поздним
   * обнулением побеждает всегда, сколько бы партий ни лежало в другой. Сравнивать
   * по партиям можно только записи с одинаковым обнулением — это один и тот же
   * счёт, начатый с одного и того же нуля.
   *
   * Почему время, а не номер обнуления. С номером проиграли бы на двух
   * устройствах: второе уже стёрло счёт в облаке под номером 3, а наше знает
   * только 1 и стирает под номером 2 — и облачная тройка перебила бы наше
   * стирание. Время же у нового стирания всегда позже. Часы, сбитые на год
   * вперёд, эту логику собьют — но такой телефон и рекорды датирует неверно.
   */
  function выбратьРейтинг(облачный, местный) {
    if (облачный.обнулено !== местный.обнулено) {
      return облачный.обнулено > местный.обнулено ? облачный : местный;
    }
    return облачный.всего[0] > местный.всего[0] ? облачный : местный;
  }

  /** Короткая примета записи — по ней решаем, отстало ли облако.
      Обнуление в примете обязательно: стёртый счёт и пустой счёт оба про «0 партий»,
      но пустой не несёт отметки, и без неё стирание не доехало бы до других устройств. */
  function приметаРейтинга(рейтинг) {
    return рейтинг.обнулено + '|' + рейтинг.всего[0];
  }

  function прочитатьРейтинг(игра) {
    var ключ = ключРейтинга(игра);
    var местный = разобратьРейтинг(прочитатьИзПамяти(ключ), игра);

    return прочитатьИзОблака(ключ)
      .then(function (текст) {
        var облачный = разобратьРейтинг(текст, игра);
        var итог = выбратьРейтинг(облачный, местный);
        // Облако ответило, а в нём отстало — дошлём. Примета — отметка обнуления
        // и число партий: обе только растут, поэтому два устройства не начнут
        // переписывать друг друга по кругу. Совсем пустой счёт (ни партий,
        // ни стирания) не досылаем — досылать нечего.
        if (итог.всего[0] > 0 || итог.обнулено > 0) {
          дослатьВОблако(ключ, приметаРейтинга(облачный), приметаРейтинга(итог),
            function () { return текстРейтинга(итог); });
        }
        return итог;
      })
      .catch(function () {
        // Облака нет, оно молчит или отказало — работаем с памятью браузера
        return местный;
      });
  }

  function записатьРейтинг(рейтинг) {
    var ключ = ключРейтинга(рейтинг.игра);
    var текст = текстРейтинга(рейтинг);
    // В память браузера пишем всегда: облако может молчать, а счёт терять нельзя
    записатьВПамять(ключ, текст);
    return записатьВОблако(ключ, текст).catch(function () { return null; });
  }

  /** Запись рейтинга в том виде, в каком она ложится в хранилище. */
  function собратьЗапись(рейтинг) {
    var запись = {
      версия: ВЕРСИЯ_РЕЙТИНГА,
      игра: рейтинг.игра,
      всего: рейтинг.всего,
      серия: рейтинг.серия,
      клетки: рейтинг.клетки,
      серии: рейтинг.серии,
      партии: рейтинг.партии,
      начало: рейтинг.начало,
      обновлено: рейтинг.обновлено
    };
    // Отметку стирания пишем, только если стирали: у большинства её нет, и место в облаке дорого
    if (рейтинг.обнулено > 0) запись.обнулено = рейтинг.обнулено;
    return запись;
  }

  /**
   * Превратить рейтинг в текст и уложиться в предел облака.
   *
   * Место кончилось — ужимаем журнал, и в строгом порядке:
   *   1) сначала самые старые БОТОВЫЕ партии, пока их больше двадцати —
   *      их проще всего пережить: свежая доля побед от этого не пострадает,
   *      а с ботами играют часто, журнал наполнится снова;
   *   2) потом самые старые ЖИВЫЕ — они ценнее: их не восстановить, и по ним
   *      будет считаться будущий общий рейтинг;
   *   3) в последнюю очередь — остаток ботовых.
   * СЧЁТЧИКИ не трогаем ни при каких обстоятельствах: они и есть рейтинг,
   * и «сыграно 700 партий» должно оставаться правдой всегда.
   */
  function текстРейтинга(рейтинг) {
    var текст = JSON.stringify(собратьЗапись(рейтинг));
    while (записьВелика(текст) && выброситьСамуюНенужную(рейтинг)) {
      текст = JSON.stringify(собратьЗапись(рейтинг));
    }
    return текст;
  }

  /** Выбросить одну запись журнала — ту, без которой легче всего обойтись.
      Вернёт «нет», когда выбрасывать уже нечего. */
  function выброситьСамуюНенужную(рейтинг) {
    if (рейтинг.партии.length === 0) return false;
    var местоБотовой = -1, местоЖивой = -1, ботовых = 0, i;

    for (i = 0; i < рейтинг.партии.length; i++) {
      if (рейтинг.партии[i][3] === КОД_ЖИВЫХ) {
        if (местоЖивой < 0) местоЖивой = i;
      } else {
        ботовых++;
        if (местоБотовой < 0) местоБотовой = i;
      }
    }

    if (местоБотовой >= 0 && ботовых > МИНИМУМ_БОТОВЫХ) { рейтинг.партии.splice(местоБотовой, 1); return true; }
    if (местоЖивой >= 0) { рейтинг.партии.splice(местоЖивой, 1); return true; }
    рейтинг.партии.shift();
    return true;
  }

  /** Держим журнал в берегах: живые и ботовые партии считаются врозь,
      поэтому вытеснить друг друга они не могут. */
  function подрезатьЖурнал(рейтинг) {
    оставитьПоследние(рейтинг, КОД_ЖИВЫХ, ЖУРНАЛ_ЖИВЫХ);
    оставитьПоследние(рейтинг, КОД_БОТОВ, ЖУРНАЛ_БОТОВЫХ);
  }

  function оставитьПоследние(рейтинг, чьи, сколько) {
    var лишних = 0, i;
    for (i = 0; i < рейтинг.партии.length; i++) {
      if (рейтинг.партии[i][3] === чьи) лишних++;
    }
    лишних -= сколько;
    if (лишних <= 0) return;

    var оставшиеся = [];
    for (i = 0; i < рейтинг.партии.length; i++) {
      if (рейтинг.партии[i][3] === чьи && лишних > 0) { лишних--; continue; }   // самые старые уходят первыми
      оставшиеся.push(рейтинг.партии[i]);
    }
    рейтинг.партии = оставшиеся;
  }

  function записьВелика(текст) {
    return текст.length > ПРЕДЕЛ_ЗНАКОВ || длинаВБайтах(текст) > ПРЕДЕЛ_БАЙТОВ;
  }

  /** Сколько байтов займёт текст в передаче. Русская буква — два байта, и об этом
      легко забыть, считая одни только знаки. Считаем с запасом в большую сторону. */
  function длинаВБайтах(текст) {
    var байты = 0;
    for (var i = 0; i < текст.length; i++) {
      var код = текст.charCodeAt(i);
      if (код < 0x80) байты += 1;
      else if (код < 0x800) байты += 2;
      else байты += 3;
    }
    return байты;
  }

  // ---------------------------------------------------------------------------
  // Рейтинг: что игра рассказывает о законченной партии
  // ---------------------------------------------------------------------------

  /**
   * Разобрать описание законченной партии.
   *
   * Игра присылает один объект, и приняли мы его нарочно в том самом виде,
   * который игра УЖЕ умеет готовить для таблицы рекордов:
   *
   *   Рейтинг.записатьПартию({
   *     исход: 'победа' | 'поражение' | 'ничья',   // обязательно
   *     подпись: 'сложный · вчетвером',            // та же подпись, что у рекорда
   *     игроков: 4,                                // сколько всего за столом (2–6)
   *     парами: false,                             // игра 2 на 2
   *     соперники: 'люди' | 'боты',                // не сказали — считаем «боты»
   *     кто: 'ид соперника' или ['ид', 'ид'],      // необязательно, про запас
   *     игра: 'дурак' | 'шашки',                   // не сказали — «дурак»
   *     раздачаДня: '2026-09-07',                  // чью раздачу дня играли; обычная партия — ''
   *     ходов: 37                                  // сколько ходов заняла партия; обычная — 0
   *   })
   *
   * «раздачаДня» и «ходов» — отметки НА ПАРТИИ, а не разрез счёта: они ложатся
   * в журнал и выходят наружу в «последниеПартии», в счётчики и клетки не идут.
   *
   * Вместо «подпись» можно прислать «сложность» — это одно и то же слово.
   * Стол берём из «игроков», а если его нет — из хвоста подписи («вчетвером»).
   * Подпись без хвоста означает игру один на один: так подписаны все рекорды
   * вдвоём, и менять это правило нельзя.
   *
   * У ЖИВОГО СОПЕРНИКА УРОВНЯ НЕТ, и это не мелочь. Партия по сети всё равно
   * присылает слово «обычный» (у любой партии есть это поле), и, поверив ему,
   * мы бы дописали чужие победы в счёт побед над обычным ботом. Поэтому у живых
   * партий уровень пустой, а считаются они отдельным разрезом «с людьми» —
   * тем самым, по которому потом будет считаться общий рейтинг.
   */
  function разобратьОписаниеПартии(описание) {
    var сказано = (описание && typeof описание === 'object') ? описание : {};
    var подпись = typeof сказано.подпись === 'string' && сказано.подпись
      ? сказано.подпись
      : (typeof сказано.сложность === 'string' ? сказано.сложность : '');
    var соперники = сказано.соперники === СОПЕРНИКИ_ЛЮДИ ? СОПЕРНИКИ_ЛЮДИ : СОПЕРНИКИ_БОТЫ;

    return {
      игра: привестиИгру(сказано.игра),
      исход: привестиИсход(сказано.исход),
      уровень: соперники === СОПЕРНИКИ_ЛЮДИ
        ? ''
        : привестиПримету(привестиСложность(уровеньИзПодписи(подпись)), ДЛИНА_ПРИМЕТЫ),
      стол: столИзОписания(сказано, подпись),
      соперники: соперники,
      кто: приметаСоперников(сказано.кто),
      ходов: привестиХоды(сказано.ходов),
      раздача: деньИзИсоДаты(сказано.раздачаДня)   // номером дня; 0 — обычная партия
    };
  }

  /** Исход партии одним из трёх слов. Незнакомое слово — это пусто:
      такую партию мы просто не считаем, чтобы не портить счёт догадкой. */
  function привестиИсход(значение) {
    for (var i = 0; i < ИСХОДЫ.length; i++) {
      if (значение === ИСХОДЫ[i]) return ИСХОДЫ[i];
    }
    return '';
  }

  /** За каким столом играли: словом, как в игре. */
  function столИзОписания(сказано, подпись) {
    if (сказано.парами === true) return СТОЛ_ПАРАМИ;
    var хвост = столИзПодписи(подпись);
    if (хвост === СТОЛ_ПАРАМИ) return СТОЛ_ПАРАМИ;

    var игроков = целоеЧисло(сказано.игроков);
    if (Object.prototype.hasOwnProperty.call(СЛОВО_СТОЛА, игроков)) return СЛОВО_СТОЛА[игроков];
    if (Object.prototype.hasOwnProperty.call(ИГРОКОВ_ЗА_СТОЛОМ, хвост)) return хвост;
    return СЛОВО_СТОЛА[2];   // подпись без хвоста — это игра один на один
  }

  /** Кто сидел напротив. Пригодится будущему общему рейтингу: там важно,
      кого именно ты обыграл. Сейчас мы это только храним. */
  function приметаСоперников(значение) {
    var строка = Array.isArray(значение) ? значение.join(',') : значение;
    if (typeof строка !== 'string') return '';
    return привестиПримету(строка.slice(0, ДЛИНА_ПРИМЕТЫ_СОПЕРНИКА), ДЛИНА_ПРИМЕТЫ_СОПЕРНИКА);
  }

  // ---------------------------------------------------------------------------
  // Рейтинг: как партия ложится в счёт
  // ---------------------------------------------------------------------------

  /** Имя клетки счёта: три приметы через палку. */
  function имяКлетки(партия) {
    return партия.уровень + '|' + партия.стол + '|' + партия.соперники;
  }

  /** Клетка счёта. Клеток стало слишком много (испорченная запись) — считаем
      партию только в общий счёт: он важнее подробностей. */
  function взятьКлетку(рейтинг, имя) {
    if (Object.prototype.hasOwnProperty.call(рейтинг.клетки, имя)) return рейтинг.клетки[имя];
    if (счётСвойств(рейтинг.клетки) >= МАКСИМУМ_КЛЕТОК) return [0, 0, 0, 0];
    рейтинг.клетки[имя] = [0, 0, 0, 0];
    return рейтинг.клетки[имя];
  }

  /** Серия разреза: [сейчас, лучшая]. Правила те же, что у клеток. */
  function взятьСерию(рейтинг, имя) {
    if (Object.prototype.hasOwnProperty.call(рейтинг.серии, имя)) return рейтинг.серии[имя];
    if (счётСвойств(рейтинг.серии) >= МАКСИМУМ_СЕРИЙ) return [0, 0];
    рейтинг.серии[имя] = [0, 0];
    return рейтинг.серии[имя];
  }

  function счётСвойств(объект) {
    var сколько = 0;
    for (var имя in объект) {
      if (Object.prototype.hasOwnProperty.call(объект, имя)) сколько++;
    }
    return сколько;
  }

  /** Прибавить партию к четырём счётчикам: сыграно, побед, поражений, ничьих. */
  function прибавитьИсход(счёт, исход) {
    счёт[0]++;
    if (исход === 'победа') счёт[1]++;
    else if (исход === 'поражение') счёт[2]++;
    else счёт[3]++;
  }

  function растиСерию(серия) {
    серия[0]++;
    if (серия[0] > серия[1]) серия[1] = серия[0];
  }

  /** Погасить все текущие серии — и общую, и в каждом разрезе.
      Серия — это победы ПОДРЯД, поэтому одно поражение обрывает их все разом:
      выиграл трижды на лёгком, проиграл на сложном — серия на лёгком тоже
      кончилась, ведь между её победами теперь стоит поражение. */
  function погаситьСерии(рейтинг) {
    рейтинг.серия[0] = 0;
    for (var имя in рейтинг.серии) {
      if (Object.prototype.hasOwnProperty.call(рейтинг.серии, имя)) рейтинг.серии[имя][0] = 0;
    }
  }

  /** Сколько дней прошло от начала отсчёта до сегодня. В журнале дата лежит
      числом: так одна запись занимает вчетверо меньше места, чем «01.09.2026». */
  function деньСегодня() {
    var сейчас = new Date();
    return Math.floor((Date.UTC(сейчас.getFullYear(), сейчас.getMonth(), сейчас.getDate()) - НАЧАЛО_ОТСЧЁТА) / ДЕНЬ);
  }

  /** Обратный перевод: номер дня — в привычную дату. */
  function датаИзДня(день) {
    if (день <= 0) return '';
    var дата = new Date(НАЧАЛО_ОТСЧЁТА + день * ДЕНЬ);
    // Дни считаем от полуночи по Гринвичу — читаем их так же, иначе восточнее
    // нулевого пояса дата съезжала бы на сутки назад.
    return датаСловами(new Date(дата.getUTCFullYear(), дата.getUTCMonth(), дата.getUTCDate()));
  }

  var ИСО_ДАТА = /^(\d{4})-(\d{2})-(\d{2})$/;

  /** Номер дня из даты вида «2026-09-07» — так игра подписывает раздачу дня.
      В журнале она лежит числом: четыре знака вместо двенадцати. Не дата — ноль,
      партия обычная. Строгость нарочно: «2026-13-45» тоже превратилась бы в
      какой-то день, поэтому проверяем, что число переводится обратно в ту же строку. */
  function деньИзИсоДаты(значение) {
    if (typeof значение !== 'string') return 0;
    var строка = значение.trim();
    var части = ИСО_ДАТА.exec(строка);
    if (!части) return 0;
    var день = Math.floor((Date.UTC(+части[1], +части[2] - 1, +части[3]) - НАЧАЛО_ОТСЧЁТА) / ДЕНЬ);
    return день > 0 && исоДатаИзДня(день) === строка ? день : 0;
  }

  /** Обратно: номер дня → «2026-09-07». Ноль — пусто, раздачи дня не было. */
  function исоДатаИзДня(день) {
    if (!(день > 0)) return '';
    var дата = new Date(НАЧАЛО_ОТСЧЁТА + день * ДЕНЬ);
    var месяц = дата.getUTCMonth() + 1;
    var число = дата.getUTCDate();
    return дата.getUTCFullYear() + '-' + (месяц < 10 ? '0' : '') + месяц + '-' + (число < 10 ? '0' : '') + число;
  }

  function кодСтола(стол) {
    if (стол === СТОЛ_ПАРАМИ) return КОД_СТОЛА_ПАРАМИ;
    return Object.prototype.hasOwnProperty.call(ИГРОКОВ_ЗА_СТОЛОМ, стол) ? ИГРОКОВ_ЗА_СТОЛОМ[стол] : 0;
  }

  function столИзКода(код) {
    if (код === КОД_СТОЛА_ПАРАМИ) return СТОЛ_ПАРАМИ;
    return Object.prototype.hasOwnProperty.call(СЛОВО_СТОЛА, код) ? СЛОВО_СТОЛА[код] : '';
  }

  function кодСоперников(соперники) {
    return соперники === СОПЕРНИКИ_ЛЮДИ ? КОД_ЖИВЫХ : КОД_БОТОВ;
  }

  function соперникиИзКода(код) {
    return код === КОД_ЖИВЫХ ? СОПЕРНИКИ_ЛЮДИ : СОПЕРНИКИ_БОТЫ;
  }

  /* Уровень в журнале — число: 0 «уровня нет» (за столом живой человек),
     дальше по порядку от лёгкого к сложному. Незнакомое слово тоже даёт ноль:
     соврать про уровень хуже, чем не назвать его. */
  function кодУровня(уровень) {
    var место = ПОРЯДОК_УРОВНЕЙ.indexOf(уровень);
    return место < 0 ? 0 : место + 1;
  }

  function уровеньИзКода(код) {
    return код >= 1 && код <= ПОРЯДОК_УРОВНЕЙ.length ? ПОРЯДОК_УРОВНЕЙ[код - 1] : '';
  }

  /** Записать партию в счёт: общий итог, клетка, серии и — для живых партий — журнал. */
  function учестьПартию(рейтинг, партия) {
    прибавитьИсход(рейтинг.всего, партия.исход);
    прибавитьИсход(взятьКлетку(рейтинг, имяКлетки(партия)), партия.исход);

    if (партия.исход === 'победа') {
      растиСерию(рейтинг.серия);
      if (партия.уровень) растиСерию(взятьСерию(рейтинг, 'уровень:' + партия.уровень));
      растиСерию(взятьСерию(рейтинг, 'стол:' + партия.стол));
      растиСерию(взятьСерию(рейтинг, 'соперники:' + партия.соперники));
    } else if (партия.исход === 'поражение') {
      погаситьСерии(рейтинг);
    }
    // Ничья серию не трогает — так же, как в самой игре.

    // В журнал идут ВСЕ партии, а не только живые: без партий с ботами нельзя
    // ответить на главный вопрос игрока — «я стал играть лучше или нет».
    // Счётчики говорят, сколько всего, журнал — как дела в последнее время.
    рейтинг.партии.push(собратьЗаписьЖурнала(
      деньСегодня(),
      ИСХОДЫ.indexOf(партия.исход),
      кодСтола(партия.стол),
      кодСоперников(партия.соперники),
      кодУровня(партия.уровень),
      партия.кто, партия.ходов, партия.раздача));
    подрезатьЖурнал(рейтинг);

    рейтинг.обновлено = сегодняшняяДата();
    if (!рейтинг.начало) рейтинг.начало = рейтинг.обновлено;
  }

  // ---------------------------------------------------------------------------
  // Рейтинг: сводка для показа
  // ---------------------------------------------------------------------------

  /* Наружу уходит не сама запись, а готовая сводка: тому, кто будет рисовать
     рейтинг, не придётся ничего складывать и делить самому. Внутреннее устройство
     (клетки, номера дней, коды исходов) остаётся здесь — как и у рекордов. */

  var СКОЛЬКО_ЖИВЫХ_ПОКАЗЫВАЕМ = 20;
  var СКОЛЬКО_ПАРТИЙ_ПОКАЗЫВАЕМ = 50;   // хватит и на список, и на свой подсчёт

  /** Доля побед в целых процентах. Партий нет — ноль, а не деление на ноль. */
  function доляПобед(счёт) {
    if (счёт[0] <= 0) return 0;
    return Math.round(счёт[1] * 100 / счёт[0]);
  }

  /** Одна строка сводки. Вид у всех строк одинаковый — так их проще показывать. */
  function строкаСчёта(название, счёт, серия) {
    return {
      название: название,
      партий: счёт[0],
      побед: счёт[1],
      поражений: счёт[2],
      ничьих: счёт[3],
      доляПобед: доляПобед(счёт),
      серияСейчас: серия ? серия[0] : 0,
      лучшаяСерия: серия ? серия[1] : 0
    };
  }

  function сложитьРяды(куда, что) {
    for (var i = 0; i < куда.length; i++) куда[i] += что[i] || 0;
  }

  /**
   * Собрать привычный разрез из клеток счёта.
   * часть — какая доля имени клетки нас интересует (0 — уровень, 1 — стол,
   * 2 — соперники), приставка — под каким именем лежит серия этого разреза,
   * порядок — в каком порядке показывать знакомые имена.
   * Строк с нулём партий в сводке нет: показывать «сложный: 0 партий» незачем.
   */
  function разрезИзКлеток(рейтинг, часть, приставка, порядок) {
    var счёт = Object.create(null);
    var имена = [];

    for (var имя in рейтинг.клетки) {
      if (!Object.prototype.hasOwnProperty.call(рейтинг.клетки, имя)) continue;
      var доля = имя.split('|')[часть];
      if (!доля) continue;                  // у живых партий уровня нет — им тут не место
      if (!Object.prototype.hasOwnProperty.call(счёт, доля)) {
        счёт[доля] = [0, 0, 0, 0];
        имена.push(доля);
      }
      сложитьРяды(счёт[доля], рейтинг.клетки[имя]);
    }

    имена.sort(function (первое, второе) {
      var а = порядок.indexOf(первое), б = порядок.indexOf(второе);
      if (а >= 0 && б >= 0) return а - б;
      if (а >= 0) return -1;
      if (б >= 0) return 1;
      return счёт[второе][0] - счёт[первое][0];   // незнакомые — по числу партий
    });

    var строки = [];
    for (var i = 0; i < имена.length; i++) {
      if (счёт[имена[i]][0] <= 0) continue;
      строки.push(строкаСчёта(имена[i], счёт[имена[i]], рейтинг.серии[приставка + имена[i]]));
    }
    return строки;
  }

  /** Подробный разрез: каждая клетка отдельной строкой, самые сыгранные сверху. */
  function подробныеСтроки(рейтинг) {
    var строки = [];
    for (var имя in рейтинг.клетки) {
      if (!Object.prototype.hasOwnProperty.call(рейтинг.клетки, имя)) continue;
      var счёт = рейтинг.клетки[имя];
      if (счёт[0] <= 0) continue;
      var части = имя.split('|');
      var строка = строкаСчёта(названиеКлетки(части), счёт, null);
      строка.уровень = части[0];
      строка.стол = части[1];
      строка.соперники = части[2];
      строки.push(строка);
    }
    строки.sort(function (первая, вторая) { return вторая.партий - первая.партий; });
    return строки;
  }

  /** Человеческое название клетки: «сложный · вшестером · с ботами». */
  function названиеКлетки(части) {
    var слова = [];
    if (части[0]) слова.push(части[0]);
    if (части[1]) слова.push(части[1]);
    слова.push(части[2] === СОПЕРНИКИ_ЛЮДИ ? 'с людьми' : 'с ботами');
    return слова.join(' · ');
  }

  /** Последние партии, свежие сверху. «Чьи» — можно спросить только живые. */
  function последниеПартии(рейтинг, сколько, толькоЖивые) {
    var строки = [];
    for (var i = рейтинг.партии.length - 1; i >= 0 && строки.length < сколько; i--) {
      var запись = рейтинг.партии[i];
      if (толькоЖивые && запись[3] !== КОД_ЖИВЫХ) continue;
      строки.push({
        дата: датаИзДня(запись[0]),
        исход: ИСХОДЫ[запись[1]] || '',
        стол: столИзКода(запись[2]),
        соперники: соперникиИзКода(запись[3]),
        уровень: уровеньИзКода(запись[4]),
        кто: типСтроки(запись[5]),
        ходов: целоеЧисло(запись[6]),
        раздачаДня: исоДатаИзДня(целоеЧисло(запись[7]))   // «2026-09-07» или '' у обычной партии
      });
    }
    return строки;
  }

  function типСтроки(значение) {
    return typeof значение === 'string' ? значение : '';
  }

  function сколькоЖивыхВЖурнале(рейтинг) {
    var сколько = 0;
    for (var i = 0; i < рейтинг.партии.length; i++) {
      if (рейтинг.партии[i][3] === КОД_ЖИВЫХ) сколько++;
    }
    return сколько;
  }

  /**
   * Счёт по последним партиям из журнала — то, чем меряют «стало лучше или хуже».
   *
   * Зачем отдельно от общего счёта: доля побед за всё время меняется медленно,
   * и человек, который заметно вырос за последний месяц, этого в ней не увидит.
   * Сравнивать надо свежее с общим: «всего 45%, за последние 20 партий 60%».
   *
   * ЧЕСТНАЯ ОГОВОРКА. Журнал помнит не всё: живых партий до шестидесяти, ботовых
   * до сорока. Поэтому «партий» в этой строке — сколько РЕАЛЬНО учтено, и оно
   * может быть меньше запрошенного. Показывать надо именно это число, иначе
   * подпись соврёт.
   */
  function счётПоПоследним(рейтинг, сколько) {
    var счёт = [0, 0, 0, 0];
    var начало = Math.max(0, рейтинг.партии.length - сколько);
    for (var i = начало; i < рейтинг.партии.length; i++) {
      прибавитьИсход(счёт, ИСХОДЫ[рейтинг.партии[i][1]] || 'ничья');
    }
    return строкаСчёта('последние ' + сколько + ' партий', счёт, null);
  }

  /**
   * Сводка рейтинга — то, что уходит наружу. Что в ней:
   *   игра          — чей это счёт («дурак», «шашки»)
   *   всего         — одна строка счёта по всем партиям сразу
   *   поУровням     — строки по уровням соперника (живые партии сюда не идут)
   *   поСтолам      — строки по столам: вдвоём… вшестером и отдельно «парами»
   *   поСоперникам  — две строки: «люди» и «боты»
   *   подробно      — все клетки: «сложный · вшестером · с ботами»
   *   заПоследние20, заПоследние50 — свежий счёт из журнала: с ним и сравнивают
   *                   общую долю побед, чтобы увидеть рост («было 40, стало 60»)
   *   последниеПартии — последние партии списком, свежие сверху; у каждой —
   *                   дата, исход, стол, соперники, уровень, кто, ходов и
   *                   раздачаДня («2026-09-07» или '' у обычной партии)
   *   партийВЖурнале — сколько партий помним поимённо (обычно меньше сыгранных)
   *   живыхПартий   — из них с живыми людьми
   *   последниеЖивые — только живые партии, свежие сверху
   *   начало, обновлено — когда счёт начат и когда пополнялся
   * Все строки устроены одинаково: название, партий, побед, поражений, ничьих,
   * доляПобед (целые проценты), серияСейчас, лучшаяСерия.
   *
   * ТОЛЬКО в ответе «записатьПартию» есть ещё поле «серияДней» — сводка серии дней
   * подряд (см. «сводкаСерииДней»): партия записана, день отмечен, вот его итог.
   * У «прочитать» и «очистить» этого поля нет: серию дней им спрашивать не за чем,
   * а лишний поход в облако при каждом открытии рейтинга — это лишнее ожидание.
   */
  function сводкаРейтинга(рейтинг) {
    return {
      игра: рейтинг.игра,
      всего: строкаСчёта('всего', рейтинг.всего, рейтинг.серия),
      поУровням: разрезИзКлеток(рейтинг, 0, 'уровень:', ПОРЯДОК_УРОВНЕЙ),
      поСтолам: разрезИзКлеток(рейтинг, 1, 'стол:', ПОРЯДОК_СТОЛОВ),
      поСоперникам: разрезИзКлеток(рейтинг, 2, 'соперники:', [СОПЕРНИКИ_ЛЮДИ, СОПЕРНИКИ_БОТЫ]),
      подробно: подробныеСтроки(рейтинг),
      заПоследние20: счётПоПоследним(рейтинг, 20),
      заПоследние50: счётПоПоследним(рейтинг, 50),
      последниеПартии: последниеПартии(рейтинг, СКОЛЬКО_ПАРТИЙ_ПОКАЗЫВАЕМ, false),
      партийВЖурнале: рейтинг.партии.length,
      живыхПартий: сколькоЖивыхВЖурнале(рейтинг),
      последниеЖивые: последниеПартии(рейтинг, СКОЛЬКО_ЖИВЫХ_ПОКАЗЫВАЕМ, true),
      начало: рейтинг.начало,
      обновлено: рейтинг.обновлено
    };
  }

  // ---------------------------------------------------------------------------
  // Рейтинг: три с половиной функции наружу
  // ---------------------------------------------------------------------------

  /**
   * Записать законченную партию.
   * Довод — описание партии одним объектом (см. «разобратьОписаниеПартии»).
   * Обещание приходит со сводкой и НИКОГДА не падает: рейтинг — вещь полезная,
   * но экран итога партии из-за него сломаться не должен.
   *
   * Заодно ОТМЕЧАЕТ СЕГОДНЯШНИЙ ДЕНЬ в серии дней подряд — здесь, а не в игре,
   * чтобы ни одна дорожка (боты, сеть, шашки) не забыла это сделать. Ответ
   * отметки кладётся в сводку полем «серияДней»: игре есть что показать на экране
   * итога без второго похода в облако. Партия, которая в счёт не пошла (исход не
   * разобран), день не отмечает — и поля «серияДней» в её сводке нет.
   */
  function записатьПартиюВРейтинг(описание) {
    var партия = разобратьОписаниеПартии(описание);

    return прочитатьРейтинг(партия.игра)
      .then(function (рейтинг) {
        // Исход не разобрали — партию не считаем вовсе. Догадка испортила бы счёт
        // молча, а молчаливая порча хуже потерянной партии.
        if (!партия.исход) return сводкаРейтинга(рейтинг);
        учестьПартию(рейтинг, партия);
        // Счёт и серия дней лежат под разными ключами, поэтому пишем их разом,
        // а не по очереди: человек в это время смотрит на надпись «записываем».
        return Promise.all([записатьРейтинг(рейтинг), отметитьДень()]).then(function (ответы) {
          var сводка = сводкаРейтинга(рейтинг);
          сводка.серияДней = ответы[1];
          return сводка;
        });
      })
      .catch(function () {
        return сводкаРейтинга(пустойРейтинг(партия.игра));
      });
  }

  /** Прочитать сводку рейтинга. Игру можно не называть — тогда это «дурак». */
  function прочитатьРейтингИгры(игра) {
    var имя = привестиИгру(игра);
    return прочитатьРейтинг(имя)
      .then(сводкаРейтинга)
      .catch(function () { return сводкаРейтинга(пустойРейтинг(имя)); });
  }

  /** Начать счёт заново. Таблицу рекордов это не трогает — она живёт отдельно.
      Отметка времени — чтобы стирание пережило молчащее облако и доехало до
      других устройств (см. «выбратьРейтинг»). */
  function очиститьРейтингИгры(игра) {
    var чистый = пустойРейтинг(привестиИгру(игра));
    чистый.обнулено = Date.now();
    return записатьРейтинг(чистый)
      .then(function () { return сводкаРейтинга(чистый); })
      .catch(function () { return сводкаРейтинга(чистый); });
  }

  // ===========================================================================
  // СЕРИЯ ДНЕЙ ПОДРЯД
  // ===========================================================================

  /* Что это. Сколько дней кряду человек заходил и доигрывал хотя бы одну партию.
     Это самый сильный крючок возвращения, какой есть у игр: «играю 6 дней
     подряд» — и не хочется прерывать. Здесь только хранение и счёт; показывает
     серию сама игра (js/game.js) через две двери в «Рейтинг» — см. шапку файла.

     СЧИТАЕМ ДНИ, А НЕ ПАРТИИ. Доиграл хотя бы одну партию сегодня — день
     отмечен; вторая партия за день серию не растит. Поэтому «отметитьДень»
     можно звать после каждой партии не задумываясь.

     КТО ОТМЕЧАЕТ. Сама «записатьПартию»: раз партия пошла в счёт — день сыгран,
     и игре об этом помнить не нужно. Явная дверь «отметитьДень» оставлена для
     партий, которые в счёт не идут, — шашки вдвоём за одним телефоном: чья
     победа, сказать нельзя, а день сыгран честно.

     ОДНА НА ВЕСЬ СБОРНИК, а не на каждую игру. Крючок — «я заходил N дней
     подряд», и человеку всё равно, дурак это был или шашки: приложение в
     Telegram он видит одним. Раздельный счёт был бы слабее (два коротких
     хвоста вместо одного длинного) и удвоил бы записи в облаке. Поэтому
     довода «игра» у этих дверей нет — нарочно, чтобы не было соблазна.

     ДЕНЬ — ПО ЧАСАМ ТЕЛЕФОНА, без часовых поясов и полуночных хитростей:
     человек живёт в своём дне. Считаем день тем же числом, что и в журнале
     партий («деньСегодня»): номер календарного дня по местным часам. Отсюда
     само собой выходит честное правило: партия вчера в 23:50 и партия сегодня
     в 00:10 — это два дня подряд, серия 2. А вчера играл, сегодня нет,
     завтра сыграл — серия 1: между отметками целый день без игры.

     Как выглядит запись в хранилище (внутренний вид, наружу не торчит):
       { версия: 1, день: 2440, серия: 3, лучшая: 5 }
     день — номер дня последней отметки, серия — дней подряд на тот день,
     лучшая — лучшая серия за всё время. Больше ничего: журнал по дням не
     нужен, он уже есть в счёте партий. */

  var ВЕРСИЯ_СЕРИИ_ДНЕЙ = 1;

  /** Так выглядит серия у того, кто ещё ни разу не доиграл партию. */
  function пустаяСерияДней() {
    return { день: 0, серия: 0, лучшая: 0 };
  }

  /** Запись из текста. Мусор и запись будущей версии дают пустую серию —
      так же, как у рейтинга: показать нули лучше, чем уронить игру. */
  function разобратьСериюДней(текст) {
    if (!текст || typeof текст !== 'string') return пустаяСерияДней();
    var данные;
    try {
      данные = JSON.parse(текст);
    } catch (ошибка) {
      return пустаяСерияДней();
    }
    if (!данные || typeof данные !== 'object') return пустаяСерияДней();
    if (целоеЧисло(данные.версия) !== ВЕРСИЯ_СЕРИИ_ДНЕЙ) return пустаяСерияДней();

    var запись = {
      день: целоеЧисло(данные.день),
      серия: целоеЧисло(данные.серия),
      лучшая: целоеЧисло(данные.лучшая)
    };
    // Отметка есть, а серии нет — такого не бывает, это порча: день отмечен, значит серия хотя бы 1
    if (запись.день > 0 && запись.серия < 1) запись.серия = 1;
    if (запись.день <= 0) запись.серия = 0;
    if (запись.лучшая < запись.серия) запись.лучшая = запись.серия;
    return запись;
  }

  function текстСерииДней(запись) {
    return JSON.stringify({
      версия: ВЕРСИЯ_СЕРИИ_ДНЕЙ, день: запись.день, серия: запись.серия, лучшая: запись.лучшая
    });
  }

  /** Короткая примета записи — по ней решаем, отстало ли облако. */
  function приметаСерииДней(запись) {
    return запись.день + '|' + запись.серия + '|' + запись.лучшая;
  }

  /**
   * Свести две записи — из облака и из памяти телефона.
   *
   * СКЛАДЫВАТЬ СЕРИИ НЕЛЬЗЯ: один и тот же день, посчитанный на двух устройствах,
   * дал бы «2 дня» за один вечер. Но и просто «взять большую серию» — ловушка:
   * в облаке может лежать серия 10, оборвавшаяся месяц назад, а в памяти —
   * живая серия 2, и, взяв «большую», мы показали бы человеку 0 (десятка давно
   * мертва) и молча стёрли бы его вчера и сегодня. Это ровно то, что рвёт крючок.
   *
   * Поэтому смотрим на записи как на ОТРЕЗКИ ДНЕЙ: серия N, кончившаяся в день D,
   * означает, что дни D−N+1 … D все отмечены. Два отрезка касаются или
   * перекрываются — это одна полоса дней, и её длина — от самого раннего
   * начала до самого позднего конца, каждый день ровно по разу. Не касаются —
   * старый отрезок оборван, берём тот, что позже. Лучшая серия всегда — большая
   * из двух: она только растёт, и потерять её нельзя ни при каком расхождении.
   *
   * Правило одинаково отвечает на любой порядок доводов, поэтому два устройства
   * не переписывают облако по кругу: свели один раз — дальше сходится само.
   */
  function свестиСерииДней(облачная, местная) {
    var лучшая = Math.max(облачная.лучшая, местная.лучшая);
    if (облачная.день <= 0) return { день: местная.день, серия: местная.серия, лучшая: лучшая };
    if (местная.день <= 0) return { день: облачная.день, серия: облачная.серия, лучшая: лучшая };

    var позже = облачная.день >= местная.день ? облачная : местная;
    var раньше = позже === облачная ? местная : облачная;
    var началоПозже = позже.день - позже.серия + 1;
    var началоРаньше = раньше.день - раньше.серия + 1;

    var серия = позже.серия;
    if (раньше.день >= началоПозже - 1) {
      // Отрезки соприкасаются — одна полоса от самого раннего начала
      серия = позже.день - Math.min(началоПозже, началоРаньше) + 1;
    }
    if (серия > лучшая) лучшая = серия;
    return { день: позже.день, серия: серия, лучшая: лучшая };
  }

  function прочитатьСериюДней() {
    var местная = разобратьСериюДней(прочитатьИзПамяти(КЛЮЧ_СЕРИИ_ДНЕЙ));

    return прочитатьИзОблака(КЛЮЧ_СЕРИИ_ДНЕЙ)
      .then(function (текст) {
        var облачная = разобратьСериюДней(текст);
        var итог = свестиСерииДней(облачная, местная);
        // Облако ответило, а в нём отстало — дошлём, как рекорды и рейтинг
        if (итог.день > 0) {
          дослатьВОблако(КЛЮЧ_СЕРИИ_ДНЕЙ, приметаСерииДней(облачная), приметаСерииДней(итог),
            function () { return текстСерииДней(итог); });
        }
        return итог;
      })
      .catch(function () {
        // Облака нет, оно молчит или отказало — работаем с памятью браузера
        return местная;
      });
  }

  function записатьСериюДней(запись) {
    var текст = текстСерииДней(запись);
    // В память браузера пишем всегда: облако может молчать, а день терять нельзя
    записатьВПамять(КЛЮЧ_СЕРИИ_ДНЕЙ, текст);
    return записатьВОблако(КЛЮЧ_СЕРИИ_ДНЕЙ, текст).catch(function () { return null; });
  }

  /**
   * Сводка наружу — то, что уходит игре:
   *   серия          — дней подряд сейчас; 0, если серия оборвана
   *   лучшая         — лучшая серия за всё время
   *   сегодняОтмечен — сыграна ли уже партия сегодня
   *   сегодняВпервые — была ли эта отметка первой за день (у чтения всегда «нет»)
   *
   * Серия ЖИВА, пока последняя отметка — сегодня или вчера: вчерашнюю ещё можно
   * продолжить сегодня, и игре есть что показать («не прерви серию»). Позавчера
   * и раньше — серия оборвана, показываем ноль, хотя в записи число ещё лежит:
   * перепишется при следующей отметке. Чтение хранилище не меняет — как loadScore.
   *
   * Отметка ИЗ БУДУЩЕГО (день позже сегодняшнего) — это сбившиеся часы или второе
   * устройство в другом часовом поясе, где уже наступило завтра. Рвать за это серию
   * нечестно: считаем, что сегодня уже отмечено, и ничего не трогаем.
   */
  function сводкаСерииДней(запись, сегодня, впервые) {
    var прошло = сегодня - запись.день;
    var жива = запись.день > 0 && прошло <= 1;
    return {
      серия: жива ? запись.серия : 0,
      лучшая: запись.лучшая,
      сегодняОтмечен: запись.день > 0 && прошло <= 0,
      сегодняВпервые: впервые
    };
  }

  /**
   * Отметить сегодняшний день. Зовётся после любой доигранной партии любой игры.
   * Первая отметка за день растит серию (или начинает новую, если вчера не играли),
   * все следующие — ничего не меняют. Обещание никогда не падает.
   */
  function отметитьДень() {
    var сегодня = деньСегодня();

    return прочитатьСериюДней()
      .then(function (запись) {
        var прошло = сегодня - запись.день;
        // Сегодня уже отмечено (или отметка из будущего, см. «сводкаСерииДней») — не трогаем
        if (запись.день > 0 && прошло <= 0) return сводкаСерииДней(запись, сегодня, false);

        // Вчера играли — серия растёт; был пропуск или это первая отметка вообще — с единицы
        запись.серия = (запись.день > 0 && прошло === 1) ? запись.серия + 1 : 1;
        запись.день = сегодня;
        if (запись.серия > запись.лучшая) запись.лучшая = запись.серия;

        return записатьСериюДней(запись).then(function () {
          return сводкаСерииДней(запись, сегодня, true);
        });
      })
      .catch(function () {
        return сводкаСерииДней(пустаяСерияДней(), сегодня, false);
      });
  }

  /** Прочитать серию дней, ничего не отмечая. Для показа в меню. */
  function серияДней() {
    var сегодня = деньСегодня();
    return прочитатьСериюДней()
      .then(function (запись) { return сводкаСерииДней(запись, сегодня, false); })
      .catch(function () { return сводкаСерииДней(пустаяСерияДней(), сегодня, false); });
  }

  // ---------------------------------------------------------------------------
  // Подключение к Telegram: раскрыть окно и взять цвета темы
  // ---------------------------------------------------------------------------

  // Слева — как поле называется у Telegram, справа — переменная CSS для дизайнера
  var ЦВЕТА_ТЕМЫ = [
    ['bg_color', '--tg-theme-bg-color'],
    ['text_color', '--tg-theme-text-color'],
    ['hint_color', '--tg-theme-hint-color'],
    ['button_color', '--tg-theme-button-color'],
    ['button_text_color', '--tg-theme-button-text-color'],
    ['secondary_bg_color', '--tg-theme-secondary-bg-color']
  ];

  /**
   * Переносим цвета темы Telegram в переменные CSS — но только все шесть разом.
   *
   * ПОЧЕМУ НЕЛЬЗЯ СТАВИТЬ ЦВЕТА ПО ОДНОМУ (не «упрощайте» обратно):
   * Telegram присылает набор цветов не всегда целиком. Стоит взять из него один
   * цвет, а остальные оставить наши — получается смесь двух палитр, которую никто
   * не проверял. Живой пример: пришёл белый цвет текста кнопки, а цвет самой кнопки
   * не пришёл и остался наш янтарный — белые буквы на янтарном фоне читать
   * невозможно. Так сломались и строка подсказки, и главные кнопки.
   * Поэтому правило простое: пришли все шесть — берём тему Telegram; пришло
   * меньше — не берём ничего и оставляем свою палитру из style.css целиком,
   * она согласована сама с собой и проверена на читаемость.
   */
  function применитьТему() {
    var приложение = получитьWebApp();
    if (!приложение || !document.documentElement) return;

    var тема = приложение.themeParams || {};
    var корень = document.documentElement;

    // Сначала убеждаемся, что пришли ВСЕ цвета, и только потом что-то меняем
    for (var i = 0; i < ЦВЕТА_ТЕМЫ.length; i++) {
      var цвет = тема[ЦВЕТА_ТЕМЫ[i][0]];
      if (typeof цвет !== 'string' || !цвет) {
        забытьТемуTelegram(корень);
        return;
      }
    }

    for (var j = 0; j < ЦВЕТА_ТЕМЫ.length; j++) {
      корень.style.setProperty(ЦВЕТА_ТЕМЫ[j][1], тема[ЦВЕТА_ТЕМЫ[j][0]]);
    }
  }

  /** Снимаем ранее выставленные цвета Telegram: игра возвращается к своей палитре.
      Нужно на случай, когда полная тема сменилась неполной — чтобы не осталось
      половины старых цветов вперемешку с нашими. */
  function забытьТемуTelegram(корень) {
    for (var i = 0; i < ЦВЕТА_ТЕМЫ.length; i++) {
      корень.style.removeProperty(ЦВЕТА_ТЕМЫ[i][1]);
    }
  }

  // ---------------------------------------------------------------------------
  // Полный экран и безопасные отступы (Telegram Bot API 8.0+)
  // ---------------------------------------------------------------------------

  /* Владелец попросил «максимально возможное разрешение»: игра должна занимать
     весь экран телефона, а не окно под шапкой Telegram. С Bot API 8.0 у Telegram
     есть requestFullscreen(): шапка исчезает, страница растягивается от края
     до края. Проверено по core.telegram.org/bots/webapps (сентябрь 2026):
       requestFullscreen() / exitFullscreen() — просьба, а не приказ;
       isFullscreen — да/нет прямо сейчас;
       fullscreenChanged — вошли или вышли; fullscreenFailed — не дали,
         с причиной: UNSUPPORTED (устройство не умеет) или ALREADY_FULLSCREEN.
     На старом Telegram (до 8.0) ничего этого нет — остаётся expand(), как было.

     ЧТО ПРИ ЭТОМ ЛОЖИТСЯ НА НАШУ СТРАНИЦУ. В полном экране поверх неё
     оказываются две вещи, и обе — не наши:
       safeAreaInset        — системный слой: выемка под камеру, часы, полоса
                              жеста снизу; считается от края ЭКРАНА;
       contentSafeAreaInset — слой Telegram: его кнопки «закрыть» и «…»
                              в углу; считается от края уже безопасной
                              области, то есть ВНУТРИ первого слоя.
     Поэтому отступ от края экрана до места, где можно рисовать своё, —
     СУММА двух слоёв по каждой стороне. Мы складываем их здесь и отдаём
     оформителю четырьмя переменными CSS на :root — «--безопасно-сверху»
     и так далее. Вне Telegram и на старом Telegram там нули: страница
     выглядит ровно как раньше.

     Ориентацию не трогаем нарочно: lockOrientation() есть, но игра
     вертикальная и так. Если в полном экране на планшете стол ляжет
     набок — это отдельное решение штаба, а не тихая правка здесь. */

  var ВЕРСИЯ_С_ПОЛНЫМ_ЭКРАНОМ = '8.0';
  var ВЕРСИЯ_С_ЛЮБЫМ_ЦВЕТОМ_ШАПКИ = '6.9';   // раньше setHeaderColor принимал только два слова

  // Слева — сторона у Telegram, справа — переменная CSS для оформителя
  var ОТСТУПЫ = [
    ['top', '--безопасно-сверху'],
    ['bottom', '--безопасно-снизу'],
    ['left', '--безопасно-слева'],
    ['right', '--безопасно-справа']
  ];

  var полныйЭкран = { просили: false, отказ: '' };   // отказ — причина словами Telegram, пусто — отказа не было

  /** Корень страницы (:root), если он есть. В проверках без браузера его может не быть.
      Имя длинное нарочно: выше, в «применитьТему», словом «корень» названа
      обычная переменная, и две одинаковые приметы в одном файле путают читателя. */
  function корневойУзел() {
    try {
      return (typeof document !== 'undefined' && document.documentElement) ? document.documentElement : null;
    } catch (ошибка) {
      return null;
    }
  }

  /** Не ниже ли версия Telegram нужной. Любая осечка — «нет». */
  function версияНеНиже(приложение, версия) {
    try {
      return typeof приложение.isVersionAtLeast === 'function' && приложение.isVersionAtLeast(версия) === true;
    } catch (ошибка) {
      return false;
    }
  }

  /** Подписаться на событие Telegram, не роняя страницу, если событий там нет. */
  function слушать(приложение, имя, обработчик) {
    try {
      if (typeof приложение.onEvent === 'function') приложение.onEvent(имя, обработчик);
    } catch (ошибка) { /* не страшно */ }
  }

  /** Отступ в пикселях: целое неотрицательное, всё остальное — ноль.
      Округляем ВВЕРХ: половина пикселя под вырезом — это всё ещё вырез,
      и лучше отступить на пиксель больше, чем спрятать под ним край карты. */
  function пиксели(значение) {
    var число = Number(значение);
    return isFinite(число) && число > 0 ? Math.ceil(число) : 0;
  }

  /**
   * Отступы безопасной зоны → четыре переменные CSS на :root.
   * Складываем оба слоя (почему — в заметке выше). Зовётся при старте всегда,
   * даже вне Telegram — чтобы переменные существовали и были нулями, а не
   * «не заданы»: тогда оформитель может писать var(--безопасно-сверху) без запаски.
   */
  function применитьОтступы() {
    var узел = корневойУзел();
    if (!узел || !узел.style || typeof узел.style.setProperty !== 'function') return;
    var приложение = получитьWebApp();
    var система = (приложение && приложение.safeAreaInset) || {};
    var телеграм = (приложение && приложение.contentSafeAreaInset) || {};
    for (var i = 0; i < ОТСТУПЫ.length; i++) {
      var сторона = ОТСТУПЫ[i][0];
      узел.style.setProperty(ОТСТУПЫ[i][1], (пиксели(система[сторона]) + пиксели(телеграм[сторона])) + 'px');
    }
  }

  /** В полном ли экране сейчас. Вне Telegram — нет. */
  function вПолномЭкране() {
    var приложение = получитьWebApp();
    return !!(приложение && приложение.isFullscreen === true);
  }

  /**
   * Одна справка обо всём разом — чтобы разбирать «почему шапка не убралась»
   * не по догадкам. Открыть консоль и набрать: Телеграм.состояние().
   * Читается так:
   *   просили: false          — просьбы не было вовсе (не Telegram или версия ниже 8.0);
   *   просили: true, полный: false, отказ: 'UNSUPPORTED' — просили, но не дали;
   *   полный: true            — всё получилось;
   *   отступы: все нули в полном экране — Telegram не прислал края (тогда шапка
   *                             могла лечь поверх верхней строки игры).
   */
  function состояниеЭкрана() {
    var приложение = получитьWebApp();
    var отступы = {};
    var узел = корневойУзел();
    for (var i = 0; i < ОТСТУПЫ.length; i++) {
      отступы[ОТСТУПЫ[i][0]] = узел && узел.style ? узел.style.getPropertyValue(ОТСТУПЫ[i][1]) : '';
    }
    var вертикаль = сейчасВертикаль();
    return {
      телеграмЕсть: !!приложение,
      версия: приложение && приложение.version ? String(приложение.version) : '',
      умеетПолныйЭкран: !!(приложение && версияНеНиже(приложение, ВЕРСИЯ_С_ПОЛНЫМ_ЭКРАНОМ)),
      просили: полныйЭкран.просили,
      полный: вПолномЭкране(),
      отказ: полныйЭкран.отказ,
      отступы: отступы,
      умеетЗамокПоворота: умеемЗаперетьПоворот(),
      поворотЗаперт: поворотЗаперт(),
      ориентация: вертикаль === null ? 'не понять' : (вертикаль ? 'вертикаль' : 'горизонталь'),
      ждёмВертикаль: поворот.ждём      // телефон открыли боком, замок отложен до поворота
    };
  }

  /* -------------------------------------------------------------------
     Стрелка «Назад» в шапке Telegram

     ЧТО СЛОМАЛОСЬ И ПОЧЕМУ ДВЕРЬ ОБЩАЯ. Стрелку показывали только шашки,
     и, уходя со своей страницы, не прятали. Стрелка живёт не на странице,
     а в самом Telegram — она переживает переход и остаётся висеть на
     следующей игре. А обработчик, привязанный к ней, умирает вместе со
     страницей. Владелец так и написал: «она появилась после того, как я
     нажал на шахматы, но не работает почему-то». Человек видит кнопку
     и жмёт в пустоту.

     ПРАВИЛО, КОТОРОЕ ЧИНИТ ЭТО ПО УСТРОЙСТВУ: обработчик снимает сама
     дверь. При каждом «показать» она сперва отвязывает прежний, а при
     «спрятать» — отвязывает и прячет. Забыть нельзя: забывать нечего.

     Вне Telegram и в старых версиях стрелки нет вовсе — тогда обе двери
     тихо отвечают «нет», и страница живёт со своей кнопкой «Назад».
     ------------------------------------------------------------------- */

  var ВЕРСИЯ_СО_СТРЕЛКОЙ = '6.1';
  var делоСтрелки = null;   // что делать по нажатию; null — стрелка не наша

  /** Умеет ли этот Telegram показывать свою стрелку. */
  function стрелкаДоступна(приложение) {
    if (!приложение || !приложение.BackButton) return false;
    return версияНеНиже(приложение, ВЕРСИЯ_СО_СТРЕЛКОЙ);
  }

  /** Отвязать прежнее дело от стрелки. Внутренняя, наружу не торчит. */
  function отвязатьСтрелку(приложение) {
    if (!делоСтрелки) return;
    try {
      if (приложение && typeof приложение.offEvent === 'function') {
        приложение.offEvent('backButtonClicked', делоСтрелки);
      }
    } catch (ошибка) { /* нечего отвязывать — и хорошо */ }
    делоСтрелки = null;
  }

  /**
   * Показать стрелку и делать по ней вот это.
   * Вернёт «да», если стрелка вправду появилась.
   *
   * Прежнее дело снимается само — поэтому позвать эту дверь дважды
   * безопасно, и два обработчика на одной стрелке не заведутся никогда.
   */
  function показатьСтрелку(дело) {
    var приложение = получитьWebApp();
    if (!стрелкаДоступна(приложение) || typeof дело !== 'function') return false;
    отвязатьСтрелку(приложение);
    делоСтрелки = function () {
      try {
        дело();
      } catch (ошибка) {
        /* Беда внутри чужого обработчика не должна ломать саму стрелку:
           иначе следующее нажатие снова уйдёт в пустоту. */
        console.warn('Стрелка «Назад»: обработчик страницы упал', ошибка);
      }
    };
    try {
      if (typeof приложение.onEvent === 'function') {
        приложение.onEvent('backButtonClicked', делоСтрелки);
      }
      приложение.BackButton.show();
      return true;
    } catch (ошибка) {
      отвязатьСтрелку(приложение);
      return false;
    }
  }

  /**
   * Спрятать стрелку и отвязать дело.
   *
   * Звать обязательно там, где стрелке делать нечего: на первом экране
   * игры и перед уходом на другую страницу. Иначе она уедет туда живой
   * с виду и мёртвой на деле.
   */
  function спрятатьСтрелку() {
    var приложение = получитьWebApp();
    отвязатьСтрелку(приложение);
    if (!стрелкаДоступна(приложение)) return false;
    try {
      приложение.BackButton.hide();
      return true;
    } catch (ошибка) {
      return false;
    }
  }

  /* Уходим со страницы — прячем стрелку сами. Это вторая половина правила
     «забыть нельзя»: даже если игра забудет спрятать её перед переходом,
     следующая страница чужой стрелки не увидит. */
  try {
    window.addEventListener('pagehide', function () { спрятатьСтрелку(); });
  } catch (ошибка) { /* нет окна — нет и стрелки */ }

  /* -------------------------------------------------------------------
     Признак «открыто внутри Telegram»: класс «в-телеграме» на <html>

     ЗАЧЕМ. На эталоне витрины верхняя строка «Игры с друзьями · ⋯» — это
     шапка самого Telegram. Внутри него он рисует её сам, и наша строка
     вышла бы второй. По этому классу оформление прячет нашу.

     КАК УЗНАЁМ. Скрипт Telegram создаёт WebApp и в обычном браузере, так что
     одного его наличия мало: внутри Telegram есть подпись initData или
     площадка не «unknown» (так же решает js/сеть.js). Скрипт Telegram
     грузится с отсрочкой, поэтому сперва смотрим в адрес: при запуске
     Telegram кладёт туда tgWebAppData и tgWebAppPlatform — так строка
     не мигает, пока скрипт в пути. Класс только ставится и не снимается:
     внутри Telegram страница из него никуда не выходит.
     ------------------------------------------------------------------- */

  var КЛАСС_ТЕЛЕГРАМА = 'в-телеграме';

  /** Игра открыта внутри Telegram, а не просто в браузере. */
  function открытоВTelegram() {
    try {
      var приложение = получитьWebApp();
      if (приложение) {
        if (приложение.initData) return true;
        var площадка = String(приложение.platform || '');
        if (площадка && площадка !== 'unknown') return true;
      }
    } catch (ошибка) { /* спросить не вышло — смотрим в адрес */ }
    try {
      if (/tgWebApp(Data|Platform)=/.test(String(window.location.hash || ''))) return true;
    } catch (ошибка) { /* адреса нет — смотрим в память вкладки */ }
    /* С шашек, шахмат и нард на витрину возвращаются обычной ссылкой, без
       решётки в адресе. Но скрипт Telegram при первом запуске записал в
       память вкладки, с чем его открыли, — по ней и узнаём. Сам ключ он
       пишет и в обычном браузере (пустым «{}»), поэтому смотрим внутрь. */
    try {
      var сохранено = JSON.parse(window.sessionStorage.getItem('__telegram__initParams') || 'null');
      if (сохранено && (сохранено.tgWebAppData ||
          (сохранено.tgWebAppPlatform && сохранено.tgWebAppPlatform !== 'unknown'))) return true;
    } catch (ошибка) { /* памяти вкладки нет или в ней мусор — значит не знаем */ }
    return false;
  }

  /** Поставить класс «в-телеграме» на <html>, если игра открыта в Telegram. */
  function отметитьТелеграм() {
    var корень = корневойУзел();
    if (!корень || !корень.classList) return;
    try {
      if (открытоВTelegram()) корень.classList.add(КЛАСС_ТЕЛЕГРАМА);
    } catch (ошибка) { /* нет разметки — нечего и отмечать */ }
  }

  /* -------------------------------------------------------------------
     Пункт «Настройки» в меню ⋯ самого Telegram

     Внутри Telegram нашей строки с «⋯» не видно (см. выше), поэтому лист
     «Ещё» со звуком и рейтингом открывается пунктом «Настройки» в меню
     Telegram (SettingsButton, Bot API 7.0).

     Устроено как стрелка «Назад»: пункт живёт в самом Telegram и переживает
     переход на другую страницу, а обработчик умирает вместе со страницей.
     Поэтому дверь сама снимает прежний обработчик, а уходя со страницы,
     пункт прячем — мёртвого пункта в меню не будет.

     Одна дверь на оба дела: кнопкаНастроек(дело) — показать пункт и делать
     по нажатию это; кнопкаНастроек(null) — спрятать. Вне Telegram и в его
     версиях ниже 7.0 (там такого пункта нет) отвечает «нет» и ничего не трогает.
     ------------------------------------------------------------------- */

  var ВЕРСИЯ_С_НАСТРОЙКАМИ = '7.0';
  var делоНастроек = null;   // что делать по нажатию; null — пункт не наш

  /** Умеет ли этот Telegram показывать пункт «Настройки». */
  function настройкиДоступны(приложение) {
    if (!приложение || !приложение.SettingsButton) return false;
    return версияНеНиже(приложение, ВЕРСИЯ_С_НАСТРОЙКАМИ);
  }

  /** Отвязать прежнее дело от пункта. Внутренняя, наружу не торчит. */
  function отвязатьНастройки(приложение) {
    if (!делоНастроек) return;
    try {
      var пункт = приложение && приложение.SettingsButton;
      if (пункт && typeof пункт.offClick === 'function') пункт.offClick(делоНастроек);
    } catch (ошибка) { /* нечего отвязывать — и хорошо */ }
    делоНастроек = null;
  }

  /**
   * Показать пункт «Настройки» и делать по нему вот это; null — спрятать.
   * Вернёт «да», если пункт вправду показан.
   */
  function кнопкаНастроек(дело) {
    var приложение = получитьWebApp();
    отвязатьНастройки(приложение);
    if (!настройкиДоступны(приложение)) return false;
    try {
      if (typeof дело !== 'function') {
        приложение.SettingsButton.hide();
        return false;
      }
      делоНастроек = function () {
        try {
          дело();
        } catch (ошибка) {
          // Беда в чужом обработчике не должна ломать сам пункт.
          console.warn('Пункт «Настройки»: обработчик страницы упал', ошибка);
        }
      };
      приложение.SettingsButton.onClick(делоНастроек);
      приложение.SettingsButton.show();
      return true;
    } catch (ошибка) {
      отвязатьНастройки(приложение);
      return false;
    }
  }

  // Уходим со страницы — прячем пункт сами, как и стрелку.
  try {
    window.addEventListener('pagehide', function () { кнопкаНастроек(null); });
  } catch (ошибка) { /* нет окна — нет и пункта */ }

  /**
   * Попросить весь экран. Вернёт «да», если просьба ушла Telegram (или экран
   * уже полный), «нет» — если просить некого: не Telegram, старая версия.
   * Ответ Telegram приходит потом событием; отказ ложится в «полныйЭкран.отказ»
   * молча — в консоли от него ни строки, игрок его не видит.
   */
  function попроситьПолныйЭкран() {
    var приложение = получитьWebApp();
    if (!приложение || typeof приложение.requestFullscreen !== 'function') return false;
    if (!версияНеНиже(приложение, ВЕРСИЯ_С_ПОЛНЫМ_ЭКРАНОМ)) return false;
    if (приложение.isFullscreen === true) return true;
    полныйЭкран.просили = true;
    полныйЭкран.отказ = '';
    try {
      приложение.requestFullscreen();
      return true;
    } catch (ошибка) {
      return false;
    }
  }

  /** Вернуть шапку Telegram. Вернёт «да», если просьба ушла. */
  function выйтиИзПолногоЭкрана() {
    var приложение = получитьWebApp();
    if (!приложение || typeof приложение.exitFullscreen !== 'function') return false;
    if (!версияНеНиже(приложение, ВЕРСИЯ_С_ПОЛНЫМ_ЭКРАНОМ)) return false;
    try {
      приложение.exitFullscreen();
      return true;
    } catch (ошибка) {
      return false;
    }
  }

  /* РАЗБОР ЦВЕТА ФОНА — и почему он такой длинный.

     Telegram принимает цвет только одним видом: «#rrggbb». А браузер отдаёт
     посчитанный цвет фона РАЗНЫМИ видами, и на этом мы уже обожглись: разбор
     понимал только «rgb(…)», а наш фон в style.css собран через color-mix —
     браузер отдаёт такой цвет как «color(srgb 0.0964706 0.199216 0.18902)».
     Разбор возвращал пусто, и Telegram не узнавал наш фон НИ РАЗУ: ни на
     дураке, ни на шашках. В полном экране это не мелочь — по этому цвету
     Telegram решает, рисовать ли часы и свои кнопки поверх нас светлыми
     или тёмными, и без него они могут стать нечитаемыми.

     Поэтому разбираем все виды, которые вообще встречаются:
       rgb(12, 34, 56) и rgba(12, 34, 56, 0.5)   — привычная запись;
       rgb(12 34 56 / 50%)                        — она же, новой записью;
       color(srgb 0.09 0.19 0.18)                 — доли от 0 до 1, так браузер
                                                    отдаёт цвет из color-mix;
       #0c2238, #ccc, #0c2238ff                   — если цвет задан прямо.
     Совсем прозрачный цвет — это «фона нет», возвращаем пусто и идём искать
     фон выше по странице. Незнакомый вид тоже пусто: тогда Telegram просто
     не зовём, и хуже от этого никому не станет. */

  /** Число канала → две шестнадцатеричные цифры, с зажимом в 0…255. */
  function двеЦифры(число) {
    var целое = Math.max(0, Math.min(255, Math.round(число)));
    var знаки = целое.toString(16);
    return (знаки.length < 2 ? '0' : '') + знаки;
  }

  /** Совсем ли прозрачный: прозрачность записана и равна нулю. */
  function совсемПрозрачный(прозрачность) {
    if (прозрачность === undefined || прозрачность === '') return false;
    var текст = String(прозрачность).trim();
    var число = Number(текст.slice(-1) === '%' ? текст.slice(0, -1) : текст);
    return isFinite(число) && число === 0;
  }

  /** Канал, записанный числом 0…255 или процентами. Не число — NaN. */
  function каналИз255(значение) {
    var текст = String(значение).trim();
    if (текст.slice(-1) === '%') return Number(текст.slice(0, -1)) * 255 / 100;
    return Number(текст);
  }

  /** Канал, записанный долей 0…1 (так пишет «color(srgb …)») или процентами. */
  function каналИзДоли(значение) {
    var текст = String(значение).trim();
    if (текст.slice(-1) === '%') return Number(текст.slice(0, -1)) * 255 / 100;
    return Number(текст) * 255;
  }

  /** Разобрать запись «имя(…)» на куски. Запятые и пробелы — разделители,
      «/» отделяет прозрачность. Запись не та — null. */
  function кускиЗаписи(текст, имя) {
    if (текст.indexOf(имя + '(') !== 0 || текст.charAt(текст.length - 1) !== ')') return null;
    var внутри = текст.slice(имя.length + 1, -1);
    var половины = внутри.split('/');
    return {
      куски: половины[0].replace(/,/g, ' ').trim().split(/\s+/),
      прозрачность: половины.length > 1 ? половины[1].trim() : ''
    };
  }

  /** «#0c2238», «#ccc», «#0c2238ff» → «#0c2238». Иное — пусто. */
  function цветИзРешётки(текст) {
    if (текст.charAt(0) !== '#') return '';
    var знаки = текст.slice(1);
    if (!/^[0-9a-f]+$/.test(знаки)) return '';
    if (знаки.length === 3 || знаки.length === 4) {
      if (знаки.length === 4 && знаки.charAt(3) === '0') return '';   // прозрачный
      var развёрнутый = '#';
      for (var i = 0; i < 3; i++) развёрнутый += знаки.charAt(i) + знаки.charAt(i);
      return развёрнутый;
    }
    if (знаки.length === 6) return '#' + знаки;
    if (знаки.length === 8) return знаки.slice(6) === '00' ? '' : '#' + знаки.slice(0, 6);
    return '';
  }

  /** «rgb(12, 34, 56)», «rgba(12,34,56,.5)», «rgb(12 34 56 / 50%)» → «#0c2238». */
  function цветИзRGB(текст) {
    var разбор = кускиЗаписи(текст, 'rgb') || кускиЗаписи(текст, 'rgba');
    if (!разбор || разбор.куски.length < 3) return '';
    // В старой записи прозрачность идёт четвёртым числом, а не после «/»
    var прозрачность = разбор.прозрачность || (разбор.куски.length > 3 ? разбор.куски[3] : '');
    if (совсемПрозрачный(прозрачность)) return '';
    return собратьЦвет(разбор.куски, 0, каналИз255);
  }

  /** «color(srgb 0.09 0.19 0.18)» → «#18322f». Другие пространства не берём:
      пересчитывать их вручную — отдельная наука, а фон у нас всегда srgb. */
  function цветИзSRGB(текст) {
    var разбор = кускиЗаписи(текст, 'color');
    if (!разбор || разбор.куски.length < 4 || разбор.куски[0] !== 'srgb') return '';
    if (совсемПрозрачный(разбор.прозрачность)) return '';
    return собратьЦвет(разбор.куски, 1, каналИзДоли);
  }

  /** Три канала подряд, начиная с места «первый», → «#rrggbb». Мусор — пусто. */
  function собратьЦвет(куски, первый, вКанал) {
    var цвет = '#';
    for (var i = первый; i < первый + 3; i++) {
      var канал = вКанал(куски[i]);
      if (!isFinite(канал)) return '';
      цвет += двеЦифры(канал);
    }
    return цвет;
  }

  /** Любой вид цвета → «#rrggbb». Прозрачный или незнакомый — пусто. */
  function цветВШестнадцатеричный(значение) {
    var текст = String(значение || '').trim().toLowerCase();
    if (!текст) return '';
    return цветИзРешётки(текст) || цветИзRGB(текст) || цветИзSRGB(текст) || '';
  }

  /**
   * Подсказать Telegram цвет нашего фона. В полном экране его шапка прозрачна,
   * и по этому цвету он выбирает, какими рисовать часы и свои кнопки поверх
   * нас — светлыми или тёмными (так советует документация requestFullscreen).
   * Цвет не выдумываем: берём тот, что оформитель задал странице в style.css.
   * Считать его можно только когда стили уже применены — потому зовётся после «load».
   */
  function подсказатьЦветФона() {
    var приложение = получитьWebApp();
    if (!приложение || !версияНеНиже(приложение, ВЕРСИЯ_С_ЛЮБЫМ_ЦВЕТОМ_ШАПКИ)) return;
    if (typeof window.getComputedStyle !== 'function' || typeof document === 'undefined') return;

    var цвет = '';
    try {
      if (document.body) цвет = цветВШестнадцатеричный(window.getComputedStyle(document.body).backgroundColor);
      if (!цвет && document.documentElement) цвет = цветВШестнадцатеричный(window.getComputedStyle(document.documentElement).backgroundColor);
    } catch (ошибка) {
      return;
    }
    if (!цвет) return;
    цветФонаСтраницы = цвет;
    // Экран сейчас держит свой цвет окна (витрина) — не перебиваем его.
    // Запомненное вернётся само, когда экран свой цвет отпустит.
    if (цветЭкрана) return;

    try { if (typeof приложение.setHeaderColor === 'function') приложение.setHeaderColor(цвет); } catch (ошибка) { /* не страшно */ }
    try { if (typeof приложение.setBackgroundColor === 'function') приложение.setBackgroundColor(цвет); } catch (ошибка) { /* не страшно */ }
  }

  /* -------------------------------------------------------------------
     Цвет окна Telegram под экран: шапка, фон и нижняя полоса

     ЗАЧЕМ. Витрина тёмная, а окно Telegram вокруг неё красилось в цвет
     страницы и в цвет темы: у владельца под тёмной витриной стояла белая
     нижняя полоса (скрипт Telegram по умолчанию ставит ей цвет темы,
     а без него — #ffffff). Поэтому на витрине окно красим в её цвет,
     а уходя на экраны игр, возвращаем как было — столы не меняются.

     «КАК БЫЛО» ЗНАЕМ ТОЧНО, а не угадываем. Шапку и фон до этого красила
     только «подсказатьЦветФона» — в цвет страницы; не красила — значит
     стояли исходные ключи Telegram «bg_color». Нижнюю полосу не красил
     никто — значит у неё исходный ключ «bottom_bar_bg_color», и с ним она
     снова следует за темой.

     ВЕРСИИ (сверено с официальным telegram-web-app.js): фон — с 6.1, шапка
     цветом — с 6.9 (раньше только ключами), нижняя полоса — с 7.10. Чего
     в этом Telegram нет, того просто не просим: старый не падает и не
     сыплет предупреждениями в консоль.
     ------------------------------------------------------------------- */

  var ВЕРСИЯ_С_ЦВЕТОМ_ФОНА = '6.1';
  var ВЕРСИЯ_С_ЦВЕТОМ_ПОЛОСЫ = '7.10';
  var цветФонаСтраницы = '';   // что подсказала «подсказатьЦветФона»; пусто — не подсказывали
  var цветЭкрана = '';         // цвет, который экран поставил поверх; пусто — не ставил
  var перекрашено = null;      // какие части окна экран перекрасил: их и только их возвращаем

  /**
   * Попросить у Telegram цвета шапки, фона и полосы. null — эту часть не трогать.
   * Вернёт, какие просьбы ушли: { шапка, фон, полоса } — да/нет.
   */
  function поставитьЦветаОкна(приложение, шапка, фон, полоса) {
    var ушло = { шапка: false, фон: false, полоса: false };
    try {
      if (шапка && typeof приложение.setHeaderColor === 'function' &&
          версияНеНиже(приложение, шапка.charAt(0) === '#' ? ВЕРСИЯ_С_ЛЮБЫМ_ЦВЕТОМ_ШАПКИ : ВЕРСИЯ_С_ЦВЕТОМ_ФОНА)) {
        приложение.setHeaderColor(шапка);
        ушло.шапка = true;
      }
    } catch (ошибка) { /* не страшно */ }
    try {
      if (фон && typeof приложение.setBackgroundColor === 'function' && версияНеНиже(приложение, ВЕРСИЯ_С_ЦВЕТОМ_ФОНА)) {
        приложение.setBackgroundColor(фон);
        ушло.фон = true;
      }
    } catch (ошибка) { /* не страшно */ }
    try {
      if (полоса && typeof приложение.setBottomBarColor === 'function' && версияНеНиже(приложение, ВЕРСИЯ_С_ЦВЕТОМ_ПОЛОСЫ)) {
        приложение.setBottomBarColor(полоса);
        ушло.полоса = true;
      }
    } catch (ошибка) { /* не страшно */ }
    return ушло;
  }

  /**
   * Покрасить окно Telegram под экран.
   *   цветОкна('#101416') — шапка, фон и нижняя полоса этого цвета
   *                         (годится любая запись цвета CSS, хоть rgb());
   *   цветОкна(null)      — вернуть как было до этого.
   * Вернёт «да», если хоть одна просьба ушла Telegram.
   */
  function цветОкна(цвет) {
    var приложение = получитьWebApp();
    if (!приложение) return false;
    if (цвет) {
      var чистый = цветВШестнадцатеричный(цвет);
      if (!чистый) return false;
      цветЭкрана = чистый;
      var ушло = поставитьЦветаОкна(приложение, чистый, чистый, чистый);
      // Помним всё, что перекрашивали хоть раз: возвращать надо и это.
      перекрашено = {
        шапка: ушло.шапка || Boolean(перекрашено && перекрашено.шапка),
        фон: ушло.фон || Boolean(перекрашено && перекрашено.фон),
        полоса: ушло.полоса || Boolean(перекрашено && перекрашено.полоса)
      };
      return ушло.шапка || ушло.фон || ушло.полоса;
    }
    if (!цветЭкрана) return false;   // ничего не ставили — возвращать нечего
    цветЭкрана = '';
    var было = перекрашено || {};
    перекрашено = null;
    var прежний = цветФонаСтраницы || 'bg_color';
    var вернули = поставитьЦветаОкна(приложение,
      было.шапка ? прежний : null,
      было.фон ? прежний : null,
      было.полоса ? 'bottom_bar_bg_color' : null);
    return вернули.шапка || вернули.фон || вернули.полоса;
  }

  // ---------------------------------------------------------------------------
  // Замок поворота: стол не должен ложиться набок
  // ---------------------------------------------------------------------------

  /* ЗАЧЕМ. Вся вёрстка игры вертикальная. Пока сверху была шапка Telegram,
     поворот телефона мало кто замечал; в полном экране игра занимает весь
     экран, и стол, легший набок, ломается на глазах.

     ГЛАВНОЕ, ЧТО НАДО ЗНАТЬ ПРО lockOrientation. Документация Telegram
     (core.telegram.org/bots/webapps, Bot API 8.0) говорит дословно: метод
     «locks the Mini App’s orientation to its CURRENT mode (either portrait
     or landscape)» — то есть запирает ТУ ориентацию, в которой телефон
     находится прямо сейчас, а не вертикальную. Позвать его вслепую при
     старте нельзя: человек мог открыть игру, держа телефон боком, и мы
     заперли бы ему горизонталь намертво — сделали бы ровно ту беду,
     от которой защищаемся.

     ПОЭТОМУ ТАК. Вертикаль ли сейчас, спрашиваем у самого браузера
     (у Telegram такого поля нет). Вертикаль — запираем сразу. Бок или
     непонятно — не запираем вовсе, а тихо ждём: как только человек
     повернёт телефон стоймя, запираем в тот же миг и слежку снимаем.

     ЧЕГО ЗДЕСЬ НЕТ. Отказ Telegram не сообщает никак: события вроде
     «orientationFailed» в документации нет, есть только поле
     isOrientationLocked — по нему и проверяем, вышло или нет. */

  var ВЕРСИЯ_С_ЗАМКОМ_ПОВОРОТА = '8.0';

  // «ждём» — телефон сейчас боком, замок отложен до первого поворота стоймя;
  // «следим» — слежка за поворотом уже поставлена, второй раз не ставим.
  var поворот = { ждём: false, следим: false };

  /** Умеет ли этот Telegram запирать поворот. */
  function умеемЗаперетьПоворот() {
    var приложение = получитьWebApp();
    return !!(приложение && typeof приложение.lockOrientation === 'function' &&
      версияНеНиже(приложение, ВЕРСИЯ_С_ЗАМКОМ_ПОВОРОТА));
  }

  /**
   * Вертикально ли сейчас держат телефон: да, нет или «не понять».
   * Спрашиваем браузер, потому что Telegram про это не рассказывает.
   * Сначала прямой вопрос про ориентацию, а если такого не умеют —
   * сравниваем высоту окна с шириной, что почти то же самое.
   */
  function сейчасВертикаль() {
    try {
      if (typeof window.matchMedia === 'function') {
        var ответ = window.matchMedia('(orientation: portrait)');
        if (ответ && typeof ответ.matches === 'boolean') return ответ.matches;
      }
    } catch (ошибка) { /* не страшно, спросим иначе */ }

    var высота = Number(window.innerHeight);
    var ширина = Number(window.innerWidth);
    if (isFinite(высота) && isFinite(ширина) && высота > 0 && ширина > 0) return высота >= ширина;
    return null;   // не понять — тогда лучше ничего не запирать
  }

  /** В полном ли заперт поворот прямо сейчас (спрашиваем сам Telegram). */
  function поворотЗаперт() {
    var приложение = получитьWebApp();
    return !!(приложение && приложение.isOrientationLocked === true);
  }

  /**
   * Запереть поворот, но ТОЛЬКО если телефон сейчас стоит вертикально.
   * Вернёт «да», если замок повешен (или уже висел), «нет» — если запирать
   * нечем или сейчас не вертикаль. Бок мы намеренно не запираем: см. заметку выше.
   */
  function заперетьПоворот() {
    if (!умеемЗаперетьПоворот()) return false;
    if (поворотЗаперт()) return true;
    if (сейчасВертикаль() !== true) return false;

    try {
      получитьWebApp().lockOrientation();
      поворот.ждём = false;
      return true;
    } catch (ошибка) {
      return false;
    }
  }

  /** Отпустить поворот: экран снова следует за телефоном.
      Заодно снимаем ожидание, иначе наш же сторож запер бы обратно. */
  function отперетьПоворот() {
    поворот.ждём = false;
    var приложение = получитьWebApp();
    if (!приложение || typeof приложение.unlockOrientation !== 'function') return false;
    if (!версияНеНиже(приложение, ВЕРСИЯ_С_ЗАМКОМ_ПОВОРОТА)) return false;
    try {
      приложение.unlockOrientation();
      return true;
    } catch (ошибка) {
      return false;
    }
  }

  /** Телефон повернули — если мы ждали вертикали, пробуем запереть. */
  function приПовороте() {
    if (!поворот.ждём) return;
    заперетьПоворот();
  }

  /** Слежка за поворотом. Ставится один раз и только когда она нужна —
      то есть когда игру открыли, держа телефон боком. */
  function следитьЗаПоворотом() {
    if (поворот.следим) return;
    поворот.следим = true;

    // Прямое событие про ориентацию — самое точное
    try {
      if (typeof window.matchMedia === 'function') {
        var ответ = window.matchMedia('(orientation: portrait)');
        if (ответ && typeof ответ.addEventListener === 'function') ответ.addEventListener('change', приПовороте);
        else if (ответ && typeof ответ.addListener === 'function') ответ.addListener(приПовороте);
      }
    } catch (ошибка) { /* не страшно, ниже есть запасные */ }

    // Запасные: старое событие поворота и просто смена размера окна
    try {
      if (typeof window.addEventListener === 'function') {
        window.addEventListener('orientationchange', приПовороте);
        window.addEventListener('resize', приПовороте);
      }
    } catch (ошибка) { /* не страшно */ }
  }

  /**
   * Замок при старте игры.
   * Стоит телефон вертикально — запираем сразу. Лежит боком (или понять
   * нельзя) — ждём, пока человек повернёт его стоймя, и запираем тогда.
   */
  function заперетьВертикальПриСтарте() {
    if (!умеемЗаперетьПоворот()) return;
    if (заперетьПоворот()) return;
    поворот.ждём = true;
    следитьЗаПоворотом();
  }

  /** Подписки на всё, что меняет края экрана. Ставятся ДО просьбы о полном экране,
      чтобы не пропустить ни отказ, ни первое изменение отступов. */
  function подписатьсяНаЭкран(приложение) {
    слушать(приложение, 'fullscreenFailed', function (событие) {
      полныйЭкран.отказ = (событие && typeof событие.error === 'string') ? событие.error : 'UNKNOWN';
    });
    слушать(приложение, 'fullscreenChanged', применитьОтступы);
    слушать(приложение, 'safeAreaChanged', применитьОтступы);
    слушать(приложение, 'contentSafeAreaChanged', применитьОтступы);
    // Подстраховка: viewportChanged приходит при повороте телефона и любой
    // смене размера окна. Если клиент поменяет края молча, без своих событий,
    // мы всё равно пересчитаем — лишний пересчёт стоит четырёх присвоений.
    слушать(приложение, 'viewportChanged', применитьОтступы);

    // Оба этих события Telegram шлёт и при повороте телефона. Для замка
    // вертикали это вторая пара глаз: если браузерные события в чужом
    // окне-обёртке не дойдут, поворот мы всё равно заметим.
    слушать(приложение, 'viewportChanged', приПовороте);
    слушать(приложение, 'safeAreaChanged', приПовороте);
  }

  var ужеПодключились = false;

  /** Один раз здороваемся с Telegram: говорим «готово», раскрываем окно, берём тему,
      просим весь экран и раздаём отступы. */
  function подключитьсяКTelegram() {
    if (ужеПодключились) return true;
    var приложение = получитьWebApp();
    if (!приложение) return false;
    ужеПодключились = true;
    // Скрипт Telegram догрузился — теперь можно спросить его самого.
    отметитьТелеграм();

    try { if (typeof приложение.ready === 'function') приложение.ready(); } catch (ошибка) { /* не страшно */ }
    // expand — чтобы игра открылась во весь экран, а не висела шторкой снизу
    try { if (typeof приложение.expand === 'function') приложение.expand(); } catch (ошибка) { /* не страшно */ }

    применитьТему();
    // Игрок может переключить светлую/тёмную тему прямо во время игры
    try {
      if (typeof приложение.onEvent === 'function') приложение.onEvent('themeChanged', применитьТему);
    } catch (ошибка) { /* не страшно */ }

    // Полный экран — строго ПОСЛЕ ready() и expand(): сначала обычное окно,
    // потом просьба убрать шапку. На старом Telegram просьба просто не уйдёт.
    подписатьсяНаЭкран(приложение);
    применитьОтступы();
    попроситьПолныйЭкран();
    // Замок вертикали — после полного экрана: вёрстка у нас вертикальная,
    // и стол не должен ложиться набок от наклона телефона
    заперетьВертикальПриСтарте();

    // Цвет фона Telegram узнаёт, когда стили уже наложены; при смене темы — заново
    if (typeof document !== 'undefined' && document.readyState === 'complete') подсказатьЦветФона();
    else window.addEventListener('load', подсказатьЦветФона);
    слушать(приложение, 'themeChanged', подсказатьЦветФона);

    return true;
  }

  // ---------------------------------------------------------------------------
  // Отдача под пальцем (вибрация)
  // ---------------------------------------------------------------------------

  /* ЗАЧЕМ. Telegram умеет коротко тряхнуть телефон в руке (HapticFeedback,
     Bot API 6.1). Стоит это несколько строк, а игра сразу перестаёт быть
     картинкой под стеклом: палец чувствует, что карта легла. Особенно нужен
     отклик отказа — сейчас игра просто не пускает ход, молча, и человек
     не понимает, почему ничего не произошло.

     ВИДЫ ОТКЛИКА (их зовут по-русски: Телеграм.отклик('карта')):
       'выбор'    — палец выбрал карту, едва заметно      selectionChanged
       'лёгкий'   — самый слабый толчок вообще            impactOccurred('light')
       'карта'    — карта легла на стол, коротко и чётко  impactOccurred('medium')
       'взятка'   — побили, забрали, отбились             impactOccurred('heavy')
       'победа'   — выиграл партию                        notificationOccurred('success')
       'проигрыш' — проиграл                              notificationOccurred('error')
       'нельзя'   — так ходить нельзя                     notificationOccurred('warning')

     ТРИ ПРАВИЛА, КОТОРЫЕ ЗДЕСЬ НЕЛЬЗЯ НАРУШАТЬ.

     1. Нет Telegram — молчим. Игра открывается и в обычном браузере, и прямо
        с диска, а в старом Telegram этих методов нет вовсе. Поэтому наличие
        проверяется на каждом шаге, ошибка гасится, в консоль ничего не сыплется
        и игра из-за отклика не падает никогда.

     2. Не частим. Вибрация на каждое движение пальца превращается в дребезг
        и съедает батарею. Между откликами держим паузу (см. ПАУЗЫ ниже).

     3. Даём выключить. Кому вибрация мешает — выключает её один раз, и выбор
        помнится, как и выбор про звук. Настройка лежит в памяти этого
        устройства под латинским ключом (см. раздел «Имена ключей»): вибрация —
        свойство телефона в руке, а не игрока, и в облако ей незачем.

     ПАУЗЫ. Дорожек две, и это нарочно. По лёгкой ходят выбор и удары — они
     частые. По «вести» ходят исходы: победа, проигрыш и отказ. Будь счётчик
     один, отклик победы съела бы пауза от только что легшей карты — то есть
     пропал бы самый нужный отклик из всех. */

  var ВЕРСИЯ_С_ОТДАЧЕЙ = '6.1';   // раньше этой версии HapticFeedback в Telegram нет
  /* -------------------------------------------------------------------
     ПЕРЕЕЗД КЛЮЧА: старое имя → новое, не потеряв выбор человека

     Переименование ключа памяти СТИРАЕТ настройку: игра идёт искать новое
     имя, не находит и решает, что человек ничего не выбирал. Звук снова
     включается, подсказки возвращаются, доска становится прежней. Для
     игрока это выглядит как «игра забыла меня».

     Поэтому переименование всегда идёт парой с переносом: если под новым
     именем пусто, а под старым что-то есть — переписываем значение под
     новое имя. Старое не стираем: игрок мог открыть игру на другом
     устройстве со старой версией страницы, и пусть она там работает.

     Дверь общая на все игры (window.Телеграм.перенестиНастройку): своя
     копия этих шести строк в каждом файле однажды разъехалась бы, и одна
     из настроек переехала бы молча мимо.
     ------------------------------------------------------------------- */

  /**
   * Перенести значение со старого имени ключа на новое.
   * Отвечает «да», если и вправду что-то перенесли.
   */
  function перенестиНастройку(старое, новое) {
    if (!старое || !новое || старое === новое) return false;
    /* «Ничего нет» здесь — ПУСТАЯ СТРОКА, а не null: так отвечает
       прочитатьИзПамяти и когда ключа нет, и когда память запрещена вовсе.
       Сравнение с null молча ломало перенос: пустая строка не равна null,
       и дверь всякий раз решала, что под новым именем уже что-то лежит.
       Поймано проверкой tests/переезд-ключей-памяти.js. */
    if (прочитатьИзПамяти(новое) !== '') return false;   // новое уже есть — не трогаем
    var прежнее = прочитатьИзПамяти(старое);
    if (прежнее === '') return false;                    // переносить нечего
    записатьВПамять(новое, прежнее);
    return true;
  }

  var КЛЮЧ_ОТДАЧИ = 'haptics';    // латиницей — почему именно так, см. «Имена ключей»

  // Сколько ждать между откликами. 120 мс на выбор — палец, ведущий по вееру
  // карт, задевает соседние по несколько раз в секунду, и без паузы это сплошной
  // зуд. 50 мс на удары — это уже не дребезг, но два ложащихся подряд хода
  // ощущаются двумя. 200 мс на вести: победа и отказ приходят поодиночке,
  // а быстрые тычки в запрещённый ход не должны трястись очередью.
  var ПАУЗА_ВЫБОРА = 120;
  var ПАУЗА_УДАРА = 50;
  var ПАУЗА_ВЕСТИ = 200;

  /* Что именно просить у Telegram на каждый вид отклика.
     «дорожка» — по какому счётчику паузы идёт этот вид (см. ПАУЗЫ выше). */
  var ВИДЫ_ОТДАЧИ = {
    'выбор':    { способ: 'выбор', дорожка: 'лёгкая', пауза: ПАУЗА_ВЫБОРА },
    'лёгкий':   { способ: 'удар', сила: 'light',  дорожка: 'лёгкая', пауза: ПАУЗА_УДАРА },
    'карта':    { способ: 'удар', сила: 'medium', дорожка: 'лёгкая', пауза: ПАУЗА_УДАРА },
    'взятка':   { способ: 'удар', сила: 'heavy',  дорожка: 'лёгкая', пауза: ПАУЗА_УДАРА },
    'победа':   { способ: 'весть', исход: 'success', дорожка: 'весть', пауза: ПАУЗА_ВЕСТИ },
    'проигрыш': { способ: 'весть', исход: 'error',   дорожка: 'весть', пауза: ПАУЗА_ВЕСТИ },
    'нельзя':   { способ: 'весть', исход: 'warning', дорожка: 'весть', пауза: ПАУЗА_ВЕСТИ }
  };

  // Когда по каждой дорожке в последний раз тряхнули телефон
  var последняяОтдача = { 'лёгкая': 0, 'весть': 0 };

  // Выключатель: выключили один раз — помним. По умолчанию вибрация включена.
  var отдачаВключена = прочитатьИзПамяти(КЛЮЧ_ОТДАЧИ) !== 'off';

  /** Умеет ли этот Telegram трясти телефон. Нет — вернём null, и мы промолчим. */
  function получитьОтдачу() {
    var приложение = получитьWebApp();
    if (!приложение) return null;
    var отдача = приложение.HapticFeedback;
    if (!отдача) return null;
    // Версию спрашиваем отдельно: в старом клиенте объект бывает на месте,
    // а на вызов приходит ругань в консоль. Нам нужна чистая консоль.
    if (!версияНеНиже(приложение, ВЕРСИЯ_С_ОТДАЧЕЙ)) return null;
    return отдача;
  }

  /** Позвать нужный метод Telegram. Вернёт «да», если он вправду сработал. */
  function тряхнутьТелефон(отдача, правило) {
    try {
      if (правило.способ === 'выбор') {
        if (typeof отдача.selectionChanged !== 'function') return false;
        отдача.selectionChanged();
        return true;
      }
      if (правило.способ === 'удар') {
        if (typeof отдача.impactOccurred !== 'function') return false;
        отдача.impactOccurred(правило.сила);
        return true;
      }
      if (typeof отдача.notificationOccurred !== 'function') return false;
      отдача.notificationOccurred(правило.исход);
      return true;
    } catch (ошибка) {
      return false;   // старый клиент мог ругнуться — это не повод ронять игру
    }
  }

  /**
   * Отдача под пальцем. Зовут одним словом: отклик('карта'), отклик('нельзя').
   * Вернёт «да», если телефон вправду тряхнуло, и «нет» во всех остальных
   * случаях: вибрация выключена, слишком часто, Telegram нет, метода нет.
   * Ответ никому не обязателен — игре достаточно позвать и забыть.
   */
  function отклик(вид) {
    if (!отдачаВключена) return false;

    var имя = String(вид);
    if (!Object.prototype.hasOwnProperty.call(ВИДЫ_ОТДАЧИ, имя)) {
      // Игрок этого не увидит; разработчику подскажем, что вид назван неверно
      предупредитьОдинРаз('отдача:' + имя,
        'Отдача: вида «' + имя + '» нет. Есть: ' + Object.keys(ВИДЫ_ОТДАЧИ).join(', ') + '.');
      return false;
    }
    var правило = ВИДЫ_ОТДАЧИ[имя];

    var сейчас = Date.now();
    if (сейчас - последняяОтдача[правило.дорожка] < правило.пауза) return false;

    var отдача = получитьОтдачу();
    if (!отдача) return false;
    if (!тряхнутьТелефон(отдача, правило)) return false;

    последняяОтдача[правило.дорожка] = сейчас;
    return true;
  }

  /** Включена ли сейчас вибрация — для кнопки-выключателя в игре. */
  function вибрацияВключена() {
    return отдачаВключена;
  }

  /** Включить или выключить вибрацию. Выбор запоминается на этом устройстве. */
  function включитьВибрацию(включить) {
    отдачаВключена = включить !== false;
    записатьВПамять(КЛЮЧ_ОТДАЧИ, отдачаВключена ? 'on' : 'off');
    return отдачаВключена;
  }

  // ---------------------------------------------------------------------------
  // Оформление: рубашка карт, доска шашек, фон
  // ---------------------------------------------------------------------------

  /* ЗАЧЕМ ОДНА ДВЕРЬ НА ТРИ ВЫБОРА. Игрок выбирает рубашку карт, доску шашек
     и фон — три разных набора картинок, но правило у всех одно: выбранное слово
     ставится признаком на самый верхний элемент страницы (<html>), а всё
     остальное делают стили. Если каждый выбор написать по-своему, экран
     настроек придётся писать трижды.

     Признаки на <html> такие (их ждёт style.css):
       data-фон       — вид фона, в том числе живого («сукно», «дыхание»)
       data-рубашка   — рубашка карт («бордо»)
       data-доска     — доска шашек и шахмат («дерево»)

     Как этим пользуются страницы:
       Оформление.поставить('фон', 'дыхание')  — выбрать и запомнить
       Оформление.какое('фон')                 — что стоит сейчас
       Оформление.всё()                        — все три разом
       Оформление.загрузить()                  — подтянуть выбор из облака

     ГДЕ ЛЕЖИТ ВЫБОР. Одной записью под латинским ключом «skins» — и в облаке
     Telegram, и в памяти телефона, ровно как рекорды. Одна запись на все три
     части нарочно: три отдельные записи — это три похода в облако вместо
     одного, а выбор оформления всё равно меняют разом, в одном экране настроек.
     Про латиницу в ключе — раздел «Имена ключей»: на кириллическом ключе
     облако молча отказывает, и выбор не доехал бы до второго устройства.

     КТО ГЛАВНЕЕ, ОБЛАКО ИЛИ ТЕЛЕФОН. У рекордов их складывают, здесь складывать
     нечего: значение одно, и надо решить, какое. Поэтому рядом с выбором лежит
     время, когда его сделали, и побеждает СВЕЖИЙ. Без этого на телефоне без
     связи человек менял бы фон, а старое облачное значение возвращало бы его
     обратно при каждом открытии игры.

     ПРИЗНАК «СТРАНИЦА СКРЫТА» — про то же <html>, но не про выбор:
     data-страница="скрыта" появляется, когда игру свернули. Живой фон движется
     всё время партии, и в кармане телефон от этого греется. Браузер сам
     придерживает движение на скрытой вкладке — но Telegram не вкладка, поэтому
     мы слушаем и его собственные события «свернули/вернули». Останавливает
     движение оформитель, одной строкой в стилях. */

  var КЛЮЧ_ОФОРМЛЕНИЯ = 'skins';        // латиницей — см. «Имена ключей»
  var ВЕРСИЯ_ОФОРМЛЕНИЯ = 1;            // выросла — значит, запись пишется иначе
  var ПРИЗНАК_ВИДИМОСТИ = 'data-страница';
  var ВЕРСИЯ_С_СОБЫТИЯМИ_ОКНА = '8.0';  // раньше «свернули/вернули» Telegram не сообщал

  /* Части оформления: как их зовёт игра, каким признаком ложатся на страницу
     и под каким латинским полем хранятся. Новый выбор (скажем, вид фишек) —
     это одна строка сюда, и он сразу умеет храниться и переезжать. */
  var ЧАСТИ_ОФОРМЛЕНИЯ = {
    'фон':     { признак: 'data-фон',     поле: 'bg', известные: ключиФонов },
    'рубашка': { признак: 'data-рубашка', поле: 'back' },
    'доска':   { признак: 'data-доска',   поле: 'board' }
  };

  /* «известные» — необязательный список видов, которые у нас вправду есть.
     Он стоит только у фона: список фонов живёт здесь же (раздел «Пять фонов
     на выбор»), а рубашки и доски рисуют другие файлы, и своих списков они
     сюда не приносят — там годным считается любое короткое слово.
     Появится такой список — допишите его сюда одной строкой, и незнакомое
     слово начнёт отсекаться само, каким бы путём выбор ни пришёл. */

  /* Что считаем годным названием вида. Короткое слово из букв (можно русских),
     цифр, дефиса и подчёркивания. Правило нужно не от строгости: название едет
     прямо в разметку страницы, и пробел, кавычка или длинная строка из
     испорченной записи испортили бы вёрстку. */
  var ПРАВИЛО_ВИДА_ОФОРМЛЕНИЯ = /^[0-9A-Za-zА-Яа-яЁё_-]{1,24}$/;

  // Что выбрано прямо сейчас: часть → название вида. Пусто — «как в стилях по умолчанию».
  var оформление = {};
  var времяВыбора = 0;      // когда выбирали в последний раз (миллисекунды)
  var окноСпит = false;     // Telegram сказал, что игру свернули

  /** Годное ли название вида. Мусор, пустая строка и длинные строки — «нет». */
  function годныйВид(значение) {
    return typeof значение === 'string' && ПРАВИЛО_ВИДА_ОФОРМЛЕНИЯ.test(значение);
  }

  /** Знакомая ли часть оформления («фон», «рубашка», «доска»). */
  function частьОформления(имя) {
    var часть = String(имя);
    if (Object.prototype.hasOwnProperty.call(ЧАСТИ_ОФОРМЛЕНИЯ, часть)) return часть;
    предупредитьОдинРаз('оформление:' + часть,
      'Оформление: части «' + часть + '» нет. Есть: ' + Object.keys(ЧАСТИ_ОФОРМЛЕНИЯ).join(', ') + '.');
    return '';
  }

  /** Поставить признак части на <html> (или снять, если вид не выбран). */
  function показатьЧасть(часть) {
    var корень = корневойУзел();
    if (!корень) return;
    var признак = ЧАСТИ_ОФОРМЛЕНИЯ[часть].признак;
    try {
      if (оформление[часть]) корень.setAttribute(признак, оформление[часть]);
      else корень.removeAttribute(признак);
    } catch (ошибка) { /* без разметки живём молча — например, в проверках */ }
  }

  /** Расставить на странице все три признака разом. */
  function показатьОформление() {
    for (var часть in ЧАСТИ_ОФОРМЛЕНИЯ) {
      if (Object.prototype.hasOwnProperty.call(ЧАСТИ_ОФОРМЛЕНИЯ, часть)) показатьЧасть(часть);
    }
  }

  /** Запись оформления в том виде, в каком она ложится в хранилище. */
  function текстОформления() {
    var виды = {};
    for (var часть in ЧАСТИ_ОФОРМЛЕНИЯ) {
      if (!Object.prototype.hasOwnProperty.call(ЧАСТИ_ОФОРМЛЕНИЯ, часть)) continue;
      if (оформление[часть]) виды[ЧАСТИ_ОФОРМЛЕНИЯ[часть].поле] = оформление[часть];
    }
    return JSON.stringify({ версия: ВЕРСИЯ_ОФОРМЛЕНИЯ, виды: виды, когда: времяВыбора });
  }

  /** Разобрать запись из хранилища. Мусор и чужие поля не роняют игру. */
  function разобратьОформление(текст) {
    var пусто = { виды: {}, когда: 0 };
    if (!текст || typeof текст !== 'string') return пусто;

    var данные;
    try {
      данные = JSON.parse(текст);
    } catch (ошибка) {
      return пусто;
    }
    if (!данные || typeof данные !== 'object' || !данные.виды || typeof данные.виды !== 'object') return пусто;

    /* Здесь проверяем только вид самого слова, а не «есть ли у нас такой».
       Нарочно: запись могла прийти из облака с устройства, где игра новее
       и фонов больше. Отбросить незнакомое слово значило бы стереть человеку
       выбор при каждом открытии старой версии. Стилей на такой фон здесь нет,
       и человек увидит вид по умолчанию — но выбор его цел и вернётся,
       как только игра обновится. Незнакомое отсекается там, где человек
       выбирает руками (см. «поставитьОформление»). */
    var виды = {};
    for (var часть in ЧАСТИ_ОФОРМЛЕНИЯ) {
      if (!Object.prototype.hasOwnProperty.call(ЧАСТИ_ОФОРМЛЕНИЯ, часть)) continue;
      var значение = данные.виды[ЧАСТИ_ОФОРМЛЕНИЯ[часть].поле];
      if (годныйВид(значение)) виды[часть] = значение;
    }
    var когда = Number(данные.когда);
    return { виды: виды, когда: isFinite(когда) && когда > 0 ? когда : 0 };
  }

  /** Взять разобранную запись себе и показать её на странице. */
  function принятьОформление(запись) {
    оформление = запись.виды;
    времяВыбора = запись.когда;
    показатьОформление();
  }

  /**
   * Выбрать оформление: поставить признак на страницу и запомнить.
   * часть — 'фон', 'рубашка' или 'доска'; вид — короткое слово ('дыхание').
   * Пустая строка снимает выбор и возвращает вид по умолчанию из стилей.
   * Вернёт «да», если выбор принят, и «нет» на незнакомую часть или мусор
   * вместо названия — страница при этом не ломается.
   */
  function поставитьОформление(часть, вид) {
    var имя = частьОформления(часть);
    if (!имя) return false;

    /* Снять выбор можно двумя способами — пустой строкой и «ничем» (null).
       А вот число или объект вместо названия — это не «снять», это ошибка
       в коде: молча вернуть вид по умолчанию значило бы спрятать её. */
    if (вид !== null && вид !== undefined && typeof вид !== 'string') {
      предупредитьОдинРаз('вид-не-строка',
        'Оформление: названием вида должно быть слово, а пришло ' + typeof вид + '. Выбор не изменён.');
      return false;
    }

    var значение = typeof вид === 'string' ? вид.trim() : '';
    if (значение && !годныйВид(значение)) {
      предупредитьОдинРаз('вид:' + значение,
        'Оформление: название «' + значение + '» не годится (нужно короткое слово ' +
        'из букв, цифр, дефиса или подчёркивания). Выбор не изменён.');
      return false;
    }

    /* Вид, которого у нас нет, не принимаем — и делаем это ЗДЕСЬ, а не в двери
       выбора фона. Проверка, стоящая на одном из двух путей, рано или поздно
       обходится: игра может позвать «Оформление.поставить» напрямую, и мусор
       прошёл бы мимо. Теперь отсекается всегда, кем бы дверь ни звали. */
    var известные = ЧАСТИ_ОФОРМЛЕНИЯ[имя].известные;
    if (значение && известные && известные().indexOf(значение) === -1) {
      предупредитьОдинРаз('нет-вида:' + имя + ':' + значение,
        'Оформление: у части «' + имя + '» нет вида «' + значение + '». ' +
        'Есть: ' + известные().join(', ') + '. Выбор не изменён.');
      return false;
    }

    if (значение) оформление[имя] = значение;
    else delete оформление[имя];
    времяВыбора = Date.now();
    показатьЧасть(имя);
    сохранитьОформление();
    return true;
  }

  /** Записать выбор: в память телефона сразу, в облако — следом и молча. */
  function сохранитьОформление() {
    var текст = текстОформления();
    записатьВПамять(КЛЮЧ_ОФОРМЛЕНИЯ, текст);
    записатьВОблако(КЛЮЧ_ОФОРМЛЕНИЯ, текст).catch(function () {
      // Облака нет или оно молчит — не беда: в памяти телефона выбор уже лежит,
      // а в облако он уедет при следующем открытии игры (см. «загрузитьОформление»)
      return null;
    });
  }

  /** Какой вид выбран сейчас. Не выбрано ничего — пустая строка. */
  function какоеОформление(часть) {
    var имя = частьОформления(часть);
    if (!имя) return '';
    return оформление[имя] || '';
  }

  /** Все три выбора разом: { фон: '…', рубашка: '…', доска: '…' }. */
  function всёОформление() {
    var ответ = {};
    for (var часть in ЧАСТИ_ОФОРМЛЕНИЯ) {
      if (Object.prototype.hasOwnProperty.call(ЧАСТИ_ОФОРМЛЕНИЯ, часть)) ответ[часть] = оформление[часть] || '';
    }
    return ответ;
  }

  /**
   * Подтянуть выбор из облака: так оформление переезжает на второе устройство.
   * Побеждает свежий выбор — у кого время больше (почему так, см. заметку выше).
   * Обещание никогда не падает: нет облака или оно молчит — остаёмся на своём.
   * Зовётся само при старте, страницам звать не обязательно.
   */
  function загрузитьОформление() {
    return прочитатьИзОблака(КЛЮЧ_ОФОРМЛЕНИЯ)
      .then(function (текст) {
        var облачное = разобратьОформление(текст);
        if (облачное.когда > времяВыбора) {
          принятьОформление(облачное);
          // Раз облачное свежее, положим его и в память телефона: без связи
          // человек увидит тот же вид, а не тот, что был до переезда
          записатьВПамять(КЛЮЧ_ОФОРМЛЕНИЯ, текстОформления());
        } else if (времяВыбора > 0) {
          // Наш выбор новее — тихо дошлём его в облако (ответа не ждём)
          дослатьВОблако(КЛЮЧ_ОФОРМЛЕНИЯ, текст, текстОформления(), текстОформления);
        }
        return всёОформление();
      })
      .catch(function () {
        return всёОформление();   // облака нет — работаем на памяти телефона
      });
  }

  // ---------------------------------------------------------------------------
  // Пять фонов на выбор: список для экрана «Оформление»
  // ---------------------------------------------------------------------------

  /* ЗАЧЕМ ОТДЕЛЬНАЯ ДВЕРЬ, ЕСЛИ ВЫШЕ УЖЕ ЕСТЬ «ОФОРМЛЕНИЕ». Оформление умеет
     поставить любой вид, но не знает, какие виды бывают: их рисует style.css.
     Экрану выбора нужен готовый список — что показать человеку и как это
     назвать по-русски. Список живёт здесь, в одном месте на обе игры: иначе
     каждая игра завела бы свой, и новый фон пришлось бы вписывать дважды.

     ХРАНЕНИЯ ЗДЕСЬ НЕТ ВОВСЕ. «выбрать» зовёт ту же дверь Оформления, что
     и любой другой выбор вида. Второе хранилище рядом с первым — верный способ
     однажды разойтись: на экране одно, на столе другое.

     КЛЮЧИ ОБЯЗАНЫ СЛОВО В СЛОВО СОВПАДАТЬ с правилами в style.css
     (там они записаны как html[data-фон="сукно"] и так далее). Разойдутся —
     фон молча не переключится: ни ошибки, ни следа в консоли, просто нажатие
     без ответа. Это худший вид поломки, поэтому совпадение ключей проверяется
     отдельно (tests/оформление-и-видимость.js читает style.css и сверяет).

     ПОЧЕМУ «СУКНО» ПЕРВЫМ И ПО УМОЛЧАНИЮ. Это неподвижный фон — то, что человек
     видит, пока ничего не выбрал. Движение можно предложить, но навязывать его
     нельзя: кому-то оно мешает, кому-то садит батарею на слабом телефоне. */

  var ФОН_ПО_УМОЛЧАНИЮ = 'сукно';

  var СПИСОК_ФОНОВ = [
    { ключ: 'сукно',   имя: 'Сукно' },            // неподвижный, стоит по умолчанию
    { ключ: 'дыхание', имя: 'Дыхание' },          // два больших пятна медленно расходятся
    { ключ: 'лампа',   имя: 'Лампа над столом' }, // свет сверху, очень медленно набирает силу
    { ключ: 'туман',   имя: 'Туман' },            // два слоя мягких полос ползут в разные стороны
    { ключ: 'отсвет',  имя: 'Северный отсвет' }   // холодное свечение вдоль верхнего края
  ];

  /**
   * Кусочек фона для кнопки выбора.
   *
   * Живой фон на кнопке размером с палец честно не показать, да и не нужно:
   * человеку хватит клочка, на котором видно цвет и характер. Поэтому здесь
   * СТРОГО РАЗМЕТКА, без единого цвета и размера: узел с признаком data-фон
   * и вложенным слоем, каким фон рисуется на самой странице. Рисует его
   * целиком style.css — так образец не разойдётся с настоящим фоном
   * и подхватит тему Telegram, чего вписанные из кода цвета не умеют.
   *
   * Нет разметки вовсе (проверки без браузера) — вернём null, и это нормально.
   */
  function образецФона(ключ) {
    try {
      if (typeof document === 'undefined' || typeof document.createElement !== 'function') return null;
      var клочок = document.createElement('div');
      клочок.className = 'образец-фона';
      клочок.setAttribute('data-фон', ключ);
      клочок.setAttribute('aria-hidden', 'true');   // картинка без слов, читалке экрана не нужна
      var слой = document.createElement('div');
      слой.className = 'фон-игры';
      клочок.appendChild(слой);
      уважитьРазмерОбразца(клочок);
      return клочок;
    } catch (ошибка) {
      return null;
    }
  }

  /**
   * Понимать размер, заданный атрибутами width и height.
   *
   * Экран выбора задаёт размер образцов одинаково для рубашек и фонов —
   * атрибутами, как это принято у рисунков (SVG). У рубашки образец и есть
   * рисунок, а у фона — обычный узел, и для него такие атрибуты пустой звук:
   * без этой стыковки клочок фона схлопнулся бы в полоску нулевой высоты.
   * Поэтому размер, пришедший атрибутом, переносим в стиль узла.
   * Не пришёл — ничего не навязываем: пусть решает style.css.
   */
  function уважитьРазмерОбразца(узел) {
    var родной = узел.setAttribute;
    узел.setAttribute = function (имя, значение) {
      родной.call(узел, имя, значение);
      var число = Number(значение);
      if (!isFinite(число) || число <= 0) return;
      if (имя === 'width') узел.style.width = число + 'px';
      if (имя === 'height') узел.style.height = число + 'px';
    };
  }

  /** Список фонов для экрана выбора: [{ ключ, имя, образец() }, …], «Сукно» первым.
      Каждый раз собирается заново — чтобы наш список нельзя было испортить снаружи. */
  function списокФонов() {
    return СПИСОК_ФОНОВ.map(function (фон) {
      return {
        ключ: фон.ключ,
        имя: фон.имя,
        образец: function () { return образецФона(фон.ключ); }
      };
    });
  }

  /** Ключи всех наших фонов. Этим списком Оформление отсекает незнакомый вид —
      см. поле «известные» в разделе выше. */
  function ключиФонов() {
    return СПИСОК_ФОНОВ.map(function (фон) { return фон.ключ; });
  }

  /** Есть ли у нас такой фон. Незнакомый ключ ничего не ломает — просто «нет». */
  function знакомыйФон(ключ) {
    return ключиФонов().indexOf(ключ) !== -1;
  }

  /**
   * Выбрать фон. Всё делает дверь Оформления: и проверяет, что такой фон
   * у нас есть, и ставит признак на страницу, и запоминает выбор.
   * Своей проверки здесь нарочно нет — она была бы вторым, отдельным
   * решением о том же самом, а два таких решения однажды разойдутся.
   * Вернёт «да», если фон принят, «нет» — на незнакомый ключ или мусор.
   */
  function выбратьФон(ключ) {
    return поставитьОформление('фон', ключ);
  }

  /** Какой фон стоит сейчас. Ничего не выбрано — «сукно», оно и рисуется. */
  function нынешнийФон() {
    var выбран = какоеОформление('фон');
    return знакомыйФон(выбран) ? выбран : ФОН_ПО_УМОЛЧАНИЮ;
  }

  // ---------------------------------------------------------------------------
  // Признак «страница скрыта»
  // ---------------------------------------------------------------------------

  /** Скрыта ли игра сейчас: свёрнута вкладка браузера или свёрнут сам Telegram. */
  function страницаСкрыта() {
    if (окноСпит) return true;
    try {
      if (typeof document === 'undefined' || !document) return false;
      if (document.hidden === true) return true;
      return document.visibilityState === 'hidden';
    } catch (ошибка) {
      return false;
    }
  }

  /** Поставить или снять data-страница="скрыта" на <html>. */
  function отметитьВидимость() {
    var корень = корневойУзел();
    if (!корень) return;
    try {
      if (страницаСкрыта()) корень.setAttribute(ПРИЗНАК_ВИДИМОСТИ, 'скрыта');
      else корень.removeAttribute(ПРИЗНАК_ВИДИМОСТИ);
    } catch (ошибка) { /* нет разметки — нечего и отмечать */ }
  }

  /** Слежка за тем, свёрнута игра или нет: и по-браузерному, и по-телеграмному. */
  function следитьЗаВидимостью() {
    отметитьВидимость();

    try {
      if (typeof document !== 'undefined' && typeof document.addEventListener === 'function') {
        document.addEventListener('visibilitychange', отметитьВидимость);
      }
    } catch (ошибка) { /* не страшно, ниже есть телеграмные события */ }

    // Telegram — не вкладка браузера: свёрнутое мини-приложение для страницы
    // остаётся «видимым». Про это он сообщает своими событиями (Bot API 8.0).
    var приложение = получитьWebApp();
    if (!приложение || !версияНеНиже(приложение, ВЕРСИЯ_С_СОБЫТИЯМИ_ОКНА)) return;
    слушать(приложение, 'activated', function () { окноСпит = false; отметитьВидимость(); });
    слушать(приложение, 'deactivated', function () { окноСпит = true; отметитьВидимость(); });
  }

  // Переменные отступов должны существовать всегда — вне Telegram это нули
  применитьОтступы();

  /* Признак «в Telegram» — как можно раньше: по адресу сразу, по самому
     скрипту Telegram — когда он догрузится (он с отсрочкой и готов к
     DOMContentLoaded). Пункт в подключении ниже — третья, запасная попытка. */
  отметитьТелеграм();
  try {
    if (typeof document !== 'undefined' && document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', отметитьТелеграм);
    }
  } catch (ошибка) { /* нет страницы — нечего отмечать */ }

  if (!подключитьсяКTelegram()) {
    // Скрипт Telegram мог ещё не догрузиться — пробуем ещё раз, когда страница будет готова
    window.addEventListener('load', function () { подключитьсяКTelegram(); });
  }

  /* Оформление ставим сразу из памяти телефона — чтобы страница не мигнула
     чужим видом, пока идёт ответ облака. Облако спрашиваем следом: если
     на другом устройстве выбрали свежее, оно применится само. */
  принятьОформление(разобратьОформление(прочитатьИзПамяти(КЛЮЧ_ОФОРМЛЕНИЯ)));
  загрузитьОформление();
  следитьЗаВидимостью();

  // Наружу отдаём ровно три функции — это весь договор с игрой
  window.isReady = isReady;
  window.saveScore = saveScore;
  window.loadScore = loadScore;

  /* Личный рейтинг — отдельная дверь, рядом с договором и мимо него.
     Собран одним объектом нарочно: три имени договора известны всей игре
     наизусть, и новые общие слова («прочитать», «очистить») рядом с ними
     стали бы ловушкой — легко перепутать, чьё это. */
  window.Рейтинг = {
    готов: isReady,                          // есть ли куда писать (то же да/нет, что у рекордов)
    записатьПартию: записатьПартиюВРейтинг,  // записать законченную партию, вернёт сводку
    прочитать: прочитатьРейтингИгры,         // сводка для показа
    очистить: очиститьРейтингИгры,           // начать счёт заново (серию дней не трогает)
    отметитьДень: отметитьДень,              // серия дней подряд: отметить сегодня, вернёт сводку
    серияДней: серияДней                     // серия дней подряд: та же сводка без отметки
  };

  /* Экран — третья дверь. Обе страницы (дурак и шашки) подключают этот файл,
     поэтому весь экран у них появляется сам при старте, звать ничего не надо.
     Дверь нужна на три случая: страница хочет вернуть шапку (например, чтобы
     показать длинный список), спросить, полный ли сейчас экран, или разобраться,
     почему полного экрана нет. */
  window.Телеграм = {
    полныйЭкран: попроситьПолныйЭкран,       // попросить весь экран ещё раз; «да» — просьба ушла
    обычныйЭкран: выйтиИзПолногоЭкрана,      // вернуть шапку Telegram; «да» — просьба ушла
    вПолномЭкране: вПолномЭкране,            // да/нет прямо сейчас
    обновитьОтступы: применитьОтступы,       // пересчитать «--безопасно-…» руками, если понадобится
    заперетьПоворот: заперетьПоворот,        // запереть вертикаль (боком не запирает — см. раздел «Замок поворота»)
    отперетьПоворот: отперетьПоворот,        // отпустить поворот: экран снова следует за телефоном
    поворотЗаперт: поворотЗаперт,            // да/нет прямо сейчас
    /* Переезд ключа памяти на новое имя, не потеряв выбор человека.
       Нужна всем играм: имена ключей сводятся к латинице, а латиница —
       единственное, что принимает облако Telegram. */
    перенестиНастройку: перенестиНастройку,
    /* Стрелка «Назад» в шапке Telegram — одна на все игры сборника.
       Дверь сама снимает прежний обработчик, поэтому мёртвой стрелки,
       переехавшей с чужой страницы, больше быть не может. */
    показатьСтрелку: показатьСтрелку,     // показатьСтрелку(дело) — «да», если появилась
    спрятатьСтрелку: спрятатьСтрелку,     // спрятать и отвязать дело
    /* Пункт «Настройки» в меню ⋯ Telegram — там открывается лист «Ещё»,
       потому что нашей строки с «⋯» внутри Telegram не видно. */
    кнопкаНастроек: кнопкаНастроек,       // кнопкаНастроек(дело) — показать; кнопкаНастроек(null) — спрятать
    /* Цвет шапки, фона и нижней полосы Telegram под экран — чтобы под
       тёмной витриной не стояла белая полоса. */
    цветОкна: цветОкна,                   // цветОкна('#101416') — покрасить; цветОкна(null) — вернуть как было
    состояние: состояниеЭкрана,              // справка для разбора: экран, отступы, замок поворота
    // Отдача под пальцем — см. раздел «Отдача под пальцем (вибрация)»
    отклик: отклик,                          // тряхнуть телефон: 'выбор', 'карта', 'взятка', 'победа', 'проигрыш', 'нельзя'
    вибрацияВключена: вибрацияВключена,      // да/нет — для кнопки-выключателя
    включитьВибрацию: включитьВибрацию       // включить (true) или выключить (false), выбор запоминается
  };

  /* Оформление — четвёртая дверь: рубашка карт, доска и фон.
     Отдельно от «Телеграм» нарочно: выбор вида — про саму страницу, а не про
     Telegram; облако здесь только способ довезти его до второго устройства.
     Подробности — в разделе «Оформление: рубашка карт, доска шашек, фон». */
  window.Оформление = {
    поставить: поставитьОформление,   // выбрать вид: поставить('фон', 'дыхание')
    какое: какоеОформление,           // что стоит сейчас: какое('фон')
    всё: всёОформление,               // все три разом: { фон, рубашка, доска }
    загрузить: загрузитьОформление,   // подтянуть выбор из облака (зовётся само при старте)
    страницаСкрыта: страницаСкрыта    // свёрнута ли игра прямо сейчас
  };

  /* Фоны — пятая дверь, для экрана «Оформление» в обеих играх.
     Своего хранения у неё нет: «выбрать» зовёт Оформление.поставить('фон', …).
     Подробности — в разделе «Пять фонов на выбор». */
  window.ОбщийФон = {
    фоны: списокФонов,      // [{ ключ, имя, образец() }, …], «Сукно» первым
    выбрать: выбратьФон,    // выбрать('дыхание'); незнакомый ключ — «нет», и ничего не ломается
    сейчас: нынешнийФон     // какой фон стоит; ничего не выбрано — «сукно»
  };
})();
