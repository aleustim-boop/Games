# Подкидной дурак — игра для Telegram

Карточная игра «Подкидной дурак» против бота. Открывается прямо внутри Telegram
по кнопке «Играть» — ничего скачивать и устанавливать не нужно. Играть можно и просто
в браузере на компьютере.

Есть два режима: против бота (работает сразу и всегда) и вдвоём с живым другом
по коду из четырёх знаков — для него нужно запустить сервер комнат, см. раздел 6.

Рекорды (самая длинная серия побед подряд) сохраняются сами: внутри Telegram —
в вашем личном облаке Telegram, поэтому они видны и на телефоне, и на планшете;
в обычном браузере — в памяти самого браузера.

Никаких платных сервисов и своей базы данных проекту не нужно.

---

## 1. Как получить файлы игры и открыть её у себя на компьютере

### Сначала скачайте файлы

Если папки с игрой у вас на компьютере ещё нет, возьмите её с GitHub — это бесплатное
хранилище, где лежит проект:

1. Откройте страницу проекта на <https://github.com>.
2. Нажмите зелёную кнопку **Code** (в правой части, над списком файлов).
3. В выпавшем окошке выберите **Download ZIP** — начнётся скачивание архива.
4. Найдите скачанный файл (обычно папка «Загрузки»), щёлкните по нему правой кнопкой
   и выберите «Извлечь всё» («Распаковать»). Появится обычная папка с файлами.

Внутри должны быть `index.html`, `style.css` и папки `js`, `bot`, `server`, `tests`.
Это и есть «папка проекта», о которой говорится дальше.

### Теперь откройте игру

Самый простой способ: найдите в папке проекта файл `index.html` и откройте его
двойным щелчком. Игра запустится в браузере.

Если хочется «как в интернете» (некоторые браузеры строже относятся к файлам с диска),
запустите маленький встроенный сервер. Для этого нужен Node.js — бесплатная программа
для запуска таких скриптов, скачивается на <https://nodejs.org> (берите версию LTS).

Откройте командную строку в папке проекта и наберите:

```
node tests/локальный-сервер.js 8123
```

Затем откройте в браузере адрес <http://127.0.0.1:8123/index.html>.
Чтобы остановить сервер, нажмите в командной строке Ctrl+C.

---

## 2. Как создать своего бота в Telegram

Бот — это «дверь» в игру: он показывает кнопку «Играть».

1. Откройте Telegram и найдите бота **@BotFather** (это официальный бот Telegram
   для создания других ботов).
2. Напишите ему команду `/newbot`.
3. Он спросит название бота (любое, например «Подкидной дурак») и имя-адрес,
   которое должно заканчиваться на `bot` — например `moy_durak_bot`.
4. В ответ BotFather пришлёт **токен** — длинную строку из двух частей через двоеточие,
   вроде `1234567890:AA...`. Это пароль от вашего бота.

**Токен никому не показывайте и никуда, кроме файла настроек, не вставляйте.**
Если случайно показали — напишите BotFather команду `/revoke`, и он выдаст новый.

---

## 3. Куда положить токен

Токен лежит в одном-единственном файле: `bot/.env`. Этот файл не попадает
в репозиторий (в общее хранилище кода) — так и задумано.

1. Зайдите в папку `bot`.
2. Скопируйте файл `.env.example` и назовите копию просто `.env` (без «.example»).
3. Откройте `.env` любым текстовым редактором (подойдёт «Блокнот») и заполните две строки:

```
BOT_TOKEN=сюда вставьте свой токен от BotFather
GAME_URL=https://ваш-логин.github.io/Games/
```

`GAME_URL` — это адрес, по которому игра опубликована в интернете (как его получить,
написано в разделе 5). Он обязательно должен начинаться с `https://`:
Telegram открывает игру только по защищённому адресу.

Кавычки, пробелы вокруг знака «равно» и лишние переносы строк не нужны.

---

## 4. Как запустить бот-лаунчер

Нужен Node.js (см. раздел 1). Откройте командную строку в папке проекта и наберите:

```
node bot/бот.js
```

Если всё заполнено правильно, вы увидите примерно такое:

```
Бот запущен: @moy_durak_bot
Адрес игры:  https://ваш-логин.github.io/Games/
Напишите боту /start в Telegram. Остановить бота — Ctrl+C.
```

Теперь найдите своего бота в Telegram, напишите ему `/start` — придёт приветствие
с двумя кнопками:

- **Играть** — открывает игру прямо внутри Telegram;
- **Позвать друга** — Telegram покажет список ваших чатов; выберите человека,
  и ему уйдёт приглашение попробовать игру. Это именно приглашение: чтобы сесть
  за один стол вдвоём, нужен сервер комнат и код игры — см. раздел 6.

Пока команда `node bot/бот.js` работает в окне командной строки — бот отвечает.
Закрыли окно или нажали Ctrl+C — бот замолчал. Игра при этом продолжает быть
доступной по своему адресу.

Если что-то заполнено не так, программа не «упадёт» с непонятной ошибкой,
а прямо напишет по-русски, чего ей не хватает.

### Чтобы заработала кнопка «Позвать друга»

Один раз разрешите боту работать в режиме подсказок:

1. Напишите @BotFather команду `/setinline`.
2. Выберите своего бота.
3. На вопрос о подсказке ответьте любым текстом, например: `позвать друга`.

Без этого шага Telegram при выборе чата напишет «ничего не найдено».

---

## 5. Как опубликовать игру бесплатно (GitHub Pages)

GitHub Pages — бесплатная площадка, которая раздаёт обычные страницы по адресу
с `https`. Именно такой адрес и нужен Telegram.

1. Заведите бесплатную учётную запись на <https://github.com>.

2. Создайте репозиторий (папку-хранилище): кнопка **New** на главной странице
   или знак **+** в правом верхнем углу → **New repository**. Имя можно любое,
   например `Games`.

   **Обязательно выберите тип «Public» (открытый).** Это самое важное место:
   на бесплатном тарифе GitHub Pages раздаёт страницы только из открытых
   репозиториев. Если выбрать «Private» (закрытый), нужного пункта в настройках
   просто не окажется, и будет непонятно, почему ничего не работает.
   Открытый репозиторий не опасен: токен бота лежит в файле `bot/.env`,
   а он в репозиторий не попадает.

3. Загрузите файлы игры в репозиторий:

   - на странице репозитория нажмите **Add file** → **Upload files**;
   - откройте на компьютере папку с игрой, выделите всё её содержимое
     (`index.html`, `style.css`, папки `js`, `bot`, `server`, `tests`, файл `README.md`)
     и перетащите мышью в окно браузера, в поле с надписью
     «Drag files here to add them to your repository»;
   - дождитесь, пока все файлы появятся списком (папки загружаются целиком);
   - прокрутите страницу вниз и нажмите зелёную кнопку **Commit changes**
     («сохранить изменения»).

   Перетаскивать нужно **содержимое** папки, а не саму папку целиком: иначе
   `index.html` окажется на уровень глубже и адрес игры будет другим.

4. В репозитории откройте **Settings** (Настройки) → в левом меню **Pages**.
   Если пункта «Pages» нет — репозиторий закрытый (Private). Вернитесь в Settings,
   пролистайте в самый низ, найдите «Change repository visibility»
   и сделайте его открытым (Public).
5. В разделе «Build and deployment» → «Source» выберите **Deploy from a branch**,
   ветку **main** и папку **/ (root)**. Нажмите **Save**.
6. Подождите одну-две минуты и обновите страницу. Наверху появится адрес вида:

```
https://ваш-логин.github.io/Games/
```

7. Откройте этот адрес в браузере — должна открыться игра.
8. Впишите этот адрес в файл `bot/.env` в строку `GAME_URL=` и перезапустите бота.

Важно: файл `bot/.env` с токеном на GitHub не загружается — он специально исключён.
Проверьте, что в списке файлов на GitHub его действительно нет.

### Как потом обновлять игру: правило про номер версии

Это правило легко пропустить, а без него игроки неделями видят старую игру.

Браузеры (а Telegram — особенно упорно) запоминают файлы игры и потом берут их
из своей запоминалки, не спрашивая сервер заново. Чтобы этого не случилось,
в файле `index.html` к именам файлов приписан номер версии:

```html
<link rel="stylesheet" href="style.css?v=ЧИСЛО">
<script src="js/telegram.js?v=ЧИСЛО"></script>
<script src="js/game.js?v=ЧИСЛО"></script>
<script src="js/сеть.js?v=ЧИСЛО"></script>
```

Вместо слова `ЧИСЛО` в настоящем `index.html` стоят цифры, и у каждого файла своя.
**Какие именно там числа сейчас — неважно, и приводить их к одинаковым не нужно.**
Ничего не исправляйте по этому примеру: он показывает только, как строка устроена.

**Правило одно: изменили `style.css`, `js/game.js`, `js/telegram.js` или `js/сеть.js` — увеличьте
на единицу то число, которое стоит у этого файла сейчас.** Стояло 5 — поставьте 6.
Делать это нужно в `index.html` до загрузки на GitHub, и только у того файла,
который вы правили. Уменьшать числа нельзя: для браузера это выглядит как
возврат к старой версии, и игроки снова увидят старую игру.

Проверить просто: откройте опубликованный адрес, и правка должна быть видна сразу,
без очистки истории браузера. Если правки не видно — почти всегда забыт номер версии.
Приписывать `?v=` к адресу самой игры бесполезно: номер должен стоять именно
в этих строках внутри `index.html`.

---

## 6. Игра вдвоём по сети (с живым другом)

Кроме игры против бота есть второй режим: партия с живым человеком.
Работает это так:

1. Первый игрок нажимает в меню **Играть с другом** → **Создать игру**.
2. На экране появляется **код из четырёх знаков** (заглавные латинские буквы
   и цифры, например `K7QP`). Код можно продиктовать по телефону или переслать
   кнопкой «Скопировать код».
   Похожих на вид знаков в коде не бывает (ни буквы «O», ни нуля, ни единицы) —
   чтобы его можно было спокойно продиктовать вслух.
3. Второй игрок нажимает **Играть с другом** → **Войти по коду**, набирает
   этот код и нажимает **Войти в игру**. Маленькими или большими буквами —
   неважно, игра поймёт и так.
4. Партия начинается. Вместо бота за столом сидит друг.

Чтобы это работало, кому-то одному нужно держать включённой маленькую программу —
**сервер комнат**. Она просто знакомит двух игроков и следит за правилами:
на каком компьютере она запущена, неважно, играть могут оба.

### Как запустить сервер комнат

Ничего скачивать и устанавливать не нужно — только сам Node.js (тот же, что
и для бота, см. раздел 1). Откройте командную строку в папке проекта и наберите:

```
node server/сервер.js
```

Сервер займёт порт 8765 (порт — это «номер двери» на компьютере) и напишет:

```
Сервер игры вдвоём работает на порту 8765
Проверить: http://127.0.0.1:8765/здоровье
Остановить — Ctrl+C. Комнаты хранятся в памяти: после перезапуска начатые партии пропадут.
```

Если он пожалуется, что порт занят, возьмите другой номер — просто допишите
его в конце команды:

```
node server/сервер.js 8790
```

Тогда и во всех адресах ниже вместо 8765 подставляйте свой номер.

### Как проверить, что сервер жив

Откройте в браузере адрес:

```
http://127.0.0.1:8765/здоровье
```

Должна появиться короткая строчка:

```
{"живой":true,"комнат":0}
```

`живой: true` — сервер отвечает. Число рядом со словом `комнат` — сколько игр
идёт прямо сейчас; сразу после запуска там ноль, а когда друзья начнут играть,
число вырастет. Если браузер пишет «не удалось подключиться» — сервер не запущен
или запущен на другом порту.

### Как сказать игре, где искать сервер

Адрес сервера игра берёт **из адреса самой страницы** — из хвостика `?сервер=`:

```
index.html?сервер=https://адрес-сервера
```

Если хвостика нет, игра ищет сервер на своём же компьютере, по адресу
`http://127.0.0.1:8765`. Для проверки вдвоём на одном компьютере этого хватает:
откройте `index.html` в двух окнах браузера.

Через тот же хвостик адрес задаётся и в боте — **код при этом править не нужно**.
В файле `bot/.env` в строку `GAME_URL` впишите адрес игры вместе с хвостиком:

```
GAME_URL=https://ваш-логин.github.io/Games/?сервер=https://адрес-сервера
```

Переехал сервер на другой адрес — поменяйте одну эту строку и перезапустите
бота (`node bot/бот.js`). Ни в `index.html`, ни в `js/сеть.js` лезть не нужно,
и номер версии `?v=` поднимать тоже не нужно.

### Две настройки в `bot/.env`

Сервер комнат читает тот же файл настроек, что и бот. Обе строки необязательные —
без них всё работает.

```
GAME_SERVER_PORT=8790
GAME_SERVER_ORIGIN=https://другой-адрес-игры
```

- **`GAME_SERVER_PORT`** — на каком порту поднимать сервер, если 8765 чем-то занят.
  Номер, написанный прямо в команде (`node server/сервер.js 8790`), главнее:
  так порт можно поменять на ходу, не открывая файл с токеном бота.
- **`GAME_SERVER_ORIGIN`** — ещё один адрес страницы, которой сервер согласен
  отвечать. По умолчанию он отвечает только двум: опубликованной игре
  (её адрес записан в `server/сервер.js`) и вашему собственному компьютеру.
  Это защита: иначе любой посторонний сайт мог бы создавать комнаты у вас
  на компьютере. Если вы опубликовали игру у себя (свой логин на GitHub Pages
  или свой домен) — впишите этот адрес сюда, **без косой черты на конце**,
  например `https://ваш-логин.github.io`. Иначе игра скажет, что сервер ей
  не отвечает.

Никаких токенов и паролей в этих двух строках нет — они и не нужны.
Токен бота лежит в том же файле, и сервер комнат его не читает и никуда не выводит.

### Как вывести домашний сервер в интернет (туннель Cloudflare)

Пока сервер комнат живёт по адресу `http://127.0.0.1:8765`, он виден только
на вашем собственном компьютере. Телефон друга и тем более Telegram до него
не дотянутся. Нужен **туннель** — это как труба: одним концом она подключается
к вашему домашнему серверу, а другим торчит в интернете и получает временный
адрес с `https`. Кто зайдёт по этому адресу, попадёт к вам на компьютер —
при этом ничего настраивать в домашнем роутере не надо.

Мы пользуемся туннелем от Cloudflare — он бесплатный, и регистрироваться
для него не нужно.

**Шаг 1. Поставьте программу `cloudflared`.** Это и есть та самая труба,
одна небольшая программа. Скачать её для Windows можно на странице
<https://developers.cloudflare.com/cloudflare-one/networks/connectors/cloudflare-tunnel/downloads/>.
Проверить, что она уже стоит, можно командой:

```
cloudflared --version
```

Ответ будет вроде `cloudflared version 2026.5.2 (built ...)`. Если командная
строка отвечает «не является внутренней или внешней командой», обращайтесь
к программе полным путём — обычно она лежит здесь:

```
"C:\Program Files (x86)\cloudflared\cloudflared.exe" --version
```

**Шаг 2. Запустите сервер комнат** (см. выше) и запомните его номер порта.
Запускали командой без номера — значит, порт 8765; запускали
`node server/сервер.js 8790` — значит, 8790.

**Шаг 3. В отдельном окне командной строки запустите туннель.** Номер в конце
команды должен совпадать с портом сервера комнат — иначе труба упрётся в пустоту:

```
cloudflared tunnel --url http://127.0.0.1:8790
```

Программа насыплет много английских строчек — пугаться их не нужно. Секунд
через пять-десять среди них появится рамка с выданным адресом, вот такая:

```
INF +---------------------------------------------------------------------+
INF |  Your quick Tunnel has been created! Visit it at                     |
INF |  (it may take some time to be reachable):                            |
INF |  https://burst-mia-belongs-vat.trycloudflare.com                     |
INF +---------------------------------------------------------------------+
```

(в начале каждой строчки на экране стоит ещё дата и время — здесь они убраны,
чтобы не мешали читать.)

Строчка с `https://…trycloudflare.com` — это и есть временный адрес вашего
сервера в интернете. Он каждый раз новый и составлен из случайных английских
слов. Скопируйте его целиком. Cloudflare честно предупреждает в той же рамке,
что адрес может заработать не в ту же секунду: если сразу не открылся,
подождите полминуты.

**Окно с туннелем нужно держать открытым.** Закрыли его или нажали Ctrl+C —
труба исчезла, и игра вдвоём тут же перестала работать. Итого у вас должно
быть открыто два окна: одно с сервером комнат, второе с туннелем.

**Шаг 4. Проверьте, что труба дошла до сервера.** Откройте в браузере
(можно и с телефона) адрес туннеля с хвостиком `/здоровье`:

```
https://johnny-dense-shield-enquiries.trycloudflare.com/здоровье
```

Должна появиться та же короткая строчка, что и на домашнем адресе:

```
{"живой":true,"комнат":0}
```

Появилась — труба работает. Если вместо неё браузер показывает ошибку
(например, 502 или 530) либо пишет, что сайт не отвечает, причина одна из трёх:
сервер комнат не запущен, туннель смотрит не на тот порт, или окно с туннелем
уже закрыто и адрес умер.

**Шаг 5. Впишите адрес туннеля в настройки бота.** В файле `bot/.env` в строке
`GAME_URL` адрес игры и адрес сервера склеиваются через хвостик `?сервер=`:

```
GAME_URL=https://ваш-логин.github.io/Games/?сервер=https://johnny-dense-shield-enquiries.trycloudflare.com
```

После этого перезапустите бота (`node bot/бот.js`) и заново нажмите
в Telegram кнопку «Играть» — игра откроется уже с новым адресом сервера.
Ни `index.html`, ни `js/сеть.js` править не нужно, номер версии `?v=` поднимать
тоже не нужно.

Если игра опубликована под вашим логином на GitHub Pages, не забудьте про
строку `GAME_SERVER_ORIGIN` (см. выше): сервер отвечает только тем адресам
страницы, которые знает. Посторонний сайт он через туннель не пустит — проверено:
на запрос с чужого адреса приходит отказ «Этому сайту сервер игры не отвечает».
То есть труба наружу не открывает дверь кому попало.

### САМОЕ ВАЖНОЕ: адрес туннеля одноразовый

**Быстрый туннель выдаёт временный адрес. Каждый новый запуск `cloudflared`
даёт ДРУГОЙ адрес, а старый перестаёт работать.**

Значит, после каждого перезапуска туннеля строку `GAME_URL` в `bot/.env`
придётся вписывать заново и перезапускать бота. Это самая частая причина
жалобы «вчера играли вдвоём, сегодня пишет, что сервер не отвечает».

Если надоест переписывать — у Cloudflare есть **постоянный (именованный)
туннель**, у которого адрес не меняется. Но он требует бесплатной регистрации
в Cloudflare и своего доменного имени, и в этой инструкции он не описан.

### Честно о том, чего пока нет

Сервер комнат — это программа на **вашем домашнем компьютере**. Туннель
(см. выше) даёт ему адрес в интернете, но самого компьютера не заменяет. Значит:

- **Играть вдвоём можно только пока этот компьютер включён**, не ушёл в сон
  и на нём запущены **обе** программы: сервер комнат (`node server/сервер.js`)
  и туннель (`cloudflared tunnel --url …`). Закрыли одно из двух окон,
  уснул компьютер, пропал свет — партия обрывается, и новую начать нельзя.
- **Адрес туннеля временный.** Перезапустили туннель — адрес другой, и строку
  `GAME_URL` в `bot/.env` надо вписывать заново (см. предупреждение выше).
- **Игра внутри Telegram открывается по защищённому адресу (`https`), поэтому
  и сервер ей нужен по `https`.** К обычному `http://…` браузер обращаться
  запретит — это не поломка игры, а правило браузера. Игра в этом случае
  так и напишет. Как раз для этого и нужен туннель.
- **Комнаты живут в памяти.** Перезапустили сервер — все начатые партии пропали,
  нужно создавать игру заново.

Проще говоря: режим «вдвоём» работает из любой точки, пока ваш домашний
компьютер включён и обе программы запущены. Круглосуточной игры «сама по себе»
не будет — для этого нужен сервер, который включён всегда.

---

## 7. Как сделать кнопку «Играть» постоянной (по желанию)

Чтобы кнопка была всегда внизу чата с ботом, а не только в приветствии:

1. Напишите @BotFather команду `/setmenubutton`.
2. Выберите своего бота.
3. Отправьте адрес игры (тот же, что в `GAME_URL`).
4. Напишите название кнопки, например: `Играть`.

---

## 8. Что где лежит

```
Games/
├── index.html      главная страница игры — её и открывают
├── style.css       внешний вид
├── js/
│   ├── game.js     правила игры и соперник-бот
│   ├── сеть.js     игра вдвоём: разговор браузера с сервером комнат
│   └── telegram.js связь с Telegram и хранение рекордов
├── server/         сервер комнат для игры вдвоём (запускается отдельно)
│   ├── сервер.js   его и запускают: node server/сервер.js
│   ├── комнаты.js  кто с кем играет, коды и пропуска
│   ├── партия-по-сети.js  раздача и ходы двух живых игроков
│   ├── шлагбаум.js защита от подбора чужих кодов
│   └── проверка-двух-игроков.js  прогон партий без живого второго игрока
├── bot/
│   ├── бот.js      бот-лаунчер: кнопка «Играть» и приглашение другу
│   ├── .env        ваши токен и адрес игры (не попадает в репозиторий)
│   └── .env.example образец этого файла с пояснениями
├── tests/          проверки игры
└── README.md       эта инструкция
```

---

## 9. Если что-то не работает

**«Не нашёл файл с настройками бота»** — вы ещё не сделали копию `.env.example`
под именем `.env`. Вернитесь к разделу 3.

**«Telegram не признал токен»** — токен скопирован не целиком или устарел.
Напишите @BotFather `/mybots`, выберите бота и возьмите токен заново.

**«Адрес игры должен начинаться с https://»** — Telegram не умеет открывать игру
с вашего компьютера (`http://localhost`). Сначала опубликуйте игру (раздел 5).

**«Похоже, этот же бот уже где-то запущен»** — вы запустили бота дважды.
Закройте лишнее окно командной строки: Telegram отдаёт сообщения только одной копии.

**Кнопка «Играть» есть, но игра не открывается** — проверьте, что адрес из `GAME_URL`
открывается в обычном браузере и начинается с `https://`.

**Кнопка «Позвать друга» пишет «ничего не найдено»** — не включён режим подсказок,
см. конец раздела 4.

**Поправили файл и загрузили на GitHub, а в игре всё по-старому** — забыт номер
версии. Поднимите `?v=` у изменённого файла в `index.html` (см. конец раздела 5).

**В настройках репозитория нет пункта «Pages»** — репозиторий закрытый.
На бесплатном тарифе Pages работает только из открытого (Public), см. раздел 5.

**Игра с другом пишет, что сервер не отвечает** — сервер комнат не запущен
(`node server/сервер.js`), запущен на другом порту или в адресе игры не тот
хвостик `?сервер=`. Сначала проверьте адрес `http://127.0.0.1:8765/здоровье`
в браузере, см. раздел 6.

**Сервер отвечает «Этому сайту сервер игры не отвечает»** — игра открыта
по адресу, которого сервер не знает. Впишите этот адрес в строку
`GAME_SERVER_ORIGIN` файла `bot/.env` и перезапустите сервер, см. раздел 6.

**Внутри Telegram игра с другом не начинается, а в браузере на компьютере
работает** — в Telegram страница открыта по `https`, и обращаться к домашнему
`http://127.0.0.1:…` браузер не даёт. Нужен адрес сервера по `https`: поднимите
туннель, см. подраздел «Как вывести домашний сервер в интернет» в разделе 6.

**Вчера играли вдвоём, сегодня пишет, что сервер не отвечает** — почти всегда
перезапущен туннель, а у него каждый раз новый адрес. Возьмите свежий адрес
из окна `cloudflared`, впишите его в `GAME_URL` в `bot/.env` и перезапустите
бота. Заодно проверьте, что окно с туннелем и окно с сервером комнат открыты оба.

**«Порт уже занят»** — на этом номере работает другая программа или вторая копия
сервера. Запустите с другим номером: `node server/сервер.js 8790`.

**Рекорды пропали** — если вы играли в обычном браузере и почистили его историю,
рекорды из памяти браузера удаляются вместе с ней. Внутри Telegram рекорды хранятся
в вашем облаке Telegram и так не теряются.
